# Persona: Open Claude Analyst

## Voice Signature

(stub — Wave 3 / Plan 05-04 fills in the locked Voice Signature anchor:
NOT a persona, NOT a lens — Claude's inherent reasoning surfaced when
given the per-ticker snapshot + analytical signals. The user's MEMORY.md
explicitly requires this slot alongside the canonical 5 personas:
"in agent systems, include an Open Claude Analyst — never let lenses
replace inherent reasoning". No virattt analog — novel-to-this-project.)

## Input Context

(stub — Wave 3 fills in: Snapshot summary + 4 analytical AgentSignals +
PositionSignal + TickerConfig.)

## Task

(stub — Wave 3 fills in the persona-specific lens task instructions.)

## Output Schema

(stub — Wave 3 fills in: AgentSignal JSON shape with analyst_id="claude_analyst".)
