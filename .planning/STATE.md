---
gmd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
current_plan: Not started
status: planning
last_updated: "2026-05-04T19:10:58.462Z"
progress:
  total_phases: 9
  completed_phases: 8
  total_plans: 34
  completed_plans: 34
---

# State: Markets

## Project Reference

See: `.planning/PROJECT.md` (last updated 2026-04-30)

**Core value:** Every morning, in one screen — which watchlist tickers need attention today and why, across short-term tactical and long-term strategic horizons.

**Current focus:** Phase 4 (Position-Adjustment Radar) **COMPLETE** — all 3 plans shipped (04-01 foundation, 04-02 PositionSignal schema, 04-03 position_adjustment analyst). POSE-01..05 all `[x]` in REQUIREMENTS.md. analysts/position_adjustment.py provides pure-function score(snapshot, config, *, computed_at=None) -> PositionSignal with 6 hand-rolled pandas indicator helpers (RSI / BB position / z-score-vs-MA50 / Stochastic %K / Williams %R / MACD histogram z-scored over 60 bars), ADX(14) trend-regime gating (>25 downweights mean-reversion to 0.5x; ambiguous zone 20-25 keeps weights at 1.0), 5-state PositionState ladder + 4-state ActionHint mapping, confidence-as-agreement-count formula with abstain rule, empty-data UNIFORM RULE 5 branches. Cross-cutting invariant test extends Phase 3's pattern. Full repo: 428 passed (310 baseline + 118 new across Phase 4); 94% coverage on the analyst module. Phase 5 (Claude Routine Wiring) unblocked — can now read PositionSignal alongside the 4 AgentSignals.

**Phase 4 lineage:** Phase 4 (Position-Adjustment Radar) IN PROGRESS — Wave 0 foundation refactor (Plan 04-01) complete. `analysts/_indicator_math.py` extracted (~165 LOC); `_build_df` + `_adx_14` + `_total_to_verdict` consolidated to single source of truth (DRY trigger fired on 4th copy). 5 ADX module constants live alongside `_adx_14`. 3 caller modules (`analysts/{fundamentals, technicals, valuation}.py`) refactored via import-only changes. 3 new synthetic-history builders appended to `tests/analysts/conftest.py` for explicit overbought/oversold/range regression testing. Phase 3 regression invariant held: all 310 existing tests stayed GREEN. 23 new tests added (19 indicator_math + 4 synthetic_fixtures). Full repo: 333 passed. Wave 1 (04-02 PositionSignal schema) and Wave 2 (04-03 Position-Adjustment analyst) unblocked.

**Phase 3 lineage:** Phase 3 (Analytical Agents — Deterministic Scoring) **COMPLETE** — all 5 plans shipped (03-01 signals scaffold + 03-02 fundamentals + 03-03 technicals + 03-04 news_sentiment + 03-05 valuation). All four analyst modules in `analysts/` (fundamentals.py, technicals.py, news_sentiment.py, valuation.py) emit pure-function `AgentSignal` per ticker with the 5-state ladder verdict + 0-100 confidence + evidence list contract; every analyst respects the empty-data UNIFORM RULE (data_unavailable=True, verdict='neutral', confidence=0, ≥1 evidence string when inputs are missing). The 2 cross-cutting invariant tests in `tests/analysts/test_invariants.py` (`test_always_four_signals` + `test_dark_snapshot_emits_four_unavailable`) flipped GREEN naturally upon Wave-2-closeout xfail-marker removal in 03-05's Task 3 — confirming all four analysts respect the cross-cutting "always 4 signals" + "dark snapshot ⇒ 4 data_unavailable=True" contracts from 03-CONTEXT.md. ANLY-01..04 all `[x]` in REQUIREMENTS.md. Phase 4 (Position-Adjustment Radar) unblocked — POSE-01..05 will reuse the hand-rolled pandas indicator pattern from 03-03 + the multi-method weighted aggregation pattern from 03-05.

## Current Phase

**Phase:** 08-mid-day-refresh-resilience **In Progress (1/2 plans complete)**
**Status:** Ready to plan
**Current Plan:** Not started
**Total Plans in Phase:** 2
**Next:** `/gmd:execute-plan 08-02` for Wave 1 — frontend/src/schemas/refresh.ts (zod discriminated union over the 3 envelope shapes locked by api/refresh.py + tests/api/test_refresh.py) + frontend/src/lib/useRefreshData.ts (TanStack hook with keepPreviousData + 5-min staleTime + retry: 1) + frontend/src/components/CurrentPriceDelta.tsx (preserves data-testid="current-price-placeholder"; covers loading + 4 happy + partial + full-failure + isError) + dual-route mount (TickerRoute + DecisionRoute replacing the Phase 7 PHASE-8-HOOK placeholder) + frontend/tests/e2e/resilience.spec.ts (3 specs: 500-on-ticker, 500-on-decision, partial-response). Plan 08-01 just closed: backend Vercel Python serverless `/api/refresh` (BaseHTTPRequestHandler + sys.path bootstrap; 3 locked envelope shapes — success / partial / full-failure-without-current-price-field), repo-root vercel.json maxDuration=30, **frontend/vercel.json SPA rewrite narrowed `/(.*)` → `/((?!api/).*)`** so /api/* requests stop silently returning the SPA shell, routine/memory_log.py + Phase E hook in run_for_watchlist (one record per (ticker, persona) per non-lite run; gated by `if result.persona_signals:`), scripts/check_provenance.py walking 6 source roots accepting 3 marker forms (case-insensitive on keyword phrases; either comment or docstring location), .pre-commit-config.yaml local hook entry, codebase audit pass adding 22+6 markers. Python pytest 704 (659 baseline + 45 new). REFRESH-01 + INFRA-06 + INFRA-07 closed; REFRESH-04 backend half closed (waiting on Wave 1 frontend resilience.spec.ts).

**Phase 6 lineage:** Phase 6 (Frontend MVP) COMPLETE — 5/5 plans, 14 VIEW + INFRA-05 reqs closed. Wave 4 (Plan 06-05 polish + responsive + Playwright) just closed: ErrorBoundary class component (3 render branches: SchemaMismatch / NotFound ticker-vs-snapshot / Generic — all expose Reload/Back link via reset method) catching SchemaMismatchError + FetchNotFoundError per CONTEXT.md UNIFORM RULE; DateSelector native <select> reading data/_dates.json via useDates() TanStack hook (newest-first ordering; preserves route shape /scan/:date ↔ /ticker/:symbol/:date and ?lens= query param; disabled while loading; today-only fallback on 404); VIEW-12 mobile-responsive verified end-to-end (Chart touch handleScroll horzTouchDrag+vertTouchDrag false + handleScale pinch + responsive height bracket 280/360/480; LensTabs -mx-4 overflow-x-auto narrow-viewport containment + min-h-11 44px Apple HIG tap-target; Header flex-col sm:flex-row stacks on mobile). 18 new vitest tests (8 ErrorBoundary + 6 DateSelector + 4 loadDates) + 10 new Playwright E2E (6 responsive + 3 error-boundary + 1 full-flow). Full Playwright suite: 60 passed + 6 skipped (mobile-only on chromium-desktop) across all 3 projects (chromium-desktop + mobile-safari iPhone 14 + mobile-chrome Pixel 7) in 13.2s; 197 vitest unit/component tests across 27 test files (179 Wave 3 baseline + 18 Wave 4); 659 Python pytest tests still green. Visual taste-check via code audit confirms Notion-Clean spec adherence (zero ad-hoc hex outside Chart.tsx PALETTE, zero gradients/backdrop-blur/glassmorphism, all shadcn shadow-sm overridden with shadow-none — hairline borders only; Inter + JetBrains Mono fonts loaded via Google Fonts CDN; prefers-reduced-motion respected). 1 deviation auto-fixed (Rule 3 blocking — pnpm exec playwright install webkit; Wave 1 SUMMARY explicitly predicted webkit binary install on first Wave 4 run) + 4 plan-wording refinements (ErrorBoundary scoped as safety-net not primary error surface; DateSelector tests use vi.mock; E2E error-wording regex accepts both ErrorBoundary + inline-banner phrasings; visual taste-check captured as code audit not screenshot diffs). Real-device manual verifications (iOS Safari, Android Chrome on actual phones; Vercel deploy + real GitHub raw URL; final visual taste judgment) deferred to user per VALIDATION.md Manual-Only Verifications table — those are user-facing concerns the assistant cannot regress and they do NOT block Phase 7. Wave 3 (Plan 06-04 deep-dive) lineage: `/ticker/:symbol/:date?` ships with 7 sections top-down — Heading + OpenClaudePin (VIEW-09 always-rendered lock + accent-tinted distinct styling) + 2 TimeframeCards (VIEW-05 dual-timeframe with thesis_status pills) + Chart (VIEW-06 lightweight-charts 5.2 v5-API rendering OHLC + MA20/MA50/BB upper-lower/RSI(14) sub-pane) + 5 PersonaCards in grid (claude_analyst FILTERED OUT — VIEW-09 separation lock at React layer) + 4 AnalyticalSignalCards + NewsList grouped by source (VIEW-08 with NEW tag for post-snapshot headlines). Header gets VIEW-13 ticker search input — Enter navigates to /ticker/:typed/:date with uppercase normalization. loadTickerData.useTickerData TanStack hook + __loadTickerForTest test export. AAPL fixture extended 3→9 headlines across 3 sources for NewsList grouping E2E. Wave 1 (Plan 06-02 frontend scaffold) lineage: `frontend/` directory scaffolded at repo root with the full locked stack (Vite 6.4 + React 19.2 + TypeScript 5.9 + Tailwind v4.2 CSS-first @theme + react-router 7.14 data-router + TanStack Query 5.100 + zod 4.4 + lightweight-charts 5.2 + clsx + tailwind-merge); Notion-Clean palette tokens (#0E0F11 / #1F2024 / #2A2C30 / #E8E9EB / #8B8E94 / #5B9DFF + state colors) in src/index.css @theme directive; Inter UI + JetBrains Mono numerics typography loaded via Google Fonts; 6 zod schemas (agent_signal/position_signal/ticker_decision/snapshot/status/dates_index) mirror the Phase 3/4/5 + Wave 0 Pydantic shapes 1:1, with Pydantic data_unavailable invariants mirrored as zod refine() refinements AND `schema_version: z.literal(2)` STRICTLY rejecting v1 snapshots per CONTEXT.md UNIFORM RULE; fetchAndParse<T> single GitHub-raw read boundary throws SchemaMismatchError (carries url + zodError) on parse failure and FetchNotFoundError on 404; URL builders snapshotUrl/indexUrl/statusUrl/datesUrl co-located; computeStaleness encodes VIEW-11 6h/24h thresholds with SIX_HOURS_MS + TWENTY_FOUR_HOURS_MS module constants and locked boundary semantics (exactly 6h → AMBER, exactly 24h → AMBER, 24h+1ms → RED); format helpers (formatNumber/formatPercent/formatCompact/formatDate UTC YYYY-MM-DD/formatTicker uppercase+dot→hyphen); react-router v7 data-router with /→/scan/today loader redirect + /scan/:date stub + /ticker/:symbol/:date? stub + Root layout with staleness slot placeholder; shadcn/ui CLI configured (components.json) + ui/.gitkeep dir reserved for Wave 2-3 vendoring; Vercel deploy config (vercel.json) with load-bearing SPA-fallback rewrite (.*) → /index.html; .env.example documents VITE_GH_USER + VITE_GH_REPO; Playwright 3-project config (chromium-desktop + mobile-safari iPhone 14 + mobile-chrome Pixel 7) with webServer pnpm preview. Wave 3 (Plan 06-04 deep-dive) just closed: `/ticker/:symbol/:date?` ships with 7 sections top-down — Heading + OpenClaudePin (VIEW-09 always-rendered lock + accent-tinted distinct styling) + 2 TimeframeCards (VIEW-05 dual-timeframe with thesis_status pills) + Chart (VIEW-06 lightweight-charts 5.2 v5-API rendering OHLC + MA20/MA50/BB upper-lower/RSI(14) sub-pane) + 5 PersonaCards in grid (claude_analyst FILTERED OUT — VIEW-09 separation lock at React layer) + 4 AnalyticalSignalCards + NewsList grouped by source (VIEW-08 with NEW tag for post-snapshot headlines). Header gets VIEW-13 ticker search input — Enter navigates to /ticker/:typed/:date with uppercase normalization. loadTickerData.useTickerData TanStack hook + __loadTickerForTest test export. AAPL fixture extended 3→9 headlines across 3 sources for NewsList grouping E2E. 179 vitest tests + 12 Playwright E2E specs on chromium-desktop (1 smoke + 5 morning-scan + 6 deep-dive) all green. typecheck + build clean. 2 deviations auto-fixed (Rule 1 lightweight-charts v4→v5 API mismatch in plan pseudocode; Rule 3 Playwright stale-build foot-gun on first deep-dive run — pnpm build before re-run). VIEW-04..09 + VIEW-12 (responsive groundwork) + VIEW-13 closed; VIEW-12 final mobile-safari verification + VIEW-14 (date selector) + VIEW-15 (zod-mismatch ErrorBoundary) + INFRA-05 closeout (Vercel deploy + real GitHub raw URL) all land in Wave 4. Wave 1 (Plan 06-02 frontend scaffold) lineage: `frontend/` directory scaffolded at repo root with the full locked stack (Vite 6.4 + React 19.2 + TypeScript 5.9 + Tailwind v4.2 CSS-first @theme + react-router 7.14 data-router + TanStack Query 5.100 + zod 4.4 + lightweight-charts 5.2 + clsx + tailwind-merge); Notion-Clean palette tokens (#0E0F11 / #1F2024 / #2A2C30 / #E8E9EB / #8B8E94 / #5B9DFF + state colors) in src/index.css @theme directive; Inter UI + JetBrains Mono numerics typography loaded via Google Fonts; 6 zod schemas (agent_signal/position_signal/ticker_decision/snapshot/status/dates_index) mirror the Phase 3/4/5 + Wave 0 Pydantic shapes 1:1, with Pydantic data_unavailable invariants mirrored as zod refine() refinements AND `schema_version: z.literal(2)` STRICTLY rejecting v1 snapshots per CONTEXT.md UNIFORM RULE; fetchAndParse<T> single GitHub-raw read boundary throws SchemaMismatchError (carries url + zodError) on parse failure and FetchNotFoundError on 404; URL builders snapshotUrl/indexUrl/statusUrl/datesUrl co-located; computeStaleness encodes VIEW-11 6h/24h thresholds with SIX_HOURS_MS + TWENTY_FOUR_HOURS_MS module constants and locked boundary semantics (exactly 6h → AMBER, exactly 24h → AMBER, 24h+1ms → RED); format helpers (formatNumber/formatPercent/formatCompact/formatDate UTC YYYY-MM-DD/formatTicker uppercase+dot→hyphen); react-router v7 data-router with /→/scan/today loader redirect + /scan/:date stub + /ticker/:symbol/:date? stub + Root layout with staleness slot placeholder; shadcn/ui CLI configured (components.json) + ui/.gitkeep dir reserved for Wave 2-3 vendoring; Vercel deploy config (vercel.json) with load-bearing SPA-fallback rewrite (.*) → /index.html; .env.example documents VITE_GH_USER + VITE_GH_REPO; Playwright 3-project config (chromium-desktop + mobile-safari iPhone 14 + mobile-chrome Pixel 7) with webServer pnpm preview; 94 vitest tests passing (64 schema + 16 staleness + 14 fetchSnapshot) + 1 Playwright smoke E2E green proving / → /scan/today redirect + heading visible. 5 deviations auto-fixed (TS6310 composite-noEmit, vitest-vite peer clash via mergeConfig, tsbuildinfo gitignore, vite/client typings, pnpm 10 onlyBuiltDependencies for esbuild postinstall). Wave 2 imports schemas + fetchAndParse + computeStaleness + Tailwind palette without modification.

## Phase Status

| # | Phase | Status |
|---|-------|--------|
| 1 | Foundation — Watchlist + Per-Ticker Config | Complete (5/5 plans) |
| 2 | Ingestion — Keyless Data Plane | Complete (7/7 plans) |
| 3 | Analytical Agents — Deterministic Scoring | Complete (5/5 plans) |
| 4 | Position-Adjustment Radar | Complete (3/3 plans) |
| 5 | Claude Routine Wiring — Persona Slate + Synthesizer | Complete (6/6 plans) |
| 6 | Frontend MVP — Morning Scan + Deep-Dive | Complete (5/5 plans) |
| 7 | Decision-Support View + Dissent Surface | Complete (1/1 plans) |
| 8 | Mid-Day Refresh + Resilience | In Progress (1/2 plans) |
| 9 | Endorsement Capture | Pending |

## Recent Decisions

- **2026-05-04 (Phase 8 / Plan 01 — backend-refresh — Wave 0 / REFRESH-01 + INFRA-06 + INFRA-07 + REFRESH-04 backend)**: Wave 0 backend shipped — Vercel Python serverless `/api/refresh` (181 LOC) wraps existing `ingestion.prices.fetch_prices` + `ingestion.news.fetch_news(return_raw=True)` via `BaseHTTPRequestHandler` subclass `handler` with `sys.path.insert(0, parents[1])` bootstrap so `from ingestion.prices import fetch_prices` resolves under Vercel's runtime. Pure `_build_response(path) -> (dict, int)` builder is the test surface; `do_GET` is thin HTTP plumbing exercised via 2 MagicMock-based integration tests. NO LLM imports (keyless data plane lock); NO CORS headers (same-origin). 3 locked envelope shapes: success ({ticker, current_price, price_timestamp, recent_headlines, errors:[], partial:false}); partial ({ticker, current_price, recent_headlines:[], errors:["rss-unavailable"], partial:true}); full-failure ({ticker, error:true, errors:["yfinance-unavailable","yahooquery-unavailable",...], partial:true} — NO current_price field — frontend dispatches via inspecting envelope, not status code). 400 reserved for missing/invalid ticker param; all other failure modes are 200 with error envelope. `parse_qs(keep_blank_values=True)` is load-bearing — distinguishes `?ticker=` empty value (→invalid-ticker) from no query string (→missing-ticker-param). `vercel.json` at repo root configures maxDuration=30 (10× typical execution; well under 300s Hobby cap). **Load-bearing fix: `frontend/vercel.json` SPA rewrite narrowed `/(.*)` → `/((?!api/).*)`** so `/api/*` requests stop silently returning the SPA HTML shell. `routine/memory_log.py` (103 LOC) `append_memory_record(*, date, ticker, persona_id, verdict, confidence, evidence_count, log_path=None)` mirrors `routine.llm_client._log_failure` atomic-append discipline verbatim (mkdir parents=True; mode='a'; sort_keys=True; one record per line). Schema validation raises ValueError on date format / ticker case / verdict ladder / 0–100 confidence / 0–10 evidence_count violations. `routine/run_for_watchlist.py` Phase E hook fires at the watchlist-loop level after each TickerResult append; gated by `if result.persona_signals:` which naturally skips lite_mode AND per-ticker pipeline failures. Memory log failures non-fatal (logger.warning + continue). New `memory_log_path: Path | None = None` kwarg on run_for_watchlist (defaults to None → DEFAULT_LOG_PATH `memory/historical_signals.jsonl`); tests inject tmp_path. `.gitignore` extended with `memory/historical_signals.jsonl` + `memory/llm_failures.jsonl` (local-only v1; v1.x revisits cross-device persistence). `scripts/check_provenance.py` (186 LOC) walks DEFAULT_ROOTS (analysts/, routine/, synthesis/, ingestion/, api/, scripts/) + MD_TARGETS (prompts/personas + prompts/synthesizer.md) accepting 3 marker forms (Pattern adapted from <ref>/<path>, Adapted from <ref>/<path>, novel-to-this-project) within first 30 lines, case-insensitive on keyword phrases, in EITHER comment syntax (# / <!-- -->) OR plain text inside a docstring. Empty `__init__.py` package markers skipped; recursive walk via rglob; main() exit 0/1 with offender list to stderr. `.pre-commit-config.yaml` local hook entry (entry: python scripts/check_provenance.py; pass_filenames: false; always_run: true). Codebase audit pass: 22 .py + 6 .md files received `# novel-to-this-project` (or `<!-- novel-to-this-project -->`) markers — schemas (analysts/data/*, analysts/schemas, analysts/position_signal, analysts/_indicator_math), ingestion adapters (errors, filings, fundamentals, http, manifest, news, prices, refresh, social, __init__), routine package markers + storage, synthesis package markers + dissent, persona prompts (buffett, burry, lynch, munger, wood + synthesizer). Existing Phase 3 analyst modules with `Adapted from virattt/ai-hedge-fund/...` already in docstrings now match the loosened regex without duplication. `python scripts/check_provenance.py` exits 0 against the live codebase (48 files scanned). 2 explicit prices resilience tests (yfinance throws → yahooquery rescues; both throw → data_unavailable=True) + 1 explicit news resilience test (all RSS broken → empty result, never raises) characterize the existing graceful-failure contract under the REFRESH-04 lock — these passed on first add (RED-skip / characterization tests, not RED→GREEN). **Decisions:** (1) Provenance regex loosened from comment-form-only to also accept docstring location + case-insensitive keyword phrases. The marker text carries the information; the comment syntax is incidental. Still requires the `<ref>/<path>` token (`\S+/`) so generic prose like "Adapted to the project's needs" is rejected. Avoided 9-file edit churn or duplicating already-documented provenance. (2) Plan's `<interfaces>` block claimed `fetch_news(return_raw=True) -> (headlines, sentiment_score)` — actual Phase 6 / Plan 06-01 signature is `(list[Headline], list[dict])` where the dict list carries the {source, published_at, title, url} 4-key shape. api/refresh.py consumes the dict list directly — it IS the locked frontend shape. (3) `parse_qs(keep_blank_values=True)` is load-bearing — without it, `?ticker=` (empty value) becomes indistinguishable from no query string (both → empty dict). Plan locks two distinct error markers; the flag is non-negotiable. (4) Provenance markers prepended ABOVE module docstrings (line 1) so SCAN_LINE_LIMIT=30 catches them even when docstrings grow. (5) Memory log uses AgentSignal.analyst_id directly (e.g. "buffett", "claude_analyst") — JSONL is queryable by canonical persona ID without lookup table. (6) Resilience tests as characterization tests (passed on first add) — they document existing fetch_prices + fetch_news graceful-failure contracts under REFRESH-04 lock; future regression breaking ingestion forgiveness fails loudly. **Tests:** RED→GREEN per task = 6 task commits (3 test commits + 3 feat commits). 13 api/refresh tests + 13 memory_log + 3 Phase E + 13 check_provenance + 2 prices resilience + 1 news resilience = 45 new tests. Full repo: 704 passed (659 baseline + 45 new). Plan-level verify (`python -m pytest tests/api/ tests/routine/test_memory_log.py tests/routine/test_run_for_watchlist.py tests/scripts/test_check_provenance.py tests/ingestion/test_prices.py tests/ingestion/test_news.py -q && python scripts/check_provenance.py`) all green. Frontmatter + plan-structure validation passed. **Phase 8 status: In Progress (1/2 plans complete — Wave 0 backend shipped; Wave 1 frontend integration pending).** REQUIREMENTS.md flips REFRESH-01 + INFRA-06 + INFRA-07 from Pending → Complete (REFRESH-04 stays Pending until Wave 1 frontend resilience.spec.ts lands — backend half is in place via the 3 ingestion resilience tests + 4 api/refresh failure-mode tests). ROADMAP.md Phase 8 row → 1/2 plans, status "In Progress"; Phase 8 plan list 08-01 entry [ ] → [x]. Plan-level commits: `0cd223d` (Task 1 RED — api/refresh tests), `4bbcd76` (Task 1 GREEN — refresh handler + vercel.json + SPA narrowing), `43de55e` (Task 2 RED — memory_log + Phase E tests), `ecf6dd3` (Task 2 GREEN — memory_log + run_for_watchlist Phase E hook + .gitignore), `a04687f` (Task 3 RED — check_provenance + ingestion resilience tests), `dd396e1` (Task 3 GREEN — check_provenance.py + .pre-commit-config + 22+6 audit-pass markers). **Deviations:** 3 auto-fixed (1 Rule 1 bug — parse_qs default drops empty values; fixed with keep_blank_values=True. 1 Rule 1 bug — provenance regex too strict for existing Phase 3 docstrings; loosened with case-insensitive keyword phrases + docstring-location acceptance. 1 Rule 3 blocking — `uv` not available in venv; substituted `python -m pytest`; identical execution.) Zero scope creep — all auto-fixes preserve the locked plan scope. **Phase 8 Wave 1 (08-02 frontend integration) UNBLOCKED** — locked response shapes documented in api/refresh.py + tests/api/test_refresh.py give the Wave 1 zod schema a verbatim mirror target; useRefreshData TanStack hook can mock against the exact envelopes; resilience.spec.ts can hit a deployed preview function or local mock. Manual verifications (Vercel preview deploy + cold-start measurement; real-browser refresh+snapshot UX taste judgment; SPA-rewrite-narrowing canary on preview URL) deferred to user per VALIDATION.md Manual-Only Verifications table — not blocking on Wave 1 execution. **Note: `.planning/config.json` left unstaged** per phase context handoff (auto-mode flag from session machinery; separate concern). Next: `/gmd:execute-plan 08-02`.

- **2026-05-04 (Phase 7 / Plan 01 — decision-support — VIEW-10 — PHASE 7 CLOSEOUT)**: Phase 7 complete in a single plan. Decision-Support View at /decision/:symbol/:date? mounted with 4 supporting components + cross-link symmetry. **Components:** (a) **RecommendationBanner** — 6 actions × 3 conviction bands = 18-state hero element. Action drives color via Record<DecisionRecommendation, string> map (add/buy → bullish + /10 vs /15 opacity differentiator; trim/take_profits → amber + /10 vs /15; hold → fg-muted/10; avoid → bearish/10) — compile-time exhaustiveness so adding a new enum value at @/schemas flags both ACTION_COLOR + ACTION_LABEL maps as missing keys. Conviction drives visual weight via Record<ConvictionBand, string> map (low → text-2xl font-medium; medium → text-3xl font-semibold; high → text-4xl font-bold). Embedded ConvictionDots indicator. role="status" + aria-label combining action label + conviction word. (b) **ConvictionDots** — 3-dot indicator with Pitfall #6 layout-shift guard: `Array.from({ length: 3 }, (_, i) => i < filled ? 'filled' : 'empty')` — ALWAYS 3 dots, never length: filled. Filled dot bg-fg solid; empty dot border border-fg-muted/50 hairline. (c) **DriversList** — short_term + long_term DriverCard pair in lg:flex-row / flex-col container. Each card has timeframe label + thesis-status pill (5-state STATUS_COLOR map mirroring TimeframeCard pattern) + confidence pill in header; body is drivers <ul> OR empty-state copy "No drivers surfaced" italicized inside the card (UNIFORM RULE — slot is always architecturally present, NEVER collapsed even when both timeframes have empty drivers arrays). DECOUPLED from TimeframeCard: TimeframeCard renders summary+status+confidence+drivers (deep-dive analysis-first); DriversList renders ONLY drivers (decision-support — recommendation IS the summary; visual overlap dilutes banner authority). (d) **DissentPanel** — Pitfall #12 always-render shell — NO `if (!has_dissent) return null` early-return; both branches render the data-testid="dissent-panel" container. has_dissent=false → muted "All personas converged. No dissent surfaced." panel with border-border/30 bg-surface/50 (intentional silence beats silent absence — user reads "no one disagrees" not "panel forgot to load"). has_dissent=true → bordered panel; PERSONA_LABEL local const maps 5 canonical investors (buffett/munger/wood/burry/lynch) + claude_analyst to display names; isClaude boolean drives accent vs amber path. **Pitfall #2 LOCK**: when dissenting_persona === 'claude_analyst', uses border-accent/40 bg-accent/5 (verbatim OpenClaudePin palette from Phase 6) AND header text reads "Open Claude Dissent" (NOT "{Persona} Dissents") — the wording itself signals "Claude's reasoning surfaced alongside personas, never replaced" per user MEMORY.md feedback_claude_knowledge lock. null persona → "Unknown persona" fallback; empty dissent_summary → italicized "No summary provided." fallback. (e) **DecisionRoute** at /decision/:symbol/:date? — third pillar route. Mirrors TickerRoute typed-error branches verbatim (FetchNotFoundError → "{symbol} not in snapshot for {date}"; SchemaMismatchError → "Schema upgrade required"; generic Error → bearish-tinted error block) — Pitfall #5 (don't write a custom error block) honored. Composition order: heading + ← Deep dive cross-link → data_unavailable notice (only when dec.data_unavailable === true; muted "Data unavailable for some signals — recommendation may be partial" ABOVE banner; banner still shows Hold/low per Phase 5 schema invariant) → RecommendationBanner hero → **PHASE-8-HOOK current-price-placeholder block (LOAD-BEARING)**: BOTH the source comment `// PHASE-8-HOOK: current-price-delta` AND the `data-testid="current-price-placeholder"` attribute present in DecisionRoute.tsx — Phase 8's plan-phase has two grep targets (one source-level, one runtime DOM); placeholder copy reads "Snapshot price as of {ISO} ET. Current-price delta arrives via mid-day refresh in Phase 8." → DriversList → Separator → DissentPanel (always rendered) → open_observation accent block (border-accent/40 bg-accent/5 — same accent palette continuity as DissentPanel claude case + OpenClaudePin) when dec.open_observation truthy. Lite-mode (ticker_decision === null) branch: explicit amber "No recommendation for {symbol} on {date}" notice mirrors TickerRoute null-decision posture — intentional state, not blank page. **Cross-link symmetry**: TickerRoute heading row extended with "→ Decision view" Link wrapped alongside existing "← Back to scan" in flex container (gap-4) preserving Phase 6 wrap-friendly aesthetic; DecisionRoute heading has mirror "← Deep dive" link. Date preserved on round-trip (verified by Playwright spec). App.tsx: third route entry { path: 'decision/:symbol/:date?', element: <DecisionRoute /> }. Root.tsx: comment-only update — useActiveDate useMatches loop is route-shape-agnostic so zero code change. **Decisions:** (1) Color × weight matrix locked verbatim from PLAN frontmatter / CONTEXT.md — action axis × conviction axis with /10 vs /15 opacity within bullish + amber color pairs gives the within-axis differentiation without introducing a new color token. (2) Pitfall #12 explicit positive-assertion test — `expect(getByTestId('dissent-panel')).toBeInTheDocument()` for has_dissent: false case; would fail loud if any future refactor added an early-return-null regression. The test asserts presence, not absence — this is the load-bearing inversion that catches the confirmation-bias-guard regression. (3) Pitfall #2 accent palette verbatim from OpenClaudePin (border-accent/40 bg-accent/5) — NOT a new variant; ensures Open Claude visual signature is consistent across deep-dive (OpenClaudePin pinned at top) and decision-support (DissentPanel claude_analyst dissent path). (4) PERSONA_LABEL local in DissentPanel — single-use; deferred extraction to a shared lookup file until 2nd consumer materializes. (5) Phase 8 hookpoint pair (source comment + data-testid) — Phase 8 grep gets both source-level location AND runtime DOM target. Reduces ambiguity for the integration point: source comment tells the planner WHERE to edit; data-testid lets E2E specs verify the wired-in <CurrentPriceDelta /> rendered in the right slot. (6) Cross-link wrapping in TickerRoute — both Links grouped in flex container preserves Phase 6 flex-wrap aesthetic on narrow viewports; gap-4 maintains visual rhythm; no new row introduced. (7) DriversList decoupled from TimeframeCard — recommendation IS the summary in decision-support context; rendering both summary + drivers in DriversList would dilute banner authority. (8) Lite-mode null-decision branch with explicit amber notice — mirrors TickerRoute null-decision posture. **Tests:** RED → GREEN per task = 6 production commits (3 RED test commits + 3 GREEN impl commits). 37 new vitest tests (3 ConvictionDots + 19 RecommendationBanner + 4 DriversList + 6 DissentPanel + 5 DecisionRoute) → suite 197 → 234. 1 new Playwright spec (decision.spec.ts) round-trip /scan → /ticker → /decision → /ticker preserving date; verified banner data-recommendation attr against NVDA fixture (take_profits); 2 driver cards; dissent has_dissent='true'; current-price-placeholder visible. Full Playwright suite 60 → 63 passed (+3 = decision spec on all 3 projects: chromium-desktop + mobile-safari + mobile-chrome) + 6 mobile-only-skipped-on-desktop (unchanged). Python pytest 659 still green (no Python changes). typecheck clean, build clean. **Plan-gate greps:** PHASE-8-HOOK source comment present (DecisionRoute.tsx lines 22 + 177); current-price-placeholder data-testid present (line 183); ZERO inline hex (`grep -nE "#[0-9a-fA-F]{3,8}"` returns 0 across all 5 new component/route files); ZERO z.enum schema redefinition (`grep -nE "z\.enum\(\["` returns 0 — types imported from @/schemas). **Deviations:** 2 auto-fixed (both Rule 3 blocking). (a) Plan step 2.6 "typecheck MUST pass before continuing" was impossible as written: App.tsx import of DecisionRoute couldn't typecheck before DecisionRoute.tsx existed. Reordered work within Task 3 to defer App.tsx + TickerRoute.tsx + Root.tsx code edits into the GREEN commit alongside DecisionRoute implementation. RED step wrote DecisionRoute.test.tsx only (vi.mock boundary on @/lib/loadTickerData; import of DecisionRoute fails — expected RED behavior). GREEN step bundled DecisionRoute.tsx + extensions in a single commit. (b) Playwright served stale build on first decision E2E run — recurring foot-gun documented in Phase 6 Wave 3 + Wave 4 SUMMARYs (`pnpm preview`-backed Playwright config requires explicit `pnpm build` after source edits). Resolved with pre-build; spec passed in 540ms post-rebuild. Worth surfacing as gap candidate for v1.x to switch Playwright webServer to `pnpm dev` OR auto-build in playwright.config.ts. **Phase 7 status: COMPLETE (1/1 plans).** VIEW-10 flipped Pending → Complete in REQUIREMENTS.md. ROADMAP.md Phase 7 row → Complete (1/1 plans, 2026-05-04). All 15 frontend VIEW requirements (VIEW-01..15) now closed across Phases 6 + 7. **Phase 8 (Mid-Day Refresh + Resilience — REFRESH-01..04 + INFRA-06..07) UNBLOCKED.** Phase 8's plan-phase will grep DecisionRoute.tsx for PHASE-8-HOOK + current-price-placeholder to find the integration point, then wire `<CurrentPriceDelta />` (fed by useRefreshData(symbol) TanStack Query hook + api/refresh.py Vercel Python serverless function returning current price + post-snapshot headlines within 10s timeout). Verifier (`/gmd:verify-phase 7`) and Nyquist coverage (`/gmd:validate-phase 7`) auto-fire from orchestrator if quota allows. Manual-only verifications (visual taste-check at /decision/AAPL/today; cross-link UX flow taste judgment) deferred to user per VALIDATION.md — those are user-facing concerns the assistant cannot regress and they do NOT block Phase 8. Plan-level commits: `be734bd` (Task 1 RED — RecommendationBanner + ConvictionDots tests), `9c972a0` (Task 1 GREEN — components), `6460ad6` (Task 2 RED — DriversList + DissentPanel tests), `d65f424` (Task 2 GREEN — components), `913eddd` (Task 3 RED — DecisionRoute test), `c1a1c8e` (Task 3 GREEN — DecisionRoute + App.tsx + TickerRoute.tsx + Root.tsx + fixtures-server.ts + decision.spec.ts + MSFT-no-dissent.json fixture). Next: `/gmd:plan-phase 8` for Phase 8 (Mid-Day Refresh).

- **2026-05-04 (Phase 6 / Plan 05 — polish + responsive + Playwright — Wave 4 / VIEW-12 + VIEW-14 + VIEW-15 — PHASE 6 CLOSEOUT)**: Wave 4 polish + Phase 6 closeout shipped. (a) **ErrorBoundary.tsx (VIEW-15)** — React 19 class-component error boundary as the SAFETY NET for errors thrown DURING RENDER (TanStack Query useQuery returns errors via query.error NOT thrown into render; ScanRoute + TickerRoute already handle inline schema-mismatch + not-found banners with the same UNIFORM RULE wording from Wave 2-3; the ErrorBoundary catches OTHER errors — lifecycle errors / non-query throws / unexpected exceptions). 3 render branches: SchemaMismatchView ('Schema version mismatch — Frontend expects schema v{frontendVersion}, snapshot is v{snapshotVersion}' or 'is older' if extraction fails; URL surfaced for debugging; Reload link), NotFoundView (URL-pattern detection: per-ticker /\\/([A-Z0-9][A-Z0-9.-]*)\\.json$/ → '{ticker} not in run for {date}' + Back-to-scan; snapshot-level _index/_status/_dates → 'No snapshot for {date}' + Today's-scan), GenericErrorView ('Something went wrong: {error.message}' + Reload). All 3 branches expose data-testid='error-boundary-reset' link with onClick={reset} calling setState({error: null}). (b) **DateSelector.tsx (VIEW-14)** — native <select> reading data/_dates.json via useDates() TanStack hook; **Notion-Clean restraint over shadcn Select**: native controls give iOS the picker UI for free, are keyboard-accessible by default, never trigger layout shift, don't require vendoring another Radix primitive; v1.x can swap in shadcn Select if richer styling needed. Newest-first ordering (storage writes ascending; selector reverses); route-shape preservation (location.pathname.startsWith('/ticker/') && params.symbol → /ticker/:symbol/:newDate else /scan/:newDate); location.search preserved on navigation (switching dates while ?lens=short keeps the lens active); disabled while loading; today-only fallback on 404 graceful first-deploy degradation. Adjacent to StalenessBadge in Root header (per plan); responsive flex-col sm:flex-row stacks vertically on mobile. (c) **loadDates.ts useDates() hook + __loadDatesForTest test export** — TanStack Query wrapper around fetchAndParse(datesUrl(), DatesIndexSchema); staleTime 60s (1 min — dates list updates exactly once per routine run); retry: false (404 is graceful — UI degrades to today-only without retry-loop noise). (d) **VIEW-12 mobile-responsive verified end-to-end**: Chart.tsx touch options handleScroll {horzTouchDrag: true, vertTouchDrag: false} + handleScale {pinch: true} — vertical page-scroll passes through (otherwise chart "captures" the swipe and users can't scroll past it on mobile); pickHeight bracket 280/360/480 at 640/1024 viewport breakpoints; ResizeObserver recomputes on rotate/resize. LensTabs.tsx -mx-4 overflow-x-auto px-4 sm:mx-0 sm:px-0 narrow-viewport containment (negative-margin extends scroll-overflow region to page edge while keeping inner content padded); min-h-11 (44px Apple HIG tap-target floor) on every TabsTrigger; min-w-max on TabsList prevents label compression. Root.tsx header flex-col sm:flex-row sm:items-center sm:justify-between → stacks (logo on top, controls below) on mobile; controls flex-wrap items-center gap-2 sm:gap-3; ticker search w-32 sm:w-40. (e) **AAPL-bad-version.json fixture** — minimal schema_version=1 shape (empty arrays / nulls) triggering SnapshotSchema.schema_version: z.literal(2) violation without needing valid v2 data underneath; v1 is REJECTED at the schema layer, period. (f) **Playwright responsive.spec.ts** — 6 mobile-only specs gated by test.skip(!isMobile, 'mobile-only'): tap-friendly tabs (>=40px boundingBox.height) / timeframe-cards-stack-vertically (second.y > first.y + 30) / no-horizontal-scroll on /scan and /ticker (document.documentElement.scrollWidth <= clientWidth + 2 sub-pixel tolerance) / persona-cards-single-column / chart-fits-viewport-width. (g) **Playwright error-boundary.spec.ts** — 3 specs: schema_version=1 mismatch via AAPL-bad-version fixture renders /(Schema version mismatch|Schema upgrade required)/i (accepts both ErrorBoundary verbose wording AND inline-banner concise rephrasing — same UNIFORM RULE concept); per-ticker 404 (ZZZZ.json) renders 'ZZZZ not in snapshot' + Back-to-scan link; snapshot 404 (date 2099-12-31, _index.json 404) renders 'No snapshot for 2099-12-31'. (h) **Playwright full-flow.spec.ts** — single comprehensive happy-path E2E: scan → switch lens (Position → Short → Long via tab clicks; asserts ?lens=short and ?lens=long URL updates) → click first ticker row in Position lens → deep-dive (4 sections: OpenClaudePin + Chart + Persona Signals heading + News Feed heading) → DateSelector navigates to 2026-05-03 with active :symbol preserved; mounts 2026-05-03 fixtures (cloned from 2026-05-04 with date-patched _status + _index) so the date-switch leg has a real target. (i) **Visual taste-check** — Notion-Clean spec adherence verified via systematic code audit (NOT screenshot diffs — Notion-Clean adherence is structurally verifiable; real-device taste judgment is deferred to user per VALIDATION.md): grep finds zero ad-hoc hex codes outside Chart.tsx PALETTE (Wave 3 decision #9 documented DRY trade-off — lightweight-charts options take raw hex not CSS-vars at chart-init time); zero gradients/backdrop-blur/animate-/glassmorphism; all shadcn shadow-sm overridden with shadow-none (Notion-Clean prefers hairline borders); Inter UI + JetBrains Mono numerics loaded via Google Fonts CDN with preconnect + display=swap; prefers-reduced-motion respected globally; color-scheme: dark + viewport-fit=cover for iPhone notch. **Decisions:** (1) ErrorBoundary as SAFETY NET, not primary error surface — TanStack Query errors flow through query.error not thrown; routes handle inline; boundary is last-resort catch. Plan's `useQuery({throwOnError})` pseudocode would have made inline error sections in routes dead code. (2) DateSelector native <select> over shadcn Select — restraint-first; mobile gets iOS picker for free; zero layout shift; v1.x upgrade path open. (3) DateSelector preserves location.search (?lens=) on navigation — without this, switching dates while on /scan/:date?lens=short would silently drop back to default Position lens. (4) ZodError version extraction walks issues looking for path ending in 'schema_version', reads issue.received as the bad value. Defensive fallback to 'snapshot is older' if extraction fails. (5) AAPL-bad-version.json minimal — empty arrays + nulls under schema_version=1 wrapper; we just need v2-literal violation; empty payload prevents downstream consumers from reaching into "v1 data" with no Wave 0 contract. (6) Chart vertTouchDrag: false deliberately — vertical page-scroll must pass through; horizontal drag captures pan; pinch captures zoom; users on mobile would otherwise be trapped by the chart. (7) Chart pickHeight reads window.innerWidth (NOT containerRef.current.clientWidth) because the breakpoint policy is VIEWPORT-driven (sm/md/lg in Tailwind), not container-driven. Container width still drives chart.applyOptions({width}) via ResizeObserver — different concern. (8) LensTabs overflow container -mx-4 + px-4 + sm:mx-0 sm:px-0 extends scroll-overflow region to page edge on mobile while keeping inner content padded; without -mx-4 tabs scroll within padded box and overflow indicator hides under page padding. (9) Playwright responsive specs use page.evaluate(() => document.documentElement.scrollWidth) instead of bounding-box because scrollWidth captures TRUE horizontal overflow including any element breaking out of viewport; +2px tolerance handles sub-pixel rounding (mobile-safari sometimes reports 375.5 on 375-wide). (10) Visual taste-check ran as CODE AUDIT (not screenshot diff) — Notion-Clean adherence is structurally verifiable by grepping for forbidden patterns + verifying mandatory tokens; zero drift detected; real-device taste verification deferred to user per VALIDATION.md (where screenshot diffs would matter). (11) Playwright reuseExistingServer=true (locally) is still a known foot-gun: pnpm build before pnpm test:e2e is the locked discipline (Wave 3 decision #10); ran pnpm build between Task 2 and Task 3 to refresh dist/ before new full-flow.spec.ts. **Tests:** 18 new vitest unit tests (8 ErrorBoundary + 6 DateSelector + 4 loadDates) + 10 new Playwright E2E (6 responsive + 3 error-boundary + 1 full-flow). Final: **197 vitest tests across 27 test files** (179 Wave 3 baseline + 18 Wave 4); **60 Playwright passed + 6 mobile-only-skipped-on-desktop = 66 invocations × 0 failed** across all 3 projects (chromium-desktop + mobile-safari iPhone 14 + mobile-chrome Pixel 7) in 13.2s. **Python repo regression: 659 tests still green** (frontend tests run separately). typecheck clean. build clean (644.41 kB index.js, 200.80 kB gzipped). **Phase 6 status: COMPLETE (5/5 plans).** All 14 frontend VIEW requirements (VIEW-01..09 + VIEW-11..15) + INFRA-05 closed. REQUIREMENTS.md VIEW-14 + VIEW-15 + INFRA-05 flipped Pending → Complete (VIEW-12 already showed Complete from Wave 3 groundwork). ROADMAP.md Phase 6 row → Complete (5/5 plans, 2026-05-04). STATE.md frontmatter: current_plan: null, status: phase_complete, completed_plans: 31, completed_phases: 6. Plan-level commits: `4b713fb` (Task 1 — ErrorBoundary + DateSelector + loadDates), `bf71ed1` (Task 2 — mobile-responsive + chart touch + Playwright responsive specs), `64544c3` (Task 3 — error-boundary E2E + full-flow E2E + visual taste-check). **Deviations:** 1 auto-fixed (Rule 3 blocking — pnpm exec playwright install webkit; Wave 1 SUMMARY explicitly predicted webkit binary install on first Wave 4 run; ~57.6MB download) + 4 plan-wording refinements (ErrorBoundary scoped as safety-net not primary; DateSelector tests use vi.mock vs QueryClient + MSW for focus on URL-routing logic; E2E error-wording regex accepts both ErrorBoundary + inline-banner phrasings; visual taste-check captured as code audit not screenshot diffs). Zero scope creep — all refinements preserve VIEW-12 / VIEW-14 / VIEW-15 contracts. **Real-device manual verifications deferred to user per VALIDATION.md Manual-Only Verifications table** (iOS Safari real-device render; Android Chrome real-device render; Vercel deploy + real GitHub raw URL; final visual taste judgment) — those are user-facing concerns the assistant cannot regress and they do NOT block Phase 7. **Phase 7 (Decision-Support View + Dissent Surface — VIEW-10) UNBLOCKED.** Frontend infrastructure stable: schemas + fetchAndParse + ErrorBoundary + DateSelector + responsive scaffolding. Phase 7 adds /decision/:symbol route rendering recommendation banner + dissent surface using existing schemas (TickerDecision + DissentSection); deep-dive's dec.recommendation + dec.dissent fields already populated by Wave 3. **Phase 8 (Mid-Day Refresh + Resilience — REFRESH-01..04) UNBLOCKED.** TanStack Query staleTime (5min) + refetchOnWindowFocus (false) is the right baseline; Phase 8 adds api/refresh.py Vercel serverless the frontend triggers on deep-dive open via queryClient.invalidateQueries(['ticker', date, symbol]). Next: `/gmd:plan-phase 7` for Phase 7 (Decision-Support View — VIEW-10).

- **2026-05-04 (Phase 6 / Plan 04 — per-ticker deep-dive — Wave 3 / VIEW-04..09 + VIEW-12 groundwork + VIEW-13)**: `/ticker/:symbol/:date?` Per-Ticker Deep-Dive view shipped — 7 sections stacked top-down per user MEMORY.md "Claude reasoning surfaced alongside personas, never replaced". Wave 1 stub REPLACED with full Wave 3 composition. (a) **Chart.tsx (VIEW-06)** — lightweight-charts 5.2 v5-API React wrapper rendering OHLC candlesticks + MA20 (accent solid) + MA50 (muted solid) + BB upper/lower (accent dotted) + RSI(14) sub-pane (amber, priceScaleId='rsi', scaleMargins.top=0.78). pairLines() helper drops null entries from indicator series before passing to setData (warmup periods produce leading nulls — MA50 has 50, RSI(14) has 14, etc.; aligned 1:1 to ohlc_history dates by index). PALETTE const mirrors src/index.css @theme tokens (lightweight-charts options take raw hex, not CSS-variable interpolation at chart-init time). ResizeObserver keeps chart width matched to container; chart.remove() in cleanup. 6 mock-based unit tests via vi.mock('lightweight-charts') with manual factory recording setData/addSeries/priceScale.applyOptions calls. (b) **OpenClaudePin.tsx (VIEW-09 + user MEMORY.md lock)** — pinned at TOP of deep-dive, accent border + accent-tinted bg (border-accent/40 bg-accent/5 per CONTEXT.md). signal prop typed `AgentSignal | undefined` so the slot ALWAYS renders, including when claude_analyst is missing entirely from persona_signals. data-muted attribute exposed for E2E. Up to 4 evidence bullets shown. (c) **PersonaCard.tsx (VIEW-07)** — 1 persona AgentSignal as a card; PERSONA_LABEL maps the 5 canonical investor analyst_ids (buffett→'Warren Buffett', munger→'Charlie Munger', wood→'Cathie Wood', burry→'Michael Burry', lynch→'Peter Lynch'); evidence[0] doubles as reasoning summary, evidence[1..] render via EvidenceList (max 3); muted body when data_unavailable=true. (d) **TimeframeCard.tsx (VIEW-05)** — short_term/long_term TimeframeBand as a card with thesis_status pill (intact/improving=bullish, weakening=amber, broken=bearish, n/a=fg-muted), confidence, summary paragraph, top-5 drivers. Two cards side-by-side on lg:flex-row, stacked on mobile. (e) **AnalyticalSignalCard.tsx** — 1 of 4 analytical signals (fundamentals/technicals/news_sentiment/valuation) with friendly captions; more compact than PersonaCard (no "voice" framing — these are deterministic Python). (f) **NewsList.tsx (VIEW-08)** — headlines grouped by source via Map<source, Headline[]>; within-group sort by published_at DESC via String#localeCompare; target='_blank' rel='noopener noreferrer' on every link; NEW tag rendered when headlinePublishedMs > snapshotComputedAtMs. Empty state when headlines=[]. (g) **ui/separator.tsx** — shadcn Separator primitive vendored (@radix-ui/react-separator dep). (h) **TickerRoute.tsx** — composes 7 sections top-down: Heading + back-to-scan link → OpenClaudePin → 2 TimeframeCards (lg:flex-row) → Chart → 5 PersonaCards in grid (md:grid-cols-2 xl:grid-cols-3, claude_analyst FILTERED OUT) → 4 AnalyticalSignalCards (lg:grid-cols-4) → NewsList grouped by source. Loading + error states with typed branches (FetchNotFoundError → '{ticker} not in snapshot' / SchemaMismatchError → 'Schema upgrade required' explicit banner per CONTEXT.md UNIFORM RULE / generic Error → message). (i) **loadTickerData.useTickerData hook** — TanStack Query with queryKey ['ticker', date, symbol]; fetchAndParse(snapshotUrl, SnapshotSchema); inherits 5min staleTime from QueryClient defaults; __loadTickerForTest test-only export mirrors loadScanData pattern. (j) **VIEW-13 ticker search typeahead** — TickerSearch input added to Root header; Enter → navigate(/ticker/${value.toUpperCase()}/${activeDate}). Active date detected via useMatches (handles both /scan/:date and /ticker/:symbol/:date? routes). Lowercase input ("aapl") uppercased at submit time. Company-name fuzzy match deferred to v1.x. (k) **AAPL fixture extended** — headlines from 3 → 9 (Yahoo Finance × 3 + Reuters × 3 + Bloomberg RSS × 3, varying published_at) so the deep-dive E2E asserts NewsList groups by 3 distinct sources. **Decisions:** (1) lightweight-charts v5 API used (chart.addSeries(CandlestickSeries, options) + chart.addSeries(LineSeries, options)) — NOT v4 addCandlestickSeries/addLineSeries which were replaced in 5.0; plan's pseudocode was outdated, this is Rule 1 (bug) auto-fix at scaffold-time. (2) OpenClaudePin signal prop = AgentSignal | undefined (NOT just AgentSignal) — VIEW-09 lock requires the slot ALWAYS render including when claude_analyst is missing entirely; single muted={!signal || signal.data_unavailable} branch handles both cases. (3) PersonaCard reasoning sourcing from evidence[0] — Phase 5 AgentSignal doesn't have a separate reasoning field; persona prompts pack reasoning + drivers into evidence[]; evidence[0] = reasoning summary, evidence[1..] = drivers via EvidenceList. v1.x can split into AgentSignal.reasoning. (4) RSI sub-pane via priceScaleId='rsi' + scaleMargins {top: 0.78, bottom: 0} — simpler than formal lightweight-charts panes API (paneIndex); gives RSI bottom ~22% of chart in single chart instance. (5) Chart unit tests via vi.mock('lightweight-charts') with manual factory — jsdom lacks WebGL/canvas; mock records setData/addSeries calls; tests assert call shape (e.g. 11 non-null pairs after dropping 19 nulls in warmup-30 fixture). (6) VIEW-13 v1 surface = uppercased text → /ticker/:typed/:date — no fuzzy match; yfinance ticker symbols ARE the v1 search surface. (7) Active date detection via useMatches (not useParams) — Root is the parent layout, not a leaf route; useMatches scans full hierarchy and finds :date from active leaf. Pattern from Wave 2 HeaderStaleness. (8) AAPL fixture extended in-place (not via _generate.mjs) for news-grouping E2E — _generate.mjs is source of truth for SnapshotSchema regeneration but headlines are a hand-curated v1.x candidate (real news fetcher generates this in prod). (9) PALETTE const inside Chart.tsx duplicates @theme tokens — minor DRY violation accepted because lightweight-charts options take raw hex, not CSS-variable interpolation at chart-init time. (10) Playwright reuseExistingServer=true (locally) means stale dist/ can mask new component changes; Rule 3 auto-fix is `pnpm build` before E2E re-run. v1.x candidate: test:e2e:fresh script. **Tests:** 12 new vitest unit tests (Chart 6 + PersonaCard 4 + OpenClaudePin 5 + TimeframeCard 8 + NewsList 6) + 4 lib (loadTickerData) + 6 Playwright E2E specs (deep dive on chromium-desktop). 179 total vitest tests across 24 test files (146 baseline → 179); 12 Playwright specs on chromium-desktop (1 smoke + 5 morning-scan + 6 deep-dive). All green. typecheck + build clean. **Phase 6 status: In Progress (4/5 plans complete — Wave 0 storage amendment + Wave 1 frontend scaffold + Wave 2 morning-scan + Wave 3 deep-dive shipped; Wave 4 06-05 polish + responsive pending).** REQUIREMENTS.md flips VIEW-04..09 + VIEW-12 + VIEW-13 from Pending → Complete (VIEW-12 partial — full mobile-safari verification in Wave 4); ROADMAP.md Phase 6 row → 4/5 plans, status "In Progress"; Phase 6 plan list 06-04 entry [ ] → [x]. Plan-level commits: `3cd2ea7` (Task 1 — Chart wrapper for lightweight-charts 5.2 with MA/BB/RSI overlays), `8857fe1` (Task 2 — persona + open-claude + timeframe + analytical + news components), `fa64df9` (Task 3 — TickerRoute deep-dive + ticker search typeahead + Playwright E2E). **Deviations:** 2 auto-fixed (1 Rule 1 bug — lightweight-charts v4 API in plan pseudocode replaced with v5 API; 1 Rule 3 blocking — Playwright stale-build on first run, fixed via pnpm build) + 3 plan-wording refinements (signal nullability tightened, reasoning sourcing from evidence[0], palette extracted to const). Zero scope creep — all auto-fixes preserve the locked plan scope. **Wave 4 (Plan 06-05 polish + responsive + Playwright + Vercel deploy) UNBLOCKED** — all 7 deep-dive sections rendering on chromium-desktop; lg:flex-row breakpoints in place for responsive verification; AAPL fixture extension gives Wave 4 mobile-safari Playwright something visual to assert on for NewsList. Next: `/gmd:execute-plan 06-05`.

- **2026-05-04 (Phase 6 / Plan 02 — frontend scaffold — Wave 1 / INFRA-05 scaffolding portion)**: Wave 1 frontend scaffold shipped — `frontend/` directory at repo root with the full locked stack from CONTEXT.md. (a) **Tooling**: pnpm 10.33.2 + Node 24 + Vite 6.4.2 + React 19.2.5 + TypeScript 5.9.3 + Tailwind v4.2.4 (CSS-first via @tailwindcss/vite plugin + `@theme` directive) + react-router 7.14.2 (data-router mode) + @tanstack/react-query 5.100.9 (staleTime 5min, refetchOnWindowFocus false) + zod 4.4.3 + lightweight-charts 5.2.0 + clsx 2.1.1 + tailwind-merge 2.6.1 in deps; vitest 2.1.9 + jsdom 25.0.1 + @testing-library/react 16.3 + @testing-library/jest-dom 6.9 + @playwright/test 1.59.1 + @types/* in dev deps; pnpm-lock.yaml committed. 7 npm scripts (dev/build/preview/typecheck/test:unit/test:e2e/lint). (b) **Notion-Clean palette tokens** in src/index.css `@theme` directive verbatim from CONTEXT.md: `--color-bg #0E0F11` (deep neutral, not pure black) + `--color-surface #1F2024` (one-step-lighter cards) + `--color-border #2A2C30` (hairline) + `--color-fg #E8E9EB` (off-white primary) + `--color-fg-muted #8B8E94` (secondary) + `--color-accent #5B9DFF` (single-restraint blue) + `--color-bullish #4ADE80` / `--color-bearish #F87171` / `--color-amber #FBBF24` (state colors) + `--color-grid #252628` (chart gridlines barely-visible). Inter UI + JetBrains Mono numerics loaded via Google Fonts `<link>` in index.html (preconnect + display=swap). prefers-reduced-motion respect baked in. (c) **6 zod schemas** (one per Pydantic file): `schemas/agent_signal.ts` (10-id `AnalystId` Literal matching analysts/signals.py + 5-state `Verdict` ladder + AgentSignalSchema with data_unavailable invariant via refine()); `schemas/position_signal.ts` (5-state `PositionState` mean-reversion ladder + 4-state `ActionHint` + consensus_score [-1,1] range + canonical no-opinion invariant); `schemas/ticker_decision.ts` (DecisionRecommendation 6-enum + ConvictionBand 3-enum + ThesisStatus 5-enum + TimeframeBand with thesis_status field + DissentSection + `schema_version: z.literal(2)` STRICTLY rejecting v1 + data_unavailable invariant); `schemas/snapshot.ts` (per-ticker JSON v2 envelope + OHLCBar + Indicators 5-series + Headline with z.string().url() validation); `schemas/status.ts` (_status.json shape); `schemas/dates_index.ts` (data/_dates.json with `schema_version: z.literal(1)`); barrel `schemas/index.ts` re-exporting all parsed types. (d) **fetch + staleness libs**: `lib/fetchSnapshot.ts` exports fetchAndParse<T>(url, schema) — single GitHub-raw read boundary; throws SchemaMismatchError class (carries url + zodError, name='SchemaMismatchError') on zod parse failure → Wave 4 ErrorBoundary renders 'schema upgrade required — re-run today's routine' banner per CONTEXT.md UNIFORM RULE; throws FetchNotFoundError class on 404 → 'No snapshot for {date}'; throws generic Error on other non-2xx. RAW_BASE built from `import.meta.env.VITE_GH_USER` + `VITE_GH_REPO` at build time. URL builders snapshotUrl/indexUrl/statusUrl/datesUrl co-located. `lib/staleness.ts` exports computeStaleness(snapshotIso, partial, now?) → 'GREEN'|'AMBER'|'RED' implementing VIEW-11 thresholds locked to REQUIREMENTS.md: <6h && !partial → GREEN; <6h && partial → AMBER; 6-24h → AMBER (both partial states); >24h → RED. SIX_HOURS_MS + TWENTY_FOUR_HOURS_MS exported as module constants. Boundary semantics locked: exactly 6h → AMBER (>= comparison); exactly 24h → AMBER (> strict on RED); 24h+1ms → RED. Defensive RED on unparseable ISO. `lib/format.ts` ships formatNumber/formatPercent/formatCompact/formatDate (UTC YYYY-MM-DD)/formatTicker (uppercase + dot→hyphen, mirrors analysts.schemas.normalize_ticker conceptually). (e) **Routes + scaffold**: react-router v7 createBrowserRouter with /→/scan/today loader redirect (no flash of placeholder) + Root layout (header brand + staleness slot placeholder + Outlet) + ScanRoute stub + TickerRoute stub. main.tsx wires React 19 createRoot + StrictMode + QueryClientProvider + RouterProvider. components.json shadcn/ui CLI config with neutral baseColor + @ aliases + tailwind.cssVariables: true; src/components/ui/.gitkeep reserves the dir (Wave 2-3 runs `pnpm dlx shadcn@latest add button card tabs badge` on demand). (f) **Deploy + envvar plumbing**: vercel.json declares framework: vite + pnpm install/build + dist outputDirectory + load-bearing rewrites `(.*) → /index.html` (without it react-router v7 client routing returns 404 on Vercel direct deep-link hits). .env.example documents VITE_GH_USER + VITE_GH_REPO; .env.local committed with placeholder example-user/example-repo so dev mode boots (real values set in Vercel dashboard, not committed). frontend/.gitignore + repo-root .gitignore extended with frontend/* exclusions including *.tsbuildinfo. (g) **Test infrastructure**: vitest config uses mergeConfig(viteConfig, ...) workaround for vitest 2.x + vite 6 peer-dep clash (vitest 2.1 ships its own Vite 5 peer; importing defineConfig from 'vite' directly causes a Plugin<any> overload mismatch on react()). Playwright config has 3 projects (chromium-desktop + mobile-safari iPhone 14 + mobile-chrome Pixel 7) + webServer pnpm preview at port 4173 (production-build E2E). chromium binary downloaded (~111MB); webkit + mobile-chrome binaries install on first Wave 4 run. **Decisions:** (1) `schema_version: z.literal(2)` chosen over z.number() so v1 snapshots are REJECTED at parse time — explicit Wave 4 banner rather than silent coercion. (2) Pydantic invariants → zod refine() one-to-one — same data_unavailable contracts hold server-side AND client-side. (3) Tailwind v4 CSS-first via @theme directive — palette tokens become first-class utilities (bg-bg, text-fg, border-border, font-mono) without a tailwind.config.ts plugin chain; .ts file exists for IDE Tailwind LSP integration only. (4) vitest-vite peer-dep clash resolved via mergeConfig(viteConfig, ...) instead of dual defineConfig imports — the documented vitest pattern; collapses back to single defineConfig at vitest 3.x. (5) tsconfig.node.json composite project uses outDir → ./node_modules/.tmp + emitDeclarationOnly:true (composite projects can't have noEmit:true per TS6310). (6) pnpm 10 onlyBuiltDependencies: ['esbuild'] required to allow esbuild's postinstall (transitive dep of vite + vitest); pnpm 10's strict default ignores postinstalls. (7) Stub route components in Wave 1 — Wave 2 populates ScanRoute, Wave 3 populates TickerRoute. (8) fetchAndParse<T> generic + ZodType<T> bound services every JSON shape via single read helper. (9) shadcn/ui base primitives NOT vendored in Wave 1 — components.json + ui/.gitkeep reserve the slot; Wave 2-3 vendor only what they actually consume per the plan's must_haves.truths language. (10) frontend/.env.local committed with placeholder values (NOT real credentials) so dev mode boots without manual setup; .env.local IS gitignored to prevent accidental real-credential commits in the future. **Tests**: 94 vitest unit tests across 8 files (12 + 12 + 14 + 12 + 7 + 7 = 64 schema + 16 staleness + 14 fetchSnapshot) + 1 Playwright smoke E2E on chromium-desktop (proves / → /scan/today redirect + 'Morning Scan — today' heading visible). Plan-level verify command (`pnpm typecheck && pnpm test:unit --run && pnpm build && pnpm test:e2e --project=chromium-desktop -g smoke`) all green. **Phase 6 status: In Progress (2/5 plans complete — Wave 0 storage amendment + Wave 1 frontend scaffold shipped; Waves 2-4 morning-scan + deep-dive + polish pending).** REQUIREMENTS.md INFRA-05 stays Pending (full requirement requires Vercel deploy + read-from-real-GitHub-raw which is Wave 4 closeout territory; Wave 1 satisfies only the scaffolding portion). ROADMAP.md Phase 6 row → 2/5 plans, status "In Progress"; Phase 6 plan list 06-02 entry [ ] → [x]. Plan-level commits: `f755908` (Task 1 — Vite+React+TS+Tailwind+router+Playwright scaffold; 24 files), `1ee1959` (Task 2 — 6 zod schemas + 64 unit tests for snapshot v2 contract), `7b2e9ff` (Task 3 — fetchSnapshot + staleness + format with VIEW-11 thresholds + zod-mismatch error class; 30 lib unit tests + vite-env.d.ts). **Deviations:** 5 auto-fixed (4 Rule 1 toolchain bugs: composite-tsconfig noEmit collision via outDir+emitDeclarationOnly, vitest-vite peer-dep type clash via mergeConfig, tsbuildinfo gitignore omission, import.meta.env typed access via vite-env.d.ts; 1 Rule 3 blocking: pnpm 10 onlyBuiltDependencies allowlist for esbuild postinstall). All deviations are toolchain-config tightening within the locked plan scope — zero scope creep. Python repo regression GREEN at 659 tests (frontend tests run separately in frontend/ via vitest). **Wave 2 (Plan 06-03 morning-scan three-lens view) UNBLOCKED** — all 6 zod schemas + their parsed types available via `import { SnapshotSchema, type Snapshot } from '@/schemas'`; fetchAndParse + URL builders ready (Wave 2 components compose `useQuery({ queryKey: ['snapshot', date, ticker], queryFn: () => fetchAndParse(snapshotUrl(date, ticker), SnapshotSchema) })` without plumbing setup); computeStaleness ready (Wave 2 StalenessBadge imports it directly + binds to _status.json.partial + _index.json.run_completed_at); Notion-Clean palette tokens are first-class Tailwind utilities; components/ui/.gitkeep reserves shadcn vendoring dir. Next: `/gmd:execute-plan 06-03`.

- **2026-05-04 (Phase 6 / Plan 01 — storage amendment — Wave 0 / INFRA-05 partial — PHASE 6 OPENS)**: Wave 0 Phase 5 amendment shipped — extends per-ticker JSON shape with everything Phase 6 frontend needs and bumps schema_version 1→2. (a) `analysts/_indicator_math.py` — appended 3 series-form helpers (`_ma_series` / `_bb_series` / `_rsi_series`) producing values byte-identical at `iloc[-1]` to existing scalar math in `analysts/technicals.py` (MA20/MA50) + `analysts/position_adjustment.py` (BB rolling primitives, RSI Wilder smoothing); locked by 3 dedicated tests. Existing scalar functions preserved verbatim. Frontend chart overlays will use the SAME math the analyst verdicts use, so they never disagree numerically. (b) `synthesis/decision.py` — added `ThesisStatus = Literal["intact", "weakening", "broken", "improving", "n/a"]` at module level; `TimeframeBand` gained `thesis_status: ThesisStatus = "n/a"` field (default 'n/a' so v1 snapshots and lite-mode TimeframeBands deserialize without ValidationError); `TickerDecision.schema_version` default bumped 1→2. (c) `prompts/synthesizer.md` — `thesis_status` instruction added per timeframe (`short_term` defaults to 'n/a' since tactical moves rarely threaten multi-year theses; `long_term` REQUIRED to choose intact/weakening/broken/improving so VIEW-04 lens has decisive data); Output Schema schema_version reference 1→2. (d) `ingestion/news.py` — `fetch_news` gains optional `return_raw: bool = False` parameter; when True returns `tuple[list[Headline], list[dict]]` where the dict list carries the raw `{source, published_at, title, url}` records. Default signature preserved exactly — no breaking changes to existing call sites (`ingestion/refresh.py:98`). (e) `routine/run_for_watchlist.py` — `TickerResult` Pydantic model gains `ohlc_history: list[OHLCBar]` + `headlines: list[dict]` fields with `default_factory=list`. `_run_one_ticker` threads them from the already-loaded `Snapshot` (the same one the analytical signals consume) — NO double-fetch (keyless data plane discipline; deviates from plan's "switch fetch_news call to return_raw=True" but is the more disciplined design). (f) `routine/storage.py` — `_build_ticker_payload` bumps `schema_version` 1→2 and adds 3 new fields: `ohlc_history` (list of 6-key OHLCBar dicts), `indicators` (dict of 5 series — ma20/ma50/bb_upper/bb_lower/rsi14 — aligned 1:1 to ohlc_history dates; NaN→None coercion via new `_series_to_jsonable` helper), `headlines` (raw dicts copied through). New `_compute_indicator_series` helper drives the indicators dict via the new `_indicator_math` series helpers. New `_write_dates_index` helper enumerates `YYYY-MM-DD` subfolders containing `_status.json` sentinels and writes `data/_dates.json` at snapshots root with schema `{schema_version: 1, dates_available: [sorted YYYY-MM-DD], updated_at: ISO}`. Phase D added AFTER Phase C `_status.json` so today's date is included in the enumeration. (g) Task 3 (ROADMAP staleness threshold doc-bug fix) was a NO-OP — `grep -rnE "2h|12h" .planning/{ROADMAP,PROJECT,REQUIREMENTS}.md` returns zero matches; the doc bug described in 06-CONTEXT.md doesn't actually exist (plan-checker confirmed pre-execution). REQUIREMENTS.md VIEW-11 already carries the canonical 6h/24h thresholds; ROADMAP.md describes the badge transition without naming specific thresholds. Recorded in SUMMARY per plan instruction. **Decisions:** (1) Schema version bump 1→2 is the ONLY breaking change; all 4 new payload fields have safe defaults so the bump is non-breaking for unmodified callers. (2) ThesisStatus default 'n/a' makes the new TimeframeBand field non-breaking for every existing TickerDecision deserialization — Phase 5 v1 snapshots (no thesis_status) deserialize cleanly with the v2 schema. (3) Series helpers compute math identically to scalar siblings via shared pandas primitives (`.rolling`, `.ewm`); locked by 3 byte-identical-at-iloc[-1] tests at 1e-9 tolerance. (4) Snapshot-as-source-of-truth threading (no double-fetch of news/OHLC) — `_run_one_ticker` reads from the already-loaded Snapshot rather than calling `fetch_news(return_raw=True)` separately. Preserves keyless data plane discipline (PROJECT.md). The `fetch_news(return_raw=True)` API is still implemented as required by the plan/tests; available for any future caller (e.g. Phase 8 mid-day refresh) that doesn't have a pre-loaded Snapshot. (5) Phase D `_write_dates_index` runs AFTER Phase C `_status.json` sentinel so today's date is included. (6) `_series_to_jsonable` coerces NaN AND inf to None for JSON-safety; frontend zod schema declares `(number | null)`. (7) RSI series math is well-defined from bar 1 onward (pandas `.ewm(adjust=False)` produces values from the first delta) — only position 0 is NaN; the scalar `_rsi_14`'s `RSI_MIN_BARS=27` warmup discipline is an analyst-level concern, not a series-math concern. The frontend can choose to hide the first ~27 entries to match analyst warmup; storage emits real values throughout. **Phase 6 status: In Progress (1/5 plans complete — Wave 0 storage amendment shipped; Waves 1-4 frontend pending).** REQUIREMENTS.md INFRA-05 stays Pending (the storage-amendment portion of INFRA-05 is satisfied; full requirement requires Vercel deploy + read-from-raw.githubusercontent.com which is Wave 1-4 territory). ROADMAP.md Phase 6 row → 1/5 plans, status "In Progress"; Phase 6 plan list 06-01 entry `[ ]` → `[x]`. Full repo regression: **659 passed** (639 baseline + 20 new). Coverage: synthesis/decision.py 100% line/branch, analysts/_indicator_math.py 97% line, routine/storage.py 93% line, routine/run_for_watchlist.py 89% line, ingestion/news.py 80% line. Plan-level commits: `8ea6252` (Task 1 RED — 22 failing tests across 5 files for series helpers + ThesisStatus + storage payload + raw headlines + threading), `1646356` (Task 2 GREEN — 6 source files implementing series helpers + ThesisStatus + thesis_status field + return_raw flag + ohlc_history/headlines threading + payload extension + Phase D dates index; 2 RED-phase test-spec corrections folded in for RSI warmup count + three-phase write order). **Deviations:** 3 auto-fixed (1 Rule 1 — Task 1 RED tests mis-asserted RSI warmup count; pandas `.ewm(adjust=False)` has only 1 NaN at position 0, not 14; corrected during GREEN phase. 1 Rule 1 — `test_three_phase_write_order` didn't account for new Phase D `_dates.json` write; assertion extended to recognize `_dates.json` as the new last write while preserving Pattern #4 sentinel discipline. 1 Rule 3 toolchain — `uv` not available on Windows; substituted `.venv/Scripts/pytest.exe` for verification commands; behavior identical, no commit). 1 plan-specified no-op (Task 3 ROADMAP doc-bug fix — bug doesn't exist, recorded in SUMMARY). **Wave 1 (Plan 06-02 frontend scaffold) UNBLOCKED** — backend produces v2 snapshots with all the data the frontend needs; zod schemas can lock to `schema_version: z.literal(2)` and deserialize ohlc_history, indicators, headlines, and thesis_status without invention. Next: `/gmd:execute-plan 06-02`.

- **2026-05-04 (Phase 5 / Plan 06 — routine.{quota, storage, git_publish, run_for_watchlist, entrypoint} — Wave 5 / LLM-08 + INFRA-01..04 — PHASE 5 CLOSEOUT)**: Five routine/ modules + 5 test files (37 tests) shipped + 12-requirement closeout. Phase 5 closes COMPLETE (6/6 plans, 12/12 requirements). `routine/quota.py` (~70 LOC) — `estimate_run_cost(watchlist) -> int` + 5 per-call token constants (N_PERSONAS=6, PERSONA_INPUT=2000, PERSONA_OUTPUT=300, SYNTHESIZER_INPUT=5500, SYNTHESIZER_OUTPUT=500) + `DEFAULT_MARKETS_DAILY_QUOTA_TOKENS=600_000` (Pattern #6 / INFRA-02); locked formula n_tickers * 19_800; 30-ticker → 594_000 < quota; 31-ticker → 613_800 > quota. `routine/storage.py` (~210 LOC) — three-phase atomic write (Pattern #4): `_atomic_write_json` (tempfile + os.replace + sort_keys; mirrors watchlist/loader.save_watchlist + ingestion/refresh._write_snapshot internal-precedent) + `_build_ticker_payload` + `write_daily_snapshot` (A: per-ticker JSONs → B: _index.json → C: _status.json LAST as run-final sentinel) + `write_failure_status` (best-effort failure path) + `StorageOutcome` frozen dataclass. Per-ticker write OSError populates failed_tickers WITHOUT aborting (LLM-08 cascade-prevention); other tickers continue. `llm_failure_count` formula: sum persona AgentSignal.data_unavailable + (1 per ticker where ticker_decision is None AND lite_mode=False). `routine/git_publish.py` (~95 LOC) — `commit_and_push(date_str, repo_root)` with 5-step subprocess.run sequence per Pattern #11: fetch / pull --rebase --autostash / add data/{date}/ / commit / push origin main. Each call: check=True, capture_output=True, text=True, timeout=60, cwd=str(repo_root). CalledProcessError logged + re-raised (fail-loudly). Anti-pattern lock: source MUST NOT contain `git add -A` or `git add .` (verified by AST-grep test). `routine/run_for_watchlist.py` (~225 LOC) — `TickerResult` Pydantic v2 model (6 fields; arbitrary_types_allowed=True for nested AgentSignal/PositionSignal/TickerDecision) + `_default_snapshot_loader` v1 stub (NotImplementedError; Phase 8 mid-day refresh extends) + `_run_one_ticker` async helper (4 analyticals + PositionSignal scored sync; lite-mode skip; persona slate + synthesizer in non-lite mode) + `run_for_watchlist` async sync-across-tickers loop (Pattern #3). Per-ticker exception caught and converted to `TickerResult(errors=[repr(exc)])`; other tickers continue. Iterates `watchlist.tickers.values()` (NOT keys) — corrects the plan's implementation_sketch which would have yielded strings. `routine/entrypoint.py` (~130 LOC) — `main() -> int` 7-step orchestration (novel-to-this-project; no virattt/TauricResearch analog): logging.basicConfig → load_watchlist (abort with exit 1 if empty) → estimate_run_cost vs MARKETS_DAILY_QUOTA_TOKENS env (default 600_000) → asyncio.run(run_for_watchlist) → write_daily_snapshot (3-phase atomic) → commit_and_push (5-step git) → return 0. Top-level except: logger.exception + best-effort write_failure_status (nested try/except so failure-status itself can never crash) + return 1. Includes `if __name__ == "__main__": sys.exit(main())` script-mode guard. **Decisions:** (1) Pattern #4 three-phase write order locked: per-ticker JSONs FIRST → _index.json SECOND → _status.json LAST (the run-final sentinel; Phase 6 frontend reads it first; absence means snapshot is in-progress). (2) LLM-08 cascade-prevention: per-ticker OSError populates failed_tickers; loop continues (one bad ticker doesn't lose the other 29). (3) llm_failure_count formula collapses persona data_unavailable + synthesizer-failure-outside-lite-mode into one int; lite mode's missing decisions are by design, NOT failures. (4) DEFAULT_MARKETS_DAILY_QUOTA_TOKENS=600_000 chosen for the 30-ticker comfortable-headroom case (594_000 < 600_000); env-overridable via MARKETS_DAILY_QUOTA_TOKENS. (5) 5-step git sequence with `pull --rebase --autostash` BEFORE add/commit/push handles manual-vs-scheduled-run race (autostash handles dirty-tree edge). (6) Anti-pattern lock against `git add -A` and `git add .` (defensive against accidentally committing temp files; AST-grep test). (7) subprocess.run kwargs locked: check=True + capture_output=True + text=True + timeout=60 + cwd=str(repo_root); 60-second timeout prevents hung-routine. (8) TickerResult Pydantic v2 with arbitrary_types_allowed=True (nested AgentSignal/PositionSignal/TickerDecision are user types from sibling packages). (9) Watchlist iteration via .values() (NOT .keys()) — corrects plan implementation_sketch. (10) Snapshot loader Callable[[ticker], Snapshot] parameterized for testability; v1 stub raises NotImplementedError; tests inject closures; Phase 8 extends. (11) asyncio.run inside main() (single event loop owns all per-ticker fan-outs); sync across tickers; async within ticker (Pattern #3). (12) Top-level exception handler intentionally broad (BLE001) — main() must NEVER crash without writing a best-effort _status.json; nested try/except means write_failure_status itself can fail without crashing. (13) Provenance per INFRA-07: routine/entrypoint.py + routine/quota.py + routine/git_publish.py + routine/run_for_watchlist.py docstrings declare "novel-to-this-project"; routine/storage.py references the watchlist/loader.save_watchlist + ingestion/refresh._write_snapshot atomic-write internal-precedent lineage. (14) Coverage cleared the ≥90/85 gate on every module: 100% line/branch on quota/storage/git_publish/run_for_watchlist; 96% line on entrypoint (only `if __name__ == "__main__"` script-mode guard uncovered, untestable under pytest by design). 37 tests in 5 test files (5 quota + 12 storage + 6 git_publish + 9 run_for_watchlist + 5 entrypoint) all GREEN. Full repo regression: **635 passed** (595 baseline post-05-05 + 40 new). **Phase 5 status: COMPLETE (6/6 plans).** All 12 LLM-XX + INFRA-XX requirements `[x]`: LLM-01 (markdown personas) + LLM-02 (loaded at runtime via load_persona_prompt + lru_cache) + LLM-03 (voice signature anchor) + LLM-04 (Pydantic-validated AgentSignal) + LLM-05 (default_factory + memory/llm_failures.jsonl) + LLM-06 (synthesizer + TickerDecision) + LLM-07 (Python-computed dissent + always-rendered DissentSection) + LLM-08 (_status.json schema) + INFRA-01 (Pattern #1 routine entrypoint) + INFRA-02 (lite_mode + quota.py) + INFRA-03 (3-phase atomic write to data/YYYY-MM-DD/) + INFRA-04 (5-step git fail-loudly publish). REQUIREMENTS.md flipped LLM-08 + INFRA-01..04 from Pending → Complete in v1 list AND traceability table; ROADMAP.md Phase 5 row → Complete with date 2026-05-04 (6/6 plans); plan list 05-06 entry [ ] → [x]. **Deviations:** 4 auto-fixed (1 Rule 3 blocking — TickerResult model lands in Task 1 instead of Task 2 because storage tests need the type before the loop logic exists; pure scheduling re-shuffle, no architectural change. 1 Rule 1 bug — removed literal `git add -A` from routine/git_publish.py docstring because the AST-grep test deliberately strict-flagged the doc-comment occurrence; rephrased to "the all-files variant"; functional behavior unchanged. 2 Rule 2 missing-critical — added 3 coverage tests for routine/run_for_watchlist.py default branches (default snapshot_loader, default client, NotImplementedError catch); added 2 coverage tests for routine/entrypoint.py exception paths (empty watchlist; nested-except in failure-status path). All 4 deviations are tightening within the locked plan scope; zero scope creep). Plan-level commits: `026f09f` (Task 1 RED — 22 failing tests for routine.{quota, storage, git_publish}), `d8bbbe1` (Task 1 GREEN — quota + storage + git_publish + TickerResult scaffold; 26 tests GREEN; 100% line/branch on all 3), `1d5cff8` (Task 2 RED — 6 failing tests for routine.run_for_watchlist), `f247360` (Task 2 GREEN — _run_one_ticker + run_for_watchlist + 3 coverage tests; 9 tests GREEN; 100% line/branch), `c3e597d` (Task 3 RED — 3 failing end-to-end integration tests for routine.entrypoint), `42c82dc` (Task 3 GREEN-ENTRYPOINT — main() orchestration + 2 coverage tests; 5 tests GREEN; 96% line), `d48e408` (Task 3 CLOSEOUT-DOCS — REQUIREMENTS.md 12 entries flipped + ROADMAP.md Phase 5 row Complete + plan list [x]). **Phase 6 (Frontend MVP — VIEW-01..09 + VIEW-11..15 + INFRA-05) UNBLOCKED** — frontend can now read data/YYYY-MM-DD/{TICKER}.json (locked schema: `{ticker, schema_version=1, snapshot_summary, analytical_signals, position_signal, persona_signals, ticker_decision, errors}`) + _index.json (run metadata) + _status.json (success/partial sentinel) via raw.githubusercontent.com. Phase 7 (Decision-Support View) and Phase 8 (Mid-Day Refresh) also unblocked from the data-side. Next: Phase 6 plan-phase via `/gmd:plan-phase 6`.

- **2026-05-04 (Phase 5 / Plan 05 — synthesizer triad: prompts/synthesizer.md + synthesis/dissent.py + synthesis/synthesizer.py — Wave 4 / LLM-06 + LLM-07)**: synthesizer triad shipped with Pattern #7 dissent-in-Python lock confirmed end-to-end. `prompts/synthesizer.md` (201 lines, replaces Wave 0 stub) — 4 locked sections (Input Context / Task / Output Schema + data_unavailable handling subsection); 3-layer recommendation priority order in plain English (tactical via PositionSignal.action_hint → short-term via persona consensus → long-term via valuation analyst + thesis_price + default fallback); conviction band rule (high: ≥5 of 11 signals agree + no dissent; medium: 3-4 agree OR mild dissent; low: ≤2 agree OR dissent + split); dual-timeframe TimeframeBand instructions (short_term LEAD WITH PositionSignal state; long_term LEAD WITH valuation analyst + lens-aligned personas); pre-computed dissent rendering instruction (Pattern #7 lock — render verbatim, do NOT compute); Output Schema names all 10 TickerDecision fields + 6 DecisionRecommendation enum values + 3 ConvictionBand values; locked TauricResearch/TradingAgents phrase "Ground every conclusion in specific evidence" lifted from portfolio_manager.py per 05-RESEARCH.md provenance. `synthesis/dissent.py` (~190 LOC) — `compute_dissent(persona_signals: list[AgentSignal]) -> tuple[Optional[str], str]`; module constants `VERDICT_TO_DIRECTION` (5-key map: strong_bullish/bullish=+1, neutral=0, bearish/strong_bearish=-1) + `DISSENT_THRESHOLD = 30` (LLM-07 boundary inclusive) + `_SUMMARY_MAX_LEN = 500`; signed-weighted-vote majority direction (NOT mode-based — magnitude of conviction matters per Anti-Pattern at 05-RESEARCH.md line 1189); opposite-direction-≥30-confidence trigger; tie-break composite key `max(dissenters, key=lambda s: (s.confidence, _neg_alpha(s.analyst_id)))` — primary confidence-highest-wins; secondary alphabetical-first-wins via `_neg_alpha` helper (tuple of negated codepoints). `synthesis/synthesizer.py` (~340 LOC) — provenance docstring referencing TauricResearch/TradingAgents/tradingagents/agents/managers/{portfolio_manager.py, research_manager.py} + 4 modifications enumerated; 3 module constants `SYNTHESIZER_PROMPT_PATH=Path("prompts/synthesizer.md")` + `SYNTHESIZER_MODEL="claude-opus-4-7"` + `SYNTHESIZER_MAX_TOKENS=4000`; `load_synthesizer_prompt` with @functools.lru_cache(maxsize=2) + FileNotFoundError on missing; `build_synthesizer_user_context` deterministic with 7 sections (Ticker / Snapshot Summary / 4 Analytical Signals / PositionSignal / 6 Persona Signals / User TickerConfig / Pre-computed Dissent — block ALWAYS present, even when no dissent); 4 private formatters (`_summarize_snapshot` / `_format_signal` / `_format_position_signal` / `_format_config`) using actual FundamentalsSnapshot field names (pe / roe / debt_to_equity, NOT pe_ratio / return_on_equity); `_data_unavailable_decision` single source of truth canonical TickerDecision shape used in 3 places (Snapshot.data_unavailable skip path, lite-mode skip path, call_with_retry default_factory); `_decision_default_factory` closure-bound; `async synthesize(...)` with 2 cost-saving skip paths (Snapshot.data_unavailable + persona_signals=[] for INFRA-02 lite mode; both return canonical decision WITHOUT calling LLM) — else `compute_dissent()` runs in Python BEFORE the LLM call (Pattern #7 lock), `build_synthesizer_user_context` injects the pre-computed dissent into the user message, `call_with_retry(model='claude-opus-4-7', max_tokens=4000, output_format=TickerDecision, default_factory=_decision_default_factory)` fires the single LLM call. `tests/conftest.py` extended with re-export of `MockAnthropicClient` + `MockMessages` + `mock_anthropic_client` + `isolated_failure_log` from `tests/routine/conftest.py` so tests/synthesis/ (sibling package) inherits them; primes Wave 5's tests/routine/test_entrypoint.py to inherit uniformly. **Decisions:** (1) Pattern #7 dissent-in-Python lock — three reasons: determinism (testable, reproducible) + no hallucination risk (Pydantic constrained-decoding only validates `str | None`, not "this string is one of the 6 valid persona IDs"; a typo'd 'warrenbufett' would pass schema validation but break Phase 6 frontend rendering) + tie-break specificity (highest-confidence-then-alphabetical is precise; LLM tie-breaking is ad-hoc). The synthesizer prompt INSTRUCTS the LLM to render the pre-computed string verbatim, never to compute dissent itself. (2) Signed-weighted-vote majority direction (NOT mode-based majority verdict): `direction_score = sum(VERDICT_TO_DIRECTION[s.verdict] * s.confidence for s in valid_personas)`. Per Anti-Pattern at 05-RESEARCH.md line 1189: a 4-bullish (conf 60 each) + 2-strong_bullish (conf 90 each) slate has direction_score=+(4*60)+(2*90)=+420 — magnitude of conviction matters, not just count. (3) DISSENT_THRESHOLD=30 boundary inclusive per LLM-07: confidence==30 on opposite direction triggers; confidence==29 does not. Both edge cases tested explicitly. (4) VERDICT_TO_DIRECTION 5-key collapse: neutral has direction=0 → NEVER counts as a dissenter (test_dissent_neutral_doesnt_count locks this — neutral isn't "opposite" to either +1 or -1 majority). (5) Tie-break composite key via `max(items, key=(confidence, _neg_alpha(id)))` — primary confidence (higher wins via natural max); secondary _neg_alpha (tuple of negated codepoints; smaller string sorts LATER under max so alphabetical-first wins). Composes cleanly without separate sort+slice. Tested by burry vs munger at conf=70 → 'burry' wins. (6) `_data_unavailable_decision` single source of truth for canonical TickerDecision shape: 3 call sites funnel through one helper; the @model_validator invariant from 05-02 (data_unavailable=True ⟹ recommendation='hold' AND conviction='low') is satisfied by construction. (7) `build_synthesizer_user_context` dissent block ALWAYS present — even when no dissent computed, the block contains literal "no dissent (has_dissent: false; render dissent.has_dissent=false)". Keeps the synthesizer prompt's section-anchor parsing deterministic — the LLM always sees a clear instruction either way. (8) Cross-package fixture re-export at root tests/conftest.py: pytest doesn't auto-discover sibling-package conftests, so we LIFT mock_anthropic_client + isolated_failure_log into root level via `from tests.routine.conftest import (...)`. Single import surface; tests/synthesis/ + tests/routine/ + Wave 5's tests/routine/test_entrypoint.py all inherit the same fixture-replay implementation. (9) `synthesize` keyword-only signature (* before all params) — forces caller-side clarity at the per-ticker call site; no positional confusion across the 7-parameter inputs. Mirrors run_persona_slate's keyword-only convention from Wave 3. (10) Module constants `SYNTHESIZER_MODEL='claude-opus-4-7'` + `SYNTHESIZER_MAX_TOKENS=4000` per 05-RESEARCH.md Pattern #2 — Opus 4.7 for synthesizer (vs Sonnet 4.6 for personas); 4000 max tokens accommodates the longer dual-timeframe TimeframeBand outputs. 16 tests in tests/synthesis/test_dissent.py + 30 tests in tests/synthesis/test_synthesizer.py (24 distinct + 6 parametrized 6-recommendation-paths expansion) all GREEN; coverage on synthesis/dissent.py: **100% line / 100% branch** (gate ≥90/85); coverage on synthesis/synthesizer.py: **100% line / 100% branch** (gate ≥90/85). Full repo regression: **595 passed** (549 baseline post-05-04 + 46 new). Pure-additive plan: zero Phase 1-4 + Wave 0-3 production files modified beyond the prompts/synthesizer.md content drop and the tests/conftest.py fixture re-export. Plan-level commits: `63548ae` (Task 1 RED — 16 failing tests for synthesis.dissent), `ba648b2` (Task 1 GREEN — synthesis/dissent.py implementation), `b3da91d` (Task 2 RED — 24 failing tests for synthesis.synthesizer + tests/conftest.py fixture re-export), `39965c7` (Task 2 GREEN — prompts/synthesizer.md full content + synthesis/synthesizer.py implementation + 6 coverage tests folded in for 100% line/branch). **Phase 5 status: In Progress (5/6 plans complete — Wave 0 foundation + Wave 1 schema + Wave 2 LLM client + Wave 3 persona slate + Wave 4 synthesizer triad shipped; Wave 5 entrypoint pending).** LLM-06 + LLM-07 marked Complete in REQUIREMENTS.md (LLM-07 Pending → Complete; LLM-06 already showed Complete from 05-02 schema close); ROADMAP.md Phase 5 row → 5/6 plans, status "In Progress"; Phase 5 plan list updates 05-05 entry from `[ ]` to `[x]`. **Deviations:** 1 Rule 2 auto-fix (added 6 coverage tests in tests/synthesis/test_synthesizer.py beyond the planned ≥10 floor to clear the 90/85% coverage gate — initial coverage was 77% line; lines 164 + branches 167->169, 169->171, 176->178, 178->180, 180->182, 211->213 uncovered because the base_inputs fixture used a minimal Snapshot/PositionSignal. Final coverage: 100% line / 100% branch). 1 test-side fixup (test_synthesize_happy_path_returns_llm_decision and test_synthesize_dissent_pre_computed_in_user_context extracted user message from `calls[0]["messages"][0]["content"]` instead of the implementation_sketch's incorrect `calls[0]["user"]` — the actual `routine.llm_client.call_with_retry` constructs the SDK call as `messages=[{"role": "user", "content": user}]` per the Anthropic SDK contract, not as a top-level user= kwarg). Both deviations are tightening, no scope creep. Wave 5 (05-06 entrypoint) UNBLOCKED — `synthesize` is the per-ticker decision producer the routine's `_run_one_ticker` will call after `run_persona_slate` returns the 6 persona AgentSignals; Pattern #7 lock means the routine never has to think about dissent computation (it's deterministic Python that runs inside synthesize). Next: `/gmd:execute-plan 05-06`.

- **2026-05-04 (Phase 5 / Plan 04 — 6 persona markdown prompts + routine/persona_runner.py async fan-out — Wave 3 / LLM-01 + LLM-02 + LLM-03 + LLM-04)**: 6 persona markdown prompts shipped at prompts/personas/{buffett,munger,wood,burry,lynch,claude_analyst}.md (replacing Wave 0 stubs entirely; 102/102/102/98/100/106 lines respectively; locked 5-section structure: `# Persona: <Name>` H1 + `## Voice Signature` + `## Input Context` + `## Task` + `## Output Schema`; voice-signature anchors per LLM-03 cross-checked by parametrized keyword presence tests — Buffett: owner earnings + moat + margin of safety; Munger: mental model + invert; Wood: disruptive innovation + exponential; Burry: contrarian; Lynch: PEG + 10-bagger; Claude: NOT a + Claude + inherent). claude_analyst.md (106 lines) explicitly distinguishes itself per user MEMORY.md feedback_claude_knowledge ("NOT a persona, NOT a lens — Claude's inherent reasoning surfaced when given the per-ticker snapshot + analytical signals"); declares "novel-to-this-project" (no virattt analog). routine/persona_runner.py (289 lines) ships the async-within-ticker fan-out: PERSONA_IDS canonical tuple of 6 ids matching Wave 0 widened AnalystId Literal (subset asserted by test_persona_ids_subset_of_widened_analyst_id) + 3 module constants (PERSONA_MODEL='claude-sonnet-4-6', PERSONA_MAX_TOKENS=2000, PERSONA_PROMPT_DIR=Path('prompts/personas')) + load_persona_prompt with @functools.lru_cache(maxsize=8) (LLM-02 lock: never as Python string; ValueError on unknown id; FileNotFoundError on missing file) + build_persona_user_context (single shared user-message string per ticker, deterministic byte-identical, includes 4 analytical signals + PositionSignal + TickerConfig) + 4 small private formatters (_summarize_snapshot/_format_signal/_format_position_signal/_format_config) + closure-bound _persona_default_factory(persona_id, ticker, computed_at) (LLM-05 cascade-prevention shape) + async run_one wrapping call_with_retry with persona's loaded system prompt + async run_persona_slate via asyncio.gather(return_exceptions=True) (Pattern #3 — async-within-ticker; outer-slot collapse to default_factory beyond call_with_retry's per-LLM retry; preserves PERSONA_IDS submission order; defensive count validation `len(analytical_signals) == 4`). Provenance per INFRA-07: docstring references virattt/ai-hedge-fund/src/agents/ + names all 5 reference persona files (warren_buffett.py, charlie_munger.py, cathie_wood.py, michael_burry.py, peter_lynch.py); claude_analyst.md declares novel-to-this-project. **Decisions:** (1) Reuse AgentSignal verbatim for persona output — no separate PersonaSignal class; AnalystId Literal widening from 05-01 (Wave 0) lets persona AgentSignals share the analytical AgentSignal contract end-to-end (Pattern #9). (2) Prompts loaded at call time via Path(prompts/personas/{persona_id}.md).read_text — wrapped in @functools.lru_cache(maxsize=8) so 6×N_TICKERS fan-out is one read per persona regardless of N_TICKERS. (3) Single shared user_context per ticker — built once via build_persona_user_context, passed to all 6 persona calls. Per-persona variation lives entirely in the SYSTEM prompt (markdown). Saves 6× context-build cost; tested for byte-identical determinism. (4) Closure-bound default factory captures persona_id + ticker + computed_at at call site so each retry-exhaustion produces a contextually-correct AgentSignal(verdict='neutral', confidence=0, evidence=['schema_failure'], data_unavailable=True). (5) PERSONA_IDS as immutable tuple, not list — usable as dict key, hashable, canonical iteration order for asyncio.gather (preserves submission order in result list, NOT completion order). (6) asyncio.gather(return_exceptions=True) outer defense — beyond call_with_retry's per-LLM retry, an UNCAUGHT Python-level exception in run_one collapses to default_factory at the outer layer. The synthesizer's compute_dissent always sees 6 AgentSignals, in canonical PERSONA_IDS order. (7) claude_analyst.md framed as "NOT a persona, NOT a lens — Claude's inherent reasoning surfaced" per user MEMORY.md feedback_claude_knowledge. The 5 reference personas constrain to lenses by design — that's their value. claude_analyst's value is the COMPLEMENTARY surface (training-data context, comparable precedent, knowledge-cutoff acknowledgment, what-the-personas-don't-say). (8) Defensive validation in run_persona_slate: len(analytical_signals) == 4 raises ValueError early — defensive against synthesizer-side ordering bugs ('fund/tech/nsen/val expected'). 45 tests in tests/routine/test_persona_runner.py (well above the ≥14 floor with parametrization expansion to 39 instances + 6 coverage tests) all GREEN. Coverage on routine/persona_runner.py: **100% line / 100% branch** (gate ≥90/85). Full repo regression: **549 passed** (504 baseline post-05-03 + 45 new). Pure-additive plan: zero Phase 1-4 + Wave 0 + Wave 1 + Wave 2 files modified; persona_runner not yet imported anywhere — Wave 5 entrypoint will. **Deviations:** 2 auto-fixed (1 Rule 1 bug — _summarize_snapshot used wrong attribute names `pe_ratio` / `return_on_equity` which don't exist on FundamentalsSnapshot — actual schema uses `pe` / `roe` / `debt_to_equity`; fixed in-place to actual schema field names. This was dead code at runtime that would have raised AttributeError when Wave 5's entrypoint passes populated snapshots. 1 Rule 2 missing-critical — added 6 coverage tests beyond the planned 14-test floor to clear the 90/85% coverage gate: test_build_user_context_with_populated_snapshot / test_build_user_context_with_dark_position_signal / test_build_user_context_with_empty_snapshot_data / test_summarize_snapshot_partial_fundamentals / test_run_persona_slate_python_exception_isolated / test_load_persona_prompt_raises_file_not_found_when_file_missing). Both deviations are tightening, no scope creep. **Phase 5 status: In Progress (4/6 plans complete — Wave 0 foundation + Wave 1 schema + Wave 2 LLM client + Wave 3 persona slate shipped; Waves 4-5 pending).** LLM-01 + LLM-02 + LLM-03 + LLM-04 marked Complete in REQUIREMENTS.md (Pending → Complete in traceability table; checkboxes `[ ]` → `[x]` on all 4); ROADMAP.md Phase 5 row → 4/6 plans, status "In Progress"; Phase 5 plan list updates 05-04 entry from `[ ]` to `[x]`. Plan-level commits: `ffb5132` (Task 1 RED — 14 tests / 40+ parametrized instances; 28 failed + 11 passed at RED), `b205064` (Task 1 GREEN — 6 persona markdown content drops + routine/persona_runner.py implementation + 6 coverage tests folded in for 100% line/branch). Wave 4 (05-05 synthesizer + dissent) UNBLOCKED: run_persona_slate's 6-AgentSignal output (PERSONA_IDS canonical order, length always 6, default-factory shape on per-persona failure) is exactly what compute_dissent reads. Wave 5 (05-06 entrypoint) UNBLOCKED: run_persona_slate is the per-ticker persona-call surface the routine's _run_one_ticker will call between analytical-signal scoring and synthesizer call. Next: `/gmd:execute-plan 05-05`.
- **2026-05-04 (Phase 5 / Plan 03 — routine.llm_client + mock_anthropic_client fixture — Wave 2 / LLM-04 + LLM-05)**: `routine/llm_client.py` (193 LOC) ships the project's single LLM-I/O boundary — `call_with_retry()` async function generic over `T bound=BaseModel` wrapping `client.messages.parse(output_format=PydanticModel)` per 05-RESEARCH.md Pattern #2. Retry policy = `DEFAULT_MAX_RETRIES=2` (cost-predictable per Pattern #8); 4 exception-path branches (ValidationError → log raw via `_try_extract_raw` + retry; APIStatusError | APIError → log + retry; bare `Exception` → kind='unknown_error' defensive cascade prevention; all paths return `default_factory()` on exhaustion per LLM-05). Failure log at `memory/llm_failures.jsonl` (append-only JSONL; sort_keys=True serialization; UTC ISO-8601 timestamps; mkdir parents=True on first write; record fields = EXACTLY `{timestamp, label, kind, message ≤1000 chars, raw ≤5000 chars or null}`). NEVER passes temperature/top_p/top_k to messages.parse (Opus 4.7 400-rejection guard per Pattern #2; AST-grep test locks this). Provenance per INFRA-07: docstring references `virattt/ai-hedge-fund/src/utils/llm.py` with 4 modifications enumerated. `tests/routine/conftest.py` populated with `MockAnthropicClient` + `MockMessages` (fixture-replay pattern: `queue_response`/`queue_exception` → `.calls` list records every kwargs dict for downstream assertions); `mock_anthropic_client` per-test fixture; `isolated_failure_log` autouse fixture (monkeypatches `LLM_FAILURE_LOG` → tmp_path so tests never pollute real `memory/`). pytest-asyncio>=0.23 added to `[dependency-groups].dev` (uv resolved 1.3.0); `asyncio_mode='auto'` configured in `[tool.pytest.ini_options]` so async tests don't need explicit `@pytest.mark.asyncio` decoration. **Decisions:** (1) `DEFAULT_MAX_RETRIES=2` (NOT 3) per Pattern #8 — keeps cost predictable; v1.x may add tenacity-backed backoff. (2) `TypeVar T bound=BaseModel` — single `call_with_retry` services Wave 3 (output_format=AgentSignal) AND Wave 4 (output_format=TickerDecision) without code duplication. (3) Defensive `except Exception` branch (kind='unknown_error') — never let an unexpected exception propagate; the per-ticker pipeline must continue. (4) Failure log truncation at the helper layer (1000 chars message / 5000 chars raw) — defensive against multi-MB structured errors. (5) Sort_keys=True + UTC + LF on each JSONL record — byte-stable serialization mirrors Phase 1/2 atomic-write discipline at the per-record level. (6) Truncation tests target `_log_failure` directly (not via `call_with_retry`) — Pydantic v2 `ValidationError.__new__` requires `title`+`line_errors` and rejects bare `__init__` overrides with TypeError, AND a real ValidationError's `str()` is auto-truncated at ~50 chars by Pydantic before reaching the wrapper; direct unit tests on the helper are higher-fidelity. (7) `_try_extract_raw` is best-effort — broad Exception catch returns None on any extraction failure; two dedicated defensive-path tests lock the contract. (8) `isolated_failure_log` autouse on tests/routine/* — Wave 3-5 tests benefit transparently from per-test log isolation. (9) `MockAnthropicClient` populated here in Wave 2 (NOT duplicated across Wave 3-5 test files) — single fixture-replay implementation services persona_runner + synthesizer_runner + entrypoint integration tests. (10) `pytest-asyncio asyncio_mode='auto'` — async test functions don't need explicit `@pytest.mark.asyncio` decoration. 17 tests in `tests/routine/test_llm_client.py` (above the ≥10 floor) GREEN; coverage on `routine/llm_client.py` **100% line / 100% branch** (gate ≥90/85). Full repo regression: **504 passed** (487 baseline + 17 new). Phase 1-4 + Wave 0/1 untouched (pure-additive plan). LLM-04 + LLM-05 marked complete in REQUIREMENTS.md. **Deviations:** 2 auto-fixed (1 Rule 1 — truncation test pivoted from ValidationError-subclass to direct `_log_failure` unit tests due to Pydantic v2 `__new__` constraints + auto-truncation of `str(exc)`; 1 Rule 2 — added 2 defensive-path tests on `_try_extract_raw` to close coverage gap from 92%→100%). Both deviations are test-side hardening; production code matched the locked sketch byte-for-byte. Plan-level commits: `ded5bfc` (Task 1 RED — 14 failing tests + conftest fixture-replay + pyproject.toml dep), `fe6a696` (Task 1 GREEN — routine/llm_client.py implementation + 3 additional tests folded in for coverage). **Phase 5 status: In Progress (3/6 plans complete — Wave 0 foundation + Wave 1 schema + Wave 2 LLM client shipped; Waves 3-5 pending).** Wave 3 (05-04 personas) + Wave 4 (05-05 synthesizer) + Wave 5 (05-06 entrypoint) all unblocked: they import `call_with_retry` and reuse `mock_anthropic_client` + `isolated_failure_log` fixtures. Next: `/gmd:execute-plan 05-04`.
- **2026-04-29 / 2026-04-30**: Project initialized via `/gmd:new-project`. Architecture locked through brainstorming flow (PROJECT.md captures all locked decisions).
- **2026-04-30**: Reference projects studied — `virattt/ai-hedge-fund` (cloned to `~/projects/reference/ai-hedge-fund/`) and `TauricResearch/TradingAgents` (cloned to `~/projects/reference/TradingAgents/`). Patterns adopted: agent state TypedDict, two-tier persona/analytical split, deterministic scoring before LLM, synthesizer with deterministic pre-filter, memory log + reflection. Patterns rejected: hardcoded Python prompts (we use markdown), trade-execution focus (we surface recommendations only), API-key data plane (we use keyless), LangGraph framework (incompatible with keyless).
- **2026-04-30**: Configuration locked — YOLO mode, Fine granularity, parallel execution, Quality model profile (Opus for research/roadmap, Sonnet for synthesis), all workflow agents enabled (Research / Plan-Check / Verifier).
- **2026-04-30**: Research completed inline (parallel research-agent spawns hit Opus quota; agents resumable at 3:50pm Chicago via SendMessage to agent IDs `ad9c4b82904461fce`, `a78c5f332fe5fa097`, `a14f95a61bb93489b` if cross-check desired). Five research docs produced: STACK.md, FEATURES.md, ARCHITECTURE.md, PITFALLS.md, SUMMARY.md.
- **2026-05-01 (Phase 1 / Plan 01 — scaffold)**: uv-managed Python project bootstrapped. Pydantic 2.13.3, pytest 9.0.3, pytest-cov 7.1.0, ruff 0.15.12 installed. Three importable packages (analysts/, watchlist/, cli/) created. `markets` console script wired end-to-end (verified). `tests/conftest.py` exposes empty/seeded/large watchlist fixtures with lazy schema imports. Pytest framework wired (collect-only exits 5, no ImportError). Plan-level commits: `fb3fad7`, `d3794ce`, `758a2fb`. Five WATCH-0* requirements marked complete in REQUIREMENTS.md.
- **2026-05-01 (deviations during Plan 01)**: (1) `uv` not installed on host — installed via `python -m pip install --user uv` to `C:/Users/Mohan/AppData/Roaming/Python/Python314/Scripts/`. (2) `uv run markets` failed after Task 2 with `ModuleNotFoundError: No module named 'cli'` — initial wheel was built before package directories existed; fixed with `uv sync --reinstall-package markets`. Both Rule 3 (blocking environmental) auto-fixes — no design changes.
- **2026-05-01 (Phase 1 / Plan 02 — schemas)**: Pydantic v2 schemas locked. `analysts/schemas.py` (171 lines) ships `TechnicalLevels`, `FundamentalTargets`, `TickerConfig`, `Watchlist` + module-level `normalize_ticker(s)` helper. 8/8 schema tests green; coverage 99% line / 98.89% branch (gate ≥95%). Hyphen-form normalization verified (BRK.B/BRK_B/BRK/B/brk-b → BRK-B). All cross-field rules use `@model_validator(mode='after')`. **Decision:** `normalize_ticker` extracted to module level (single source of truth) — Plans 04/05 will `from analysts.schemas import normalize_ticker` and reuse directly, no duplication. **Decision:** Strict watchlist key mode — dict-key/value.ticker mismatch raises `ValidationError` naming the offender, never silently rewrites. WATCH-04 + WATCH-05 covered (already marked `[x]` in REQUIREMENTS.md from Plan 01 over-attribution; behavior now actually delivered). Plan-level commits: `66795a2` (RED test), `87aaa4e` (GREEN impl). Zero deviations from plan.
- **2026-05-01 (Phase 1 / Plan 03 — loader)**: Stdlib atomic-write loader landed. `watchlist/loader.py` (69 lines) ships `load_watchlist(path)` + `save_watchlist(wl, path)` + `DEFAULT_PATH`. **Decision:** stdlib `json.dumps(model_dump(mode="json"), indent=2, sort_keys=True) + "\n"` for serialization (NOT `model_dump_json`) per CONTEXT.md correction #3 / Pydantic v2 issue #7424 — guarantees byte-identical round-trip and stable git diffs. **Decision:** `tempfile.NamedTemporaryFile(delete=False, dir=parent) ... with-block-exit ... os.replace(tmp_path, path)` per Pitfall #2 (Windows file-lock release); on OSError, `tmp_path.unlink(missing_ok=True)` then re-raise. **Decision:** `load_watchlist` on missing path returns empty `Watchlist()` (not an error) — drives first-run CLI UX. **Decision:** save_watchlist re-validates the model before persistence (defense-in-depth). 7/7 loader tests green (15/15 combined with schemas); coverage 100% line+branch (gate ≥90%); 50-ticker load measured 6.18ms (<<100ms gate); round-trip byte-identical confirmed; no orphan tmp files in any test path. **Deviation (Rule 2):** Added 7th test `test_save_cleanup_on_replace_failure` (monkeypatched `os.replace` raises OSError → asserts cleanup + re-raise) to lock the failure-cleanup contract and close the coverage gap on lines 67-69 (originally-specified 6 tests came in at 88.89% coverage). No implementation deviation from `01-RESEARCH.md` "Loader / atomic save" example. Plan-level commits: `7c1bf15` (RED), `e40f0eb` (GREEN). WATCH-01, WATCH-02, WATCH-05 confirmed covered.
- **2026-05-01 (Phase 1 / Plan 04 — CLI core)**: argparse dispatcher + add/remove + format_validation_error shipped. `cli/main.py` (71 lines), `cli/_errors.py` (38), `cli/add_ticker.py` (102), `cli/remove_ticker.py` (73) — 284 production lines total. **Decision:** SUBCOMMANDS dict-of-tuples pattern in `cli/main.py` is Plan 05's documented extension surface (4-line patch: 2 imports + 2 dict entries; zero modifications to existing lines). **Decision:** `cli/remove_ticker.py` imports `normalize_ticker` from `analysts.schemas` (zero inline regex; closes Plan 02's deferred decision). **Decision:** ISO 8601 timestamps stick with `+00:00` form (NOT `Z`) per Pitfall #3 — round-trips natively through `datetime.fromisoformat`. **Decision:** Optional groups (TechnicalLevels, FundamentalTargets) constructed only when at least one of their flags is passed (no `null` clutter in watchlist.json). **Decision:** ValidationError caught in main() → format_validation_error → stderr → exit 2 (no raw tracebacks); FileNotFoundError → stderr → exit 1; other exceptions propagate (debug-friendly). 9/9 CLI tests green (probes 1-W3-01..09); combined Wave 1+2+3 24/24 green; coverage 85.62% on the four cli/* files (gate ≥85%). BRK.B → BRK-B verified end-to-end via `test_add_brk_normalizes_to_hyphen` AND smoke test. ValidationError surfaces cleanly via smoke test (`markets add AAPL --thesis -1` → `validation failed (1 error): - thesis_price: ... (got: -1.0)` exit 2, no file created). Plan-level commits: `4a3ae49` (RED), `0446715` (GREEN). WATCH-02, WATCH-03, WATCH-04, WATCH-05 confirmed covered. **Zero deviations** from plan.
- **2026-05-01 (Phase 1 / Plan 05 — CLI readonly + example)**: Read-only CLI surface shipped. `cli/list_watchlist.py` (45 lines) + `cli/show_ticker.py` (83 lines) + `watchlist.example.json` (5 tickers, 4 lenses, BRK-B hyphenated) + `README.md` (88 lines, quick-start + schema table + provenance). **Decision (locked):** `cli/_errors.py:suggest_ticker(unknown, known)` is the single source of truth for did-you-mean across show + remove — closes Plan 04's deferred decision. Plan 04's remove tests stay green with no test changes (refactor is internal). **Decision (locked):** `cli/show_ticker.py` imports `normalize_ticker` from `analysts.schemas` — same pattern as Plan 04 remove. **Decision:** `markets list` empty watchlist exits 0 (mirrors loader contract); show error codes mirror remove (0/1/2). **Decision:** Notes column truncated at 40 chars in list output. **Decision:** `watchlist.example.json` generated via dogfooded `markets add` (not hand-crafted) — exercises full add → schema → loader → save pipeline. WATCH-01 user-facing demo proven (`test_list_30_plus_tickers` over 35-ticker fixture); WATCH-03 ergonomics extended to show (`markets show AAPK` → did-you-mean AAPL). 33/33 Phase 1 tests pass; coverage 94% global, 100% list_watchlist.py, 98% show_ticker.py — well above the ≥90% gate. Plan-level commits: `6f555d3` (RED), `ba679ce` (GREEN). **Zero deviations** from plan (3 extra coverage tests added to clear the gate are documented in SUMMARY).
- **2026-05-01 (Phase 2 / Plan 01 — foundation)**: Wave 1 ingestion foundation shipped. `ingestion/http.py` (76 lines) provides `get_session()` returning a process-shared `requests.Session` with EDGAR-compliant `User-Agent` (env-overridable via `MARKETS_USER_AGENT`), `HTTPAdapter` mounted with `Retry(total=3, backoff_factor=0.3, status_forcelist=[429,500,502,503,504], allowed_methods={GET,HEAD}, raise_on_status=False)`, `DEFAULT_TIMEOUT=10.0` constant, and `polite_sleep(source, last_call, min_interval)` helper. `ingestion/errors.py` ships the 3-class hierarchy (`IngestionError` → `NetworkError`, `SchemaDriftError`). `analysts/data/` sub-package ships eight Pydantic schemas (`PriceSnapshot`, `OHLCBar`, `FundamentalsSnapshot`, `FilingMetadata`, `Headline`, `RedditPost`, `StockTwitsPost`, `SocialSignal`) — every one delegates ticker normalization to `analysts.schemas.normalize_ticker` (no regex duplication) and uses `ConfigDict(extra="forbid")`. `pyproject.toml` extended with 5 runtime deps (yfinance/yahooquery/requests/feedparser/beautifulsoup4) + 1 dev dep (responses); `ingestion` registered in hatch packages and coverage source. **Decision:** `raise_on_status=False` on Retry — callers see the final response (status + body) when retries exhaust on 5xx so the Plan 02-06 orchestrator can branch on response type rather than catching `MaxRetryError`. **Decision:** `polite_sleep` is caller-owned-state (no module singletons that would bleed between tests). **Decision:** Fundamentals carry NO positivity constraint at the schema layer — FCF/ROE can be negative legitimately; downstream sanity checks enforce ranges. **Decision:** `Headline.url` is plain `str` (not HttpUrl) — Yahoo redirect URLs sometimes fail strict checks. **Decision:** `FilingMetadata.form_type` Literal includes `"OTHER"` escape-hatch for unknown EDGAR forms. 34/34 Phase 2 W1 tests green (9 http + 2 errors + 23 schemas; full suite 69/69). Coverage: errors.py 100%/100%, http.py 97%/91%, analysts/data/* 100%/100%. Plan-level commits: `0ffcabf` (chore scaffold), `b370d9c` (RED tests), `b58f932` (GREEN http impl), `b206206` (RED schemas), `777ff28` (GREEN schemas). **Deviations:** 4 auto-fixed (1 Rule 1 bug — `raise_on_status=False`; 2 Rule 2 missing-critical — added `test_schemas_reject_non_string_ticker` for branch coverage + 4 extra schema tests beyond the planned ~16; 1 Rule 3 blocking — observed `uv.lock` is gitignored and skipped from staging). All tightening, no scope creep. DATA-06 marked complete in REQUIREMENTS.md.
- **2026-05-01 (Phase 2 / Plan 03 — EDGAR filings)**: `ingestion/filings.py` (252 lines) lands `lookup_cik` (in-process whole-index cache → 10-digit zero-padded CIK) + `fetch_filings` (default `forms=("10-K","10-Q","8-K")`, `limit=20`, sorted by `filed_date` desc). 22 tests green covering probes 2-W2-06 (happy), 2-W2-07 (403 → `NetworkError` with diagnostic), 2-W2-08 (429 retry via shared session adapter). Coverage **98% line / 100% branch** on `ingestion.filings` (gate ≥90%/≥85%). Full repo suite 112/112 green. UA header verified at the responses-lib boundary on BOTH endpoints (company_tickers + submissions). **Decisions:** (1) Whole-index cache is module-level `_CIK_CACHE` — single fetch fills the entire mapping for a process; tests reset via autouse fixture. (2) Unknown ticker returns `[]` (not error) — orchestrator interprets as "no filings, move on". (3) Bad row (unparseable filingDate / failed Pydantic validation) is logged + skipped per-row, never fails the whole fetch. (4) Form-type `"OTHER"` escape-hatch from Plan 02-01 schema is exercised — unknown forms passed through `forms=(...)` map cleanly. (5) Module-level `_LAST_CALL` (110ms gap = 9 req/sec under SEC's 10/sec ceiling) — filings.py is the SOLE owner of EDGAR's politeness budget. (6) urllib3 Retry adapter requires N+1 mocked responses for retry-exhaustion tests (Rule 3 auto-fix). (7) 403 diagnostic includes the exact UA header the session is using — speeds future debugging. Plan-level commits: `04b0316` (RED tests + 3 fixtures), `0fe0171` (GREEN impl + 8 hardening tests). DATA-03 marked complete. **Deviations:** 2 auto-fixed (1 Rule 2 missing-critical — added 8 hardening tests for coverage gate; 1 Rule 3 blocking — N+1 mock-response trick for retry-exhaustion tests). Parallel-safety constraint observed: only `ingestion/filings.py`, `tests/ingestion/test_filings.py`, and `tests/ingestion/fixtures/edgar_*` touched.
- **2026-05-02 (Phase 3 / Plan 01 — AgentSignal schema + tests/analysts/ foundation)**: Wave 1 / Foundation shipped. `analysts/signals.py` (120 lines) ships the locked output contract — `AgentSignal` Pydantic v2 with 5-state `Verdict` ladder (strong_bullish | bullish | neutral | bearish | strong_bearish), 0-100 confidence, evidence list (≤10 items, ≤200 chars each), `data_unavailable` bool, ticker normalized via `analysts.schemas.normalize_ticker`, `ConfigDict(extra="forbid")`, plus a schema-level `@model_validator(mode="after")` enforcing the locked invariant `data_unavailable=True ⟹ verdict='neutral' AND confidence=0` (closes 03-RESEARCH.md Pitfall #4). Public Literal types `Verdict` + `AnalystId` co-located at module level for downstream import. `tests/analysts/__init__.py` + `tests/analysts/conftest.py` (175 lines) ship the shared fixture toolbox: `frozen_now` (pinned UTC datetime 2026-05-01T13:30Z, same as Phase 2 fixtures), `make_ticker_config` + `make_snapshot` builder closures, and three module-level synthetic-history builders (`synthetic_uptrend_history`, `synthetic_downtrend_history`, `synthetic_sideways_history`) with deterministic noise (no `random.random()`; `0.001 * ((i % 5) - 2)` for HLC variance + `i * 100` for volume drift) so two calls with identical args produce byte-identical OHLCBar lists. `tests/analysts/test_signals.py` (215 lines) ships 24 tests (above the plan's ≥16 floor — `@pytest.mark.parametrize` over the 5 verdict literals expands to 5 instances, plus 2 tightening tests for the 200-char inclusive boundary and the AnalystId Literal coverage). `tests/analysts/test_invariants.py` (111 lines) ships 2 cross-cutting integration-style tests under `@pytest.mark.xfail(reason=..., strict=True)` markers — `test_always_four_signals` + `test_dark_snapshot_emits_four_unavailable` — flip GREEN naturally when 03-05 lands and removes the markers as its final task. `pyproject.toml` +1 line appends `"vaderSentiment>=3.3,<4"` to `[project.dependencies]` (no production import in this plan; staged here so 03-04 lands without a Wave 0 reshuffle). `.planning/REQUIREMENTS.md` widens ANLY-01..04 from 3-state ("bullish/bearish/neutral") to **5-state ladder** wording with explicit Literal values. `.planning/ROADMAP.md` corrects Phase 3 row + Phase 3 Goal from "Five Python analyst modules" → "Four Python analyst modules emit structured signals per ticker (5th is Phase 4 POSE)" and populates the Phase 3 Plans list with 5 checkbox entries (03-01..03-05). **Decisions:** (1) Schema-level invariant via `@model_validator(mode="after")` — buggy analysts can never write a `(data_unavailable=True, verdict='bullish', confidence=80)` signal to disk; Phase 5/6 consumers can rely on the contract without defensive checks. (2) Verdict + AnalystId Literal types co-located at module level — every Wave 2 analyst module imports them as the single source of truth, no string duplication. (3) Factory-fixture pattern (closures over keyword overrides) beats fixture-class pattern for unit-test readability when the same model needs 1-2 field overrides per test. (4) Synthetic-history builders are MODULE-level (NOT pytest fixtures) so Wave 2 test files can pin `n` explicitly (e.g. 252 bars for MA200 warm-up coverage in 03-03). (5) Determinism contract on synthetic builders — no random sources, byte-identical lists across calls. (6) `synthetic_sideways_history` parameters chosen by formula (20-bar sin period at 2% amplitude, no drift) — by construction MA20 ≈ MA50 ≈ MA200 ≈ start and ADX < 20 (range regime), which is what 03-03 needs for trend-gating verification. (7) `test_invariants.py` ships at Wave 1 with `strict=True` xfail markers — file collects cleanly even before 03-02..03-05 land (analyst-module imports inside each test body, so `ImportError` during XFAIL is the expected RED state); 03-05 explicitly removes the markers in its final task; an unexpected GREEN before then fails loudly so over-shipping in Wave 2 is caught immediately. (8) vaderSentiment added at Wave 1 with NO production import — dep + lockfile entry survives even if Wave 2 plans are reordered. (9) ROADMAP.md plan-progress row updated MANUALLY, not via `gmd-tools roadmap update-plan-progress 3` (precedent set in Plan 02-07 closeout — the command mangles the descriptive Phase Summary row format). 24 schema tests + 2 xfail invariant tests; coverage on `analysts/signals.py` **100% line / 100% branch** (gate ≥90% / ≥85%); full repo regression **212 passed + 2 xfailed** (up from 188 at Phase 2 close). **Deviations:** 2 auto-fixed (both Rule 2 — additive boundary/coverage tests beyond the plan's ≥16 floor: `test_evidence_string_at_cap_ok` for the 200-char inclusive boundary; `test_analyst_id_literal_rejects_unknown` for AnalystId Literal coverage). Plan-level commits: `64d0d38` (Task 1 chore — vaderSentiment dep), `201d617` (Task 2 test — fixture toolbox scaffold), `fe5f973` (Task 3 RED — failing tests for AgentSignal), `f03e3fe` (Task 3 GREEN — AgentSignal schema implementation), `f0931a2` (Task 4 test — xfail invariant scaffold), `d7a6871` (Task 5 docs — REQUIREMENTS widening), `a09be56` (Task 6 docs — ROADMAP "Five → Four"). ANLY-01..04 remain Pending (filled in by Wave 2 plans 03-02..03-05). Wave 2 fully unblocked from this side.
- **2026-05-02 (Phase 2 / Plan 07 — fundamentals analyst-consensus + prices history-depth bump)**: Phase 2 amendment — additive schema + ingestion change required by Phase 3 ANLY-04 (valuation analyst tertiary blend) and ANLY-02 (technicals warm-up). `analysts/data/fundamentals.py` (+21 lines) appends 4 new Optional analyst-consensus fields (`analyst_target_mean: Optional[float]`, `analyst_target_median: Optional[float]`, `analyst_recommendation_mean: Optional[float]`, `analyst_opinion_count: Optional[int]`) after `market_cap`; `extra="forbid"` discipline preserved. `ingestion/fundamentals.py` (+51 lines) ships new `_safe_int` helper (mirrors `_safe_float` defensive posture: rejects bools / NaN / inf / non-numeric strings; floors floats toward zero) + 4 new `info.get()` reads on `targetMeanPrice` / `targetMedianPrice` / `recommendationMean` / `numberOfAnalystOpinions` + 4 new constructor kwargs on `FundamentalsSnapshot(...)`. `ingestion/prices.py` (+13 lines) bumps `fetch_prices` default `period` from `"3mo"` (~63 trading bars) to `"1y"` (~252 bars; sufficient for MA200 / 6m momentum / stable ADX downstream). 11 new tests (9 fundamentals + 2 prices) all green; targeted suite 29/29; full repo 188/188 (up from 177 at Plan 02-06 close). **Decisions:** (1) Analyst-consensus fields are NOT in canonical-marker `data_unavailable` predicate (small-caps legitimately have zero coverage on healthy days). (2) `_safe_int` rejects strings outright (parity with `_safe_float`); floors floats toward zero via `int(v)`. (3) Pydantic v2 schema-boundary behavior locked: 42.0 coerces to 42 cleanly; 42.5 raises `ValidationError`. (4) `'1y'` chosen as smallest period satisfying ALL Phase 3 technicals requirements simultaneously; `'6mo'` would miss MA200, `'2y'` buys nothing extra. (5) Plan 02-06 orchestrator implicitly benefits from longer history (no `refresh.py` change needed — `fetch_prices(ticker)` no-kwarg call gets the new default transparently). **Deviations:** 1 cosmetic — Task 1 RED + Task 2 RED tests committed together in `a4672f1` (shared imports / fixtures); TDD discipline preserved (intermediate `7ba8b49` schema-only commit kept the 5 ingestion tests still red). Plan-level commits: `a4672f1` / `7ba8b49` / `6b55cda` / `23fde2f` / `1715208`. **Phase 2 closes complete: 7/7 plans, 188/188 tests green, DATA-01..08 all `[x]`.**
- **2026-05-01 (Phase 2 / Plan 05 — social ingestion)**: `ingestion/social.py` (334 lines) lands `fetch_social(ticker) -> SocialSignal` aggregating Reddit search.rss (across r/wallstreetbets+r/stocks+r/investing via the global search endpoint with `q=TICKER OR $TICKER`) + StockTwits trending list + StockTwits per-symbol stream — anonymous-only, no OAuth, no API keys. 26 tests green covering probes 2-W2-14 (Reddit happy + UA verification), 2-W2-15 (trending), 2-W2-16 (per-symbol). Coverage **94% line / 96% branch** on `ingestion.social` (gates ≥90/≥85). Plan-scoped suite 120/120 green. **Decisions:** (1) `REDDIT_USER_AGENT="markets/0.1 (anonymous research aggregator)"` applied via per-request `headers={...}` on `session.get()` — replaces shared session UA for THIS call only, leaves other modules unaffected (Pitfall #2). (2) Subreddit extraction is two-tier — primary regex `/r/([^/]+)/` against entry link, fallback to `entry.tags[0].term` (`<category term=>`); entries with neither dropped. (3) Sentiment whitelist: `{Bullish, Bearish}` lowercase to `{bullish, bearish}`, anything else (novel value, non-dict, non-string) → None. Survives StockTwits adding new sentiment classes. (4) Per-source failure isolated: each `_fetch_*` returns `[]` on any failure (HTTP exception, non-200, non-JSON, malformed); `data_unavailable=True` set ONLY when all three empty AND ticker absent from trending. (5) Trending rank is 1-based position; None when ticker not in list. (6) Invalid ticker → SocialSignal(ticker="INVALID", data_unavailable=True) with no HTTP calls — sentinel pattern matching `prices.py`. (7) `fetch_social` does not catch its own exceptions — only the helpers swallow upstream weather; aggregator branches purely on list lengths and `ticker in trending`. Plan-level commits: `8ed1a9c` (RED 14 tests + 3 fixtures), `284c168` (GREEN impl + 11 coverage-completing tests). DATA-05 marked complete. **Deviations:** 2 auto-fixed (both Rule 2 missing-critical — 11 coverage tests added beyond plan's ~10 to clear gate; 1 malformed-XML contract test). Parallel-safety constraint observed alongside 02-04 (news): only `ingestion/social.py`, `tests/ingestion/test_social.py`, and `tests/ingestion/fixtures/{reddit_search_aapl.xml, stocktwits_trending.json, stocktwits_aapl.json}` touched.

## Context Notes

- **All ingestion is keyless**: yfinance + SEC EDGAR (User-Agent only) + RSS feeds + Reddit RSS + StockTwits unauthenticated trending
- **LLM compute**: runs from user's Claude subscription via scheduled Claude Code routines — no Anthropic API key
- **Storage**: GitHub repo itself — daily JSON snapshots at `data/YYYY-MM-DD/TICKER.json` plus `_index.json` and `_status.json`
- **Frontend reads**: directly from `raw.githubusercontent.com` (no backend server)
- **Mid-day refresh**: single Vercel Python serverless function (keyless, no LLM)
- **Provenance discipline**: every reference-adapted file carries a header comment naming source file and modifications
- **Outputs**: dual-timeframe per ticker (short_term + long_term) with per-persona signals, position-adjustment state, and synthesizer-produced recommendation
- **Persona slate**: Buffett, Munger, Wood, Burry, Lynch, Open Claude Analyst (all markdown prompts in `prompts/personas/`)

## Open Items / Research Flags

- **Phase 5 implementation:** Verify current Claude Code routine constraints (cron syntax, output format, quota visibility, env-var injection for git push token, retry behavior)
- **Phase 2 implementation:** Verify yfinance current breakage state; pin known-good version
- **Phase 8 implementation:** Verify Vercel Python serverless cold-start timing under realistic ticker fetch + RSS parse path
- **v1.x (Phase 9 successor):** Endorsement performance math depends on user's actual newsletter sources — research at v1.x kickoff

## Open Items — Environmental

- **`uv` PATH:** `uv.exe` is at `C:/Users/Mohan/AppData/Roaming/Python/Python314/Scripts/`. This is currently NOT on the user PATH (bash or PowerShell). Each subsequent plan that runs `uv` commands must either prepend that directory to PATH per-command, or the user can permanently add it to PATH (recommended). Alternative: `pipx install uv`.
- **Hatchling editable-install gotcha confirmed:** When a plan creates a new top-level package listed in `[tool.hatch.build.targets.wheel] packages`, follow with `uv sync --reinstall-package markets` so the editable wheel is rebuilt to include it. (Plan 01 Task 2 hit this.)

## Last Touched

2026-05-04 after Phase 7 / Plan 01 (decision-support — VIEW-10 — PHASE 7 CLOSEOUT) execution complete; 6 production commits across 3 TDD tasks: `be734bd` (Task 1 RED — RecommendationBanner + ConvictionDots tests; 22 tests against non-existent components), `9c972a0` (Task 1 GREEN — RecommendationBanner.tsx 6×3 matrix hero + ConvictionDots.tsx Pitfall #6 layout-shift guard), `6460ad6` (Task 2 RED — DriversList + DissentPanel tests; 10 tests including explicit Pitfall #12 positive-assertion + Pitfall #2 accent-styling assertion), `d65f424` (Task 2 GREEN — DriversList.tsx UNIFORM RULE empty-state + DissentPanel.tsx always-render shell with claude_analyst accent path), `913eddd` (Task 3 RED — DecisionRoute test; 5 tests via vi.mock('@/lib/loadTickerData')), `c1a1c8e` (Task 3 GREEN — DecisionRoute.tsx + App.tsx third route + TickerRoute.tsx '→ Decision view' Link + Root.tsx comment update + fixtures-server.ts msftNoDissent option + decision.spec.ts Playwright round-trip + MSFT-no-dissent.json fixture). Final: **234 vitest tests across 32 test files** (197 Phase 6 baseline + 37 Phase 7); **63 Playwright passed + 6 mobile-only-skipped-on-desktop** across all 3 projects (chromium-desktop + mobile-safari iPhone 14 + mobile-chrome Pixel 7) in 13.2s; **Python repo regression: 659 tests still green** (frontend tests run separately). typecheck clean, build clean. **Phase 7 status: COMPLETE (1/1 plans).** VIEW-10 flipped Pending → Complete in REQUIREMENTS.md. ROADMAP.md Phase 7 row → Complete (1/1 plans, 2026-05-04). STATE.md frontmatter: current_plan: null, status: phase_complete, completed_plans: 32, completed_phases: 7. **Decisions:** (1) Banner color × weight matrix locked verbatim — action drives color (bullish/amber/fg-muted/bearish + /10 vs /15 opacity differentiator within color pairs); conviction drives visual weight (text-2xl/3xl/4xl + medium/semibold/bold). 18 visual states. Compile-time exhaustiveness via Record<DecisionRecommendation, ...> + Record<ConvictionBand, ...>. (2) Pitfall #12 explicit positive-assertion test — getByTestId('dissent-panel').toBeInTheDocument() for has_dissent: false case; would fail loud if a future refactor added an early-return-null regression. (3) Pitfall #2 accent palette verbatim from OpenClaudePin (border-accent/40 bg-accent/5) — Open Claude visually distinct from 5 canonical investor personas; header text "Open Claude Dissent" reinforces verbally. (4) PERSONA_LABEL local in DissentPanel — single-use; deferred extraction. (5) Phase 8 hookpoint pair (source comment + data-testid) — both grep targets present in DecisionRoute.tsx for the planner's deterministic search. (6) Cross-link wrapping in TickerRoute heading — both Links grouped in flex container preserves Phase 6 wrap-friendly aesthetic. (7) DriversList decoupled from TimeframeCard — recommendation IS the summary in decision-support context. (8) Lite-mode null-decision branch — explicit amber notice mirrors TickerRoute null-decision posture. **Deviations:** 2 auto-fixed (both Rule 3 blocking — App.tsx import ordering reordered to defer extensions into Task 3 GREEN commit; Playwright stale-build foot-gun resolved with pre-build, recurring pattern documented in Phase 6 Wave 3 + Wave 4 SUMMARYs). Both deviations were workflow ordering fixes; zero scope change; visual + architectural specs executed verbatim. **Phase 8 (Mid-Day Refresh + Resilience — REFRESH-01..04 + INFRA-06..07) UNBLOCKED.** Phase 8's plan-phase will grep DecisionRoute.tsx for PHASE-8-HOOK + current-price-placeholder to find the integration point, then wire <CurrentPriceDelta /> fed by useRefreshData(symbol) TanStack Query hook + api/refresh.py Vercel Python serverless function. Manual-only verifications (visual taste-check at /decision/AAPL/today; cross-link UX flow taste judgment) deferred to user per VALIDATION.md — those are user-facing concerns the assistant cannot regress and they do NOT block Phase 8. Next: `/gmd:plan-phase 8` for Phase 8 (Mid-Day Refresh + Resilience).

2026-05-04 after Phase 6 / Plan 05 (polish + responsive + Playwright — Wave 4 / VIEW-12 + VIEW-14 + VIEW-15 — PHASE 6 CLOSEOUT) execution complete; 3 task commits `4b713fb` (Task 1 — ErrorBoundary class component catching SchemaMismatchError + FetchNotFoundError per CONTEXT.md UNIFORM RULE; DateSelector native <select> reading data/_dates.json via useDates() TanStack hook; loadDates.ts useDates + __loadDatesForTest export; Root.tsx wraps Outlet in ErrorBoundary + adds DateSelector to header with flex-col sm:flex-row stacks; AAPL-bad-version.json fixture with schema_version=1; 18 new vitest tests — 8 ErrorBoundary + 6 DateSelector + 4 loadDates), `bf71ed1` (Task 2 — Chart touch options handleScroll horzTouchDrag + handleScale pinch + responsive height bracket 280/360/480 with ResizeObserver-driven recompute; LensTabs -mx-4 overflow-x-auto narrow-viewport containment + min-h-11 44px Apple HIG tap-target; 6 mobile-only Playwright responsive specs — both mobile-safari iPhone 14 + mobile-chrome Pixel 7 projects green), `64544c3` (Task 3 — error-boundary.spec.ts 3 specs covering schema_version mismatch + per-ticker 404 + snapshot 404; full-flow.spec.ts comprehensive happy-path E2E scan→3-lenses→deep-dive→DateSelector preserving :symbol; visual taste-check via systematic code audit confirms Notion-Clean spec). Final: **197 vitest tests across 27 test files** (179 Wave 3 baseline + 18 Wave 4); **60 Playwright passed + 6 mobile-only-skipped-on-desktop** across all 3 projects in 13.2s. **Python repo regression: 659 tests still green** (frontend tests run separately). typecheck clean. build clean. **Phase 6 status: COMPLETE (5/5 plans).** All 14 frontend VIEW requirements (VIEW-01..09 + VIEW-11..15) + INFRA-05 closed in REQUIREMENTS.md (VIEW-14 + VIEW-15 + INFRA-05 flipped Pending → Complete this plan; VIEW-12 already showed Complete from Wave 3 groundwork). ROADMAP.md Phase 6 row → Complete (5/5 plans, 2026-05-04). STATE.md frontmatter: current_plan: null, status: phase_complete, completed_plans: 31, completed_phases: 6. **Decisions:** (1) ErrorBoundary as SAFETY NET, not primary error surface — TanStack Query errors flow through query.error not thrown; routes handle inline; boundary is last-resort catch for lifecycle/non-query errors. (2) DateSelector native <select> over shadcn Select — Notion-Clean restraint; mobile gets iOS picker for free; zero layout shift; v1.x upgrade path open. (3) DateSelector preserves location.search (?lens=) on date navigation — without this, switching dates while ?lens=short would silently drop the lens. (4) ZodError version extraction walks issues for path ending 'schema_version', reads issue.received as the bad value; defensive fallback to 'snapshot is older' if extraction fails. (5) AAPL-bad-version.json minimal — empty arrays + nulls under schema_version=1; v2-literal violation without needing valid v2 data. (6) Chart vertTouchDrag: false — vertical page-scroll passes through; pan via horizontal drag; zoom via pinch. Otherwise users on mobile would be trapped by the chart. (7) Chart pickHeight reads window.innerWidth (NOT container width) — viewport-driven policy. (8) LensTabs -mx-4 + px-4 + sm:mx-0 sm:px-0 extends scroll-overflow region to page edge on mobile while keeping inner content padded. (9) Playwright responsive specs use document.documentElement.scrollWidth (TRUE horizontal overflow) with +2px sub-pixel tolerance. (10) Visual taste-check ran as code audit (NOT screenshot diff) — Notion-Clean adherence is structurally verifiable; real-device taste verification deferred to user per VALIDATION.md Manual-Only Verifications. (11) Playwright reuseExistingServer=true (locally) requires pnpm build before pnpm test:e2e (Wave 3 decision #10 lesson held; ran build between Task 2 and Task 3). **Deviations:** 1 auto-fixed (Rule 3 blocking — pnpm exec playwright install webkit; ~57.6MB download; Wave 1 SUMMARY explicitly predicted this on first Wave 4 run) + 4 plan-wording refinements (ErrorBoundary scoped as safety-net not primary; DateSelector tests vi.mock vs QueryClient + MSW for URL-routing focus; E2E error-wording regex accepts both ErrorBoundary verbose + inline-banner concise phrasings — same UNIFORM RULE concept; visual taste-check captured as code audit not screenshot diffs because real-device verification is the user-facing concern that screenshots can't capture). Zero scope creep — all refinements preserve VIEW-12 / VIEW-14 / VIEW-15 contracts. **Real-device manual verifications deferred to user per VALIDATION.md Manual-Only Verifications table** (iOS Safari real-device render; Android Chrome real-device render; Vercel deploy + real GitHub raw URL; final visual taste judgment) — those are user-facing concerns the assistant cannot regress and they do NOT block Phase 7. **Phase 7 (Decision-Support View + Dissent Surface — VIEW-10) UNBLOCKED.** Frontend infrastructure stable: schemas + fetchAndParse + ErrorBoundary + DateSelector + responsive scaffolding. Phase 7 adds /decision/:symbol route rendering recommendation banner + dissent surface using existing schemas (TickerDecision + DissentSection). **Phase 8 (Mid-Day Refresh + Resilience — REFRESH-01..04) UNBLOCKED.** TanStack Query staleTime (5min) + refetchOnWindowFocus (false) is the right baseline; Phase 8 adds api/refresh.py Vercel serverless triggered on deep-dive open via queryClient.invalidateQueries. Next: `/gmd:plan-phase 7` for Phase 7 (Decision-Support View — VIEW-10).

2026-05-04 after Phase 5 / Plan 06 (routine.{quota, storage, git_publish, run_for_watchlist, entrypoint} — Wave 5 / LLM-08 + INFRA-01..04 — PHASE 5 CLOSEOUT) execution complete; 7 task commits `026f09f` (Task 1 RED — 22 failing tests for routine.{quota, storage, git_publish}; ModuleNotFoundError on the 3 modules), `d8bbbe1` (Task 1 GREEN — quota estimator + 3-phase atomic write + 5-step git publish + TickerResult scaffold; 26 tests GREEN; 100% line/branch on quota/storage/git_publish), `1d5cff8` (Task 2 RED — 6 failing tests for routine.run_for_watchlist; ImportError on run_for_watchlist function), `f247360` (Task 2 GREEN — _run_one_ticker async helper + run_for_watchlist sync-across-tickers loop with per-ticker exception isolation + 3 coverage tests; 9 tests GREEN; 100% line/branch), `c3e597d` (Task 3 RED — 3 failing end-to-end integration tests for routine.entrypoint; ImportError on routine.entrypoint), `42c82dc` (Task 3 GREEN-ENTRYPOINT — main() 7-step orchestration + top-level exception handler + script-mode guard + 2 coverage tests; 5 tests GREEN; 96% line/branch — only `if __name__ == "__main__"` script-mode guard uncovered), `d48e408` (Task 3 CLOSEOUT-DOCS — REQUIREMENTS.md 12 entries flipped to Complete + ROADMAP.md Phase 5 row Complete + plan list 05-06 [x]). 37 new tests in 5 test files (5 quota + 12 storage + 6 git_publish + 9 run_for_watchlist + 5 entrypoint) all GREEN. Coverage: 100% line/branch on routine/{quota, storage, git_publish, run_for_watchlist}; 96% line on routine/entrypoint (clears the ≥90/85 gate). Full repo regression: **635 passed** (595 baseline post-05-05 + 40 new). **Phase 5 status: COMPLETE (6/6 plans).** All 12 LLM-XX + INFRA-XX requirements `[x]` in REQUIREMENTS.md (LLM-01..08 + INFRA-01..04 in v1 list AND traceability table); ROADMAP.md Phase 5 row → Complete (6/6 plans, 2026-05-04); Phase 5 plan list 05-06 entry [ ] → [x]. **Decisions:** (1) Pattern #4 three-phase write order locked: per-ticker JSONs FIRST → _index.json SECOND → _status.json LAST (run-final sentinel). (2) LLM-08 cascade-prevention: per-ticker OSError populates failed_tickers; loop continues. (3) llm_failure_count formula = persona data_unavailable + (1 per ticker where ticker_decision is None AND lite_mode=False). (4) DEFAULT_MARKETS_DAILY_QUOTA_TOKENS=600_000 chosen for 30-ticker comfortable headroom (594_000 < 600_000); env-overridable via MARKETS_DAILY_QUOTA_TOKENS. (5) 5-step git sequence with pull --rebase --autostash BEFORE add/commit/push handles manual-vs-scheduled-run race. (6) Anti-pattern lock against `git add -A` and `git add .` (AST-grep test). (7) subprocess.run kwargs locked: check=True + capture_output=True + text=True + timeout=60 + cwd=str(repo_root); 60s prevents hung-routine. (8) TickerResult Pydantic v2 with arbitrary_types_allowed=True. (9) Watchlist iteration via .values() (NOT .keys()) — corrects plan implementation_sketch which would have yielded strings. (10) Snapshot loader Callable[[ticker], Snapshot] parameterized; v1 stub raises NotImplementedError. (11) asyncio.run inside main() — single event loop owns all per-ticker fan-outs. (12) Top-level exception handler intentionally broad — main() must NEVER crash without writing best-effort _status.json; nested try/except inside the handler. (13) Provenance per INFRA-07: routine/entrypoint.py + routine/quota.py + routine/git_publish.py + routine/run_for_watchlist.py declare 'novel-to-this-project'; routine/storage.py references the watchlist/loader.save_watchlist atomic-write internal-precedent lineage. (14) Coverage cleared the ≥90/85 gate on every module. **Deviations:** 4 auto-fixed (1 Rule 3 blocking — TickerResult lands in Task 1 instead of Task 2 because storage tests need the type before the loop logic exists; pure scheduling re-shuffle. 1 Rule 1 bug — removed literal `git add -A` from routine/git_publish.py docstring because AST-grep test deliberately strict-flagged the doc-comment occurrence; rephrased to 'the all-files variant'. 2 Rule 2 missing-critical — added 3 coverage tests for routine/run_for_watchlist.py default branches; added 2 coverage tests for routine/entrypoint.py exception paths. All 4 are tightening within plan scope; zero scope creep). **Phase 6 (Frontend MVP) UNBLOCKED** — frontend can now read data/YYYY-MM-DD/{TICKER}.json (per-ticker schema lock: `{ticker, schema_version=1, snapshot_summary, analytical_signals, position_signal, persona_signals, ticker_decision, errors}`) + _index.json (run metadata) + _status.json (success/partial sentinel) via raw.githubusercontent.com. Phase 7 (Decision-Support View) and Phase 8 (Mid-Day Refresh) also unblocked. Next: `/gmd:plan-phase 6` for Phase 6 (Frontend MVP — Morning Scan + Deep-Dive — VIEW-01..09 + VIEW-11..15 + INFRA-05).

2026-05-04 after Phase 5 / Plan 04 (6 persona markdown prompts + routine/persona_runner.py async fan-out — Wave 3 / LLM-01 + LLM-02 + LLM-03 + LLM-04) execution complete; 2 task commits `ffb5132` (Task 1 RED — 14 tests / 40+ parametrized instances in tests/routine/test_persona_runner.py covering persona file presence + 5-section locked structure + voice-signature keyword anchors + Output Schema field names + claude_analyst.md "NOT a persona" framing + PERSONA_IDS canonical order + AnalystId Literal subset + module constants + load_persona_prompt cache/invalid-id + user_context content/determinism + run_one happy/default_factory + run_persona_slate fan-out/single-failure-isolation/wrong-signal-count + provenance per INFRA-07; RED state was 28 failed + 11 passed — line-count failures + ImportError on routine.persona_runner + provenance-grep failure on stub content) and `b205064` (Task 1 GREEN — 6 persona markdown content drops at prompts/personas/{buffett,munger,wood,burry,lynch,claude_analyst}.md replacing Wave 0 stubs with full content 102/102/102/98/100/106 lines respectively + routine/persona_runner.py 289 LOC: provenance docstring referencing virattt/ai-hedge-fund/src/agents/ + names all 5 reference persona files + PersonaId Literal + PERSONA_IDS tuple + 3 module constants PERSONA_MODEL/PERSONA_MAX_TOKENS/PERSONA_PROMPT_DIR + load_persona_prompt with @functools.lru_cache(maxsize=8) + build_persona_user_context + 4 small private formatters + closure-bound _persona_default_factory + async run_one + async run_persona_slate via asyncio.gather(return_exceptions=True) with outer-slot collapse + 6 coverage tests folded in for 100% line/branch). 45 tests in tests/routine/test_persona_runner.py (well above the ≥14 floor with parametrization expansion to 39 instances + 6 coverage tests) GREEN; coverage on routine/persona_runner.py: **100% line / 100% branch** (gate ≥90/85). Full repo regression: **549 passed** (504 baseline post-05-03 + 45 new). Pure-additive plan: zero Phase 1-4 + Wave 0 + Wave 1 + Wave 2 files modified; persona_runner not yet imported anywhere — Wave 5 entrypoint will import it. **Decisions:** (1) Reuse AgentSignal verbatim for persona output — no separate PersonaSignal class; AnalystId Literal widening from 05-01 (Wave 0) lets persona AgentSignals share the analytical AgentSignal contract end-to-end. (2) Prompts loaded at call time + @functools.lru_cache(maxsize=8) — LLM-02 lock; 6×N_TICKERS fan-out is one read per persona regardless of N_TICKERS. (3) Single shared user_context per ticker — built once via build_persona_user_context, passed to all 6 persona calls. Per-persona variation lives entirely in the SYSTEM prompt (markdown). Saves 6× context-build cost; tested for byte-identical determinism. (4) Closure-bound default factory captures persona_id + ticker + computed_at at call site so each retry-exhaustion produces a contextually-correct AgentSignal(verdict='neutral', confidence=0, evidence=['schema_failure'], data_unavailable=True). (5) PERSONA_IDS as immutable tuple, not list — usable as dict key, hashable, canonical iteration order for asyncio.gather (preserves submission order in result list, NOT completion order). (6) asyncio.gather(return_exceptions=True) outer defense — beyond call_with_retry's per-LLM retry, an UNCAUGHT Python-level exception in run_one collapses to default_factory at the outer layer. The synthesizer's compute_dissent always sees 6 AgentSignals, in canonical PERSONA_IDS order. (7) claude_analyst.md framed as "NOT a persona, NOT a lens — Claude's inherent reasoning surfaced" per user MEMORY.md feedback_claude_knowledge ("never let lenses replace inherent reasoning"). The 5 reference personas constrain to lenses by design — that's their value. claude_analyst's value is the COMPLEMENTARY surface (training-data context, comparable precedent, knowledge-cutoff acknowledgment, what-the-personas-don't-say). Explicit "novel-to-this-project" provenance — no virattt analog. (8) Defensive validation in run_persona_slate: len(analytical_signals) == 4 raises ValueError early — defensive against synthesizer-side ordering bugs ('fund/tech/nsen/val expected'). Saves cryptic downstream IndexError. **Phase 5 status: In Progress (4/6 plans complete — Wave 0 foundation + Wave 1 schema + Wave 2 LLM client + Wave 3 persona slate shipped; Waves 4-5 pending).** LLM-01 + LLM-02 + LLM-03 + LLM-04 marked Complete in REQUIREMENTS.md (Pending → Complete in traceability table; checkboxes `[ ]` → `[x]` on all 4); ROADMAP.md Phase 5 row → 4/6 plans, status "In Progress"; Phase 5 plan list updates 05-04 entry from `[ ]` to `[x]`. **Deviations:** 2 auto-fixed (1 Rule 1 — _summarize_snapshot used wrong attribute names `pe_ratio` / `return_on_equity` which don't exist on FundamentalsSnapshot — actual schema fields are `pe` / `roe` / `debt_to_equity`. Fixed in-place to actual schema field names. This was dead code at runtime that would have raised AttributeError when Wave 5's entrypoint passes populated snapshots; the partial-fundamentals coverage test exposed it. 1 Rule 2 — added 6 coverage tests beyond the planned 14-test floor to clear the 90/85% coverage gate: test_build_user_context_with_populated_snapshot / test_build_user_context_with_dark_position_signal / test_build_user_context_with_empty_snapshot_data / test_summarize_snapshot_partial_fundamentals / test_run_persona_slate_python_exception_isolated / test_load_persona_prompt_raises_file_not_found_when_file_missing). Both deviations are tightening, no scope creep. Wave 4 (05-05 synthesizer + dissent) UNBLOCKED — run_persona_slate's 6-AgentSignal output (PERSONA_IDS canonical order, length always 6, default-factory shape on per-persona failure) is exactly what compute_dissent reads. Wave 5 (05-06 entrypoint) UNBLOCKED — run_persona_slate is the per-ticker persona-call surface the routine's _run_one_ticker will call between analytical-signal scoring and synthesizer call. Async fan-out within ticker (~6× faster wall-clock); sync across tickers (Wave 5's run_for_watchlist enforces sync across the watchlist). Next: `/gmd:execute-plan 05-05`.

2026-05-04 after Phase 5 / Plan 03 (routine.llm_client + mock_anthropic_client fixture — Wave 2 / LLM-04 + LLM-05) execution complete; 2 task commits `ded5bfc` (Task 1 RED — 14 failing tests in tests/routine/test_llm_client.py + MockAnthropicClient + MockMessages fixture-replay implementation in tests/routine/conftest.py + pytest-asyncio>=0.23 dev dep + asyncio_mode='auto' configured) and `fe6a696` (Task 1 GREEN — routine/llm_client.py 193 LOC: provenance docstring referencing virattt/ai-hedge-fund/src/utils/llm.py per INFRA-07 + 4 module constants LLM_FAILURE_LOG/DEFAULT_MAX_RETRIES/_MESSAGE_TRUNCATION_LIMIT/_RAW_TRUNCATION_LIMIT + TypeVar T bound=BaseModel + async call_with_retry generic over T + _try_extract_raw + _log_failure helpers + 3 additional tests folded in for 100% coverage) landed in this executor session. 17 tests in tests/routine/test_llm_client.py (above the ≥10 floor) GREEN; coverage on routine/llm_client.py: **100% line / 100% branch** (gate ≥90/85). Full repo regression: **504 passed** (487 baseline post-05-02 + 17 new). Pure-additive plan: zero Phase 1-4 files touched; only pyproject.toml extended (pytest-asyncio dev dep + asyncio_mode='auto') and tests/routine/conftest.py replaced (Wave 0 placeholder → fixture-replay implementation). **Decisions:** (1) DEFAULT_MAX_RETRIES=2 (NOT 3) per Pattern #8 — keeps cost predictable; v1.x may add tenacity-backed exponential backoff. (2) TypeVar T bound=BaseModel makes call_with_retry generic over output_format — single function services Wave 3 (output_format=AgentSignal) AND Wave 4 (output_format=TickerDecision) without code duplication. (3) Defensive `except Exception` branch with kind='unknown_error' — never let an unexpected exception propagate out of call_with_retry; the per-ticker pipeline must continue. (4) Failure log truncation at the helper layer (1000 chars message / 5000 chars raw) — defensive against multi-MB structured errors. (5) Sort_keys=True + UTC ISO-8601 + LF on each JSONL record — byte-stable serialization mirrors Phase 1/2 atomic-write discipline at the per-record level. (6) Truncation tests target _log_failure DIRECTLY (not via call_with_retry) — Pydantic v2 ValidationError.__new__ requires title+line_errors and rejects bare __init__ overrides with TypeError, AND a real ValidationError's str() is auto-truncated at ~50 chars by Pydantic before reaching the wrapper; direct unit tests on the helper are higher-fidelity than fighting Pydantic exception machinery. (7) _try_extract_raw is best-effort — broad Exception catch returns None on any extraction failure; two dedicated defensive-path tests lock the contract. (8) isolated_failure_log autouse fixture on tests/routine/* — Wave 3-5 tests benefit transparently from per-test log isolation without explicit fixture declaration. (9) MockAnthropicClient populated here in Wave 2 (NOT duplicated across Wave 3-5 test files) — single fixture-replay implementation services persona_runner + synthesizer_runner + entrypoint integration tests downstream. (10) pytest-asyncio asyncio_mode='auto' — async test functions don't need explicit @pytest.mark.asyncio decoration; cleaner test bodies. **Phase 5 status: In Progress (3/6 plans complete — Wave 0 foundation + Wave 1 schema + Wave 2 LLM client shipped; Waves 3-5 pending).** LLM-04 + LLM-05 marked complete in REQUIREMENTS.md (Pending → Complete in traceability table; checkboxes `[ ]` → `[x]`); ROADMAP.md Phase 5 row → 3/6 plans, status "In Progress"; Phase 5 plan list updates 05-03 entry from `[ ]` to `[x]`. **Deviations:** 2 auto-fixed (1 Rule 1 — truncation test pivoted from a ValidationError-subclass approach to direct _log_failure unit tests due to Pydantic v2 __new__ constraints + Pydantic's auto-truncation of str(exc) at ~50 chars; 1 Rule 2 — added 2 defensive-path tests on _try_extract_raw to close coverage gap from 92%→100%). Both deviations are test-side hardening; production code in routine/llm_client.py matched the locked sketch byte-for-byte. Wave 3 (05-04 personas) + Wave 4 (05-05 synthesizer) + Wave 5 (05-06 entrypoint) all unblocked: they import call_with_retry + LLM_FAILURE_LOG + DEFAULT_MAX_RETRIES from routine.llm_client and reuse mock_anthropic_client + isolated_failure_log fixtures from tests/routine/conftest.py. The fixture-replay pattern means each Wave-3+ test reads as a specification ("given the LLM returns X, then the runner returns Y") rather than a chain of MagicMock spies. Next: `/gmd:execute-plan 05-04`.

2026-05-04 after Phase 5 / Plan 02 (TickerDecision Pydantic schema — Wave 1 / LLM-06) executor reconciliation complete; the 2 task commits `d0054e9` (Task 1 RED — 45 failing tests in tests/synthesis/test_decision.py covering shape/defaults + ticker normalization + 6+3+2 enum coverage + length caps + confidence range + data_unavailable invariant 4 paths + JSON round-trip + PEER-not-subtype + UTC datetime preservation) and `287dc72` (Task 1 GREEN — synthesis/decision.py 212 LOC: provenance docstring referencing TauricResearch/TradingAgents portfolio_manager.py per INFRA-07 + 3 module-level Literal types DecisionRecommendation/ConvictionBand/Timeframe + TimeframeBand + DissentSection + TickerDecision + 3 validators including @model_validator(mode='after') enforcing data_unavailable=True ⟹ recommendation='hold' AND conviction='low') landed in the prior session. This executor pass authored 05-02-SUMMARY.md and brought STATE.md / ROADMAP.md / REQUIREMENTS.md up to date for both 05-01 and 05-02 closeouts (the 05-01 closeout commit `fe7a818` only added 05-01-SUMMARY.md without state/roadmap/requirements updates). 45 tests in tests/synthesis/test_decision.py (well above the ≥18 floor) GREEN; coverage on synthesis/decision.py: **100% line / 100% branch** (gate ≥90/85). Full repo regression: **487 passed** (442 baseline post-05-01 + 45 new). Pure-additive plan: zero Phase 1-4 files touched, zero existing test files modified. PEER-not-subtype contract with AgentSignal AND PositionSignal locked by dedicated `test_ticker_decision_not_subclass_of_other_signals`. **Decisions:** (1) Schema-level @model_validator(mode='after') invariant — buggy synthesizer code can never write a (data_unavailable=True, recommendation='buy', conviction='high') TickerDecision to disk; matches AgentSignal._data_unavailable_implies_neutral_zero + PositionSignal._data_unavailable_implies_fair_zero precedent. Error message includes the literal substring "data_unavailable=True invariant violated" so violation tests can grep on it. (2) TimeframeBand has NO confidence default — caller MUST set explicitly; avoids silent-zero-confidence trap. (3) TimeframeBand.summary has min_length=1 — empty summaries are a synthesizer bug. (4) DissentSection.dissenting_persona is `str | None`, NOT a Literal of the 6 persona IDs — keeps the dissent contract decoupled from analysts.signals.AnalystId so Phase 5+ can extend the persona slate via markdown drop without forcing a schema migration. (5) schema_version=1 + extra='forbid' coordinated pair — Phase 9 + v1.x can additively extend; structural changes require deliberate version bump. (6) PEER-not-subtype with AgentSignal AND PositionSignal — three peer schema families coexist; none inherit from any other. (7) Provenance per INFRA-07 — module docstring references TauricResearch/TradingAgents/tradingagents/agents/managers/portfolio_manager.py + enumerates 6 modifications (6-state vs 5-state recommendation; explicit DissentSection — novel; dual-timeframe TimeframeBand vs single-timeframe; ConfigDict extra='forbid'; ticker normalize delegation; @model_validator data_unavailable invariant). (8) ROADMAP.md plan-progress row updated MANUALLY per precedent (gmd-tools roadmap update-plan-progress mangles the descriptive Phase Summary row format). **Phase 5 status: In Progress (2/6 plans complete — Wave 0 foundation + Wave 1 schema shipped; Waves 2-5 pending).** LLM-06 marked complete in REQUIREMENTS.md (Pending → Complete in traceability table; checkbox `[ ]` → `[x]`); ROADMAP.md Phase 5 row → 2/6 plans, status "In Progress"; Phase 5 plan list added with 2 `[x]` entries (05-01 + 05-02) and 4 `[ ]` entries (05-03..05-06). **Deviations:** 0 — implementation matched the locked sketch byte-for-byte; all 45 tests passed on first GREEN run; the executor reconciliation found both commits already landed and required no implementation work, only the metadata closeout. Plan-level commits: `d0054e9` (Task 1 RED), `287dc72` (Task 1 GREEN). Wave 2 (05-03 LLM client) + Wave 3 (05-04 personas) + Wave 4 (05-05 synthesizer) + Wave 5 (05-06 routine entrypoint) all unblocked from the schema side; the next gating dependency is 05-03 LLM client which Wave 3 personas + Wave 4 synthesizer both consume. Next: `/gmd:execute-plan 05-03`.

2026-05-03 after Phase 5 / Plan 01 (Wave 0 foundation — anthropic SDK install + AnalystId Literal widening 4→10 + 4 package markers + 7 prompt markdown stubs) execution complete; 4 task commits `667b015` (Task 1 RED — failing tests for widened AnalystId Literal + scaffold routine/synthesis packages), `4dcd592` (Task 1 GREEN — widen AnalystId Literal to 10 IDs: 4 analytical + 6 persona slate buffett/munger/wood/burry/lynch/claude_analyst), `8c8ffc1` (Task 2 GREEN — scaffold prompts/personas/ stubs + synthesizer.md + SDK smoke test), `fe7a818` (docs — 05-01-SUMMARY.md only). 14 new tests added (12 widening parametrized + 2 SDK smoke tests). `pyproject.toml` 3 surgical edits (anthropic>=0.95,<1 dep + routine/synthesis added to [tool.hatch.build.targets.wheel].packages + coverage.source); uv resolved anthropic 0.97.0. `analysts/signals.py` AnalystId Literal widened from 4 IDs (fundamentals/technicals/news_sentiment/valuation) to 10 IDs (4 analytical preserved in original order + 6 persona slate appended in iteration order matching Wave 3's asyncio.gather fan-out). 4 empty package markers (routine/, synthesis/, tests/routine/, tests/synthesis/) + placeholder conftest.py at tests/routine/. 7 markdown stubs at prompts/personas/{6 personas}.md + prompts/synthesizer.md with locked 5-section structure (Voice Signature / Input Context / Task / Output Schema / Provenance). 2 SDK smoke tests verify `from anthropic import AsyncAnthropic, APIError, APIStatusError` works AND `client.messages.parse` attribute is present. Full repo: **442 passed** (428 Phase 4 baseline + 14 new). Phase 1-4 regression invariant: ALL 428 existing tests stayed GREEN. **Decisions:** (1) anthropic SDK pinned at >=0.95,<1 per 05-RESEARCH.md CORRECTION #3 (the `output_format=PydanticModel` parameter shipped in 0.95). (2) AnalystId widening preserves order — 4 analytical first, 6 persona slate second; matches Wave 3 PERSONA_IDS tuple iteration order. (3) Persona output IS an AgentSignal verbatim — no separate PersonaSignal class; Phase 5's only schema change in analysts/ is the Literal widening. (4) 5-section markdown structure locked at Wave 0 so Wave 3's voice-signature-presence assertions surface actionable failures (not "file not found"). (5) `uv.lock` stays gitignored per pre-existing project convention (treated as a build artifact). (6) `tests/conftest.py` extended at the root level with the `frozen_now` fixture (lifted as duplicate from `tests/analysts/conftest.py`) so tests/synthesis/ + tests/routine/ inherit it via root-level discovery without re-declaring; tests/analysts/conftest.py keeps its own definition for defensive backwards compat. **Deviations:** 0 — implementation matched the locked sketch byte-for-byte. **Phase 5 status: In Progress (1/6 plans complete).** Wave 0 closes; Plan 05-02 (TickerDecision schema) UNBLOCKED → executed immediately after this in the same session as commits `d0054e9` + `287dc72`. The 05-01 closeout commit `fe7a818` authored 05-01-SUMMARY.md but did NOT update STATE.md / ROADMAP.md / REQUIREMENTS.md — those updates fold into 05-02's closeout via this paragraph + the 05-02 paragraph above.

2026-05-03 after Phase 4 / Plan 03 (position_adjustment analyst — 6-indicator consensus + ADX trend-regime gating + 5-state ladder + 4-state action_hint) execution complete; 2 task commits `3485f5b` (Task 1 RED — ~36 RED tests, parametrized expansion to 59 effective + cross-cutting test_dark_snapshot_emits_pose_unavailable in test_invariants.py) and `11e8639` (Task 2 GREEN — analysts/position_adjustment.py implementation + 4 Rule-1 test-side fixes folded in: sub-signal mapper sign correction; sideways state assertion loosening; ADX range-or-ambiguous union; downweight test rewritten with helper monkeypatch). 59 new tests in `tests/analysts/test_position_adjustment.py` + 1 cross-cutting invariant. Coverage on `analysts/position_adjustment.py`: **94% line+branch combined** (gate ≥90/85). Full repo: **428 passed** (368 baseline + 60 new). PEER-not-subtype contract with AgentSignal locked. ADX boundary discontinuity mitigated by ambiguous zone (20-25); trend-regime gating verified at exactly ADX=25 via monkeypatch. **Phase 4 status: COMPLETE (3/3 plans).** POSE-01..05 all marked complete in REQUIREMENTS.md (Pending → Complete in traceability table; checkboxes `[ ]` → `[x]`); ROADMAP.md Phase 4 row → Complete with completion date 2026-05-03; Phase 4 plan list added with 3 `[x]` entries. **Phases 1-4 complete: 4/9 phases shipped, 428 tests green, 0 xfailed.** Next: Phase 5 (Claude Routine Wiring) plan-phase via `/gmd:plan-phase 5`.

2026-05-03 after Phase 4 / Plan 02 (PositionSignal Pydantic schema) execution complete; 2 task commits `e146a44` (Task 1 RED — 27 failing tests, parametrized expansion to 35 effective) and `fa89b14` (Task 1 GREEN — analysts/position_signal.py implementation, all 35 green on first run, 0 deviations). 35 new tests in `tests/analysts/test_position_signal.py` (above plan's ≥12 floor). Coverage on `analysts/position_signal.py`: **100% line / 100% branch** (gate ≥90/85). Full repo: **368 passed** (333 baseline + 35 new). PEER-not-subtype contract with AgentSignal locked by dedicated issubclass test. Schema-level @model_validator(mode='after') enforces the 5-field data_unavailable=True invariant — closes 04-RESEARCH.md Pitfall #1 at construction time. **Phase 4 status: In Progress (2/3 plans complete).** Wave 1 closes; Wave 2 (04-03 position_adjustment analyst, the final Phase 4 plan) unblocked. Next: `/gmd:execute-plan 04-03`.

2026-05-03 after Phase 4 / Plan 01 (Wave 0 foundation refactor — extract `_build_df` + `_adx_14` + `_total_to_verdict` to shared `analysts/_indicator_math.py` + 3 new synthetic-history builders for explicit overbought/oversold/range regression testing) execution complete; 4 task commits `bc4d56d` (Task 1 RED — failing tests for indicator_math), `3e35fa8` (Task 1 GREEN — refactor extracts helpers + refactors 3 callers), `0be3138` (Task 2 RED — failing tests for synthetic fixtures), `0328292` (Task 2 GREEN — append 3 builders to conftest.py). 23 new tests added (19 indicator_math + 4 synthetic_fixtures). Coverage on `analysts/_indicator_math.py`: **97% line+branch combined** (gate ≥90/85). Phase 3 regression invariant: ALL 310 existing tests stayed GREEN through both refactor and fixture additions. Full repo: **333 passed** (310 baseline + 23 new). **Phase 4 status: In Progress (1/3 plans complete).** Wave 0 closes; Wave 1 (04-02 PositionSignal schema) and Wave 2 (04-03 position_adjustment analyst) unblocked. Next: `/gmd:execute-plan 04-02`.

2026-05-03 after Phase 3 / Plan 05 (valuation analyst — thesis/targets/consensus 3-tier blend with density-weighted confidence + Wave 2 closeout removing xfail markers) execution complete; 3 task commits `037324e` (Task 1 RED — failing tests for valuation analyst), `e89b293` (Task 2 GREEN — analysts/valuation.py implementation, all 23 tests green on first try, 0 deviations), `36f3912` (Task 3 — Wave 2 closeout, xfail markers REMOVED from tests/analysts/test_invariants.py; both cross-cutting tests flipped GREEN naturally). 23 new tests in `tests/analysts/test_valuation.py` (above plan's ≥18 floor). Coverage on `analysts/valuation.py`: **100% line / 100% branch** (gate ≥90/85). Full repo suite **310 passed, 0 xfailed** (up from 285 + 2 xfailed at end of 03-04: +23 valuation tests + 2 invariants flipping from xfail → pass). **Phase 3 status: COMPLETE (5/5 plans).** ANLY-04 marked complete in REQUIREMENTS.md (Pending → Complete); ANLY-01..04 ALL `[x]` (full Phase 3 traceability closed). Wave 2 closeout verified: the strict=True markers held throughout Wave 2 (Plans 03-02 / 03-03 / 03-04 / 03-05) — they did NOT unexpectedly turn GREEN at any earlier plan; the 2 cross-cutting invariants flipped GREEN ONLY when the markers were removed in 03-05's Task 3, confirming all four analysts respect the cross-cutting contracts. Next: `/gmd:plan-phase 4` (Position-Adjustment Radar — POSE-01..05).

- **2026-05-02 (Phase 3 / Plan 01 closeout)**: SUMMARY.md authored at `.planning/phases/03-analytical-agents-deterministic-scoring/03-01-SUMMARY.md` matching the rich format used by `02-06-SUMMARY.md` (frontmatter + Performance + Accomplishments + Task Commits + Files Created/Modified + Decisions Made + Deviations + Self-Check + Next Phase Readiness). STATE.md and ROADMAP.md updated for Phase 3 plan 1/5 progress. ROADMAP plan-progress row updated MANUALLY (not via `gmd-tools roadmap update-plan-progress 3`) per the precedent set in Plan 02-07 closeout — that command mangles the descriptive row format.

- **2026-05-03 (Phase 3 / Plan 05 — valuation analyst + Wave 2 closeout)**: Wave 2 / fourth and FINAL analyst lands. `analysts/valuation.py` (~250 lines) ships pure-function `score(snapshot: Snapshot, config: TickerConfig, *, computed_at: Optional[datetime] = None) -> AgentSignal` deterministically scoring current price against three valuation anchors with explicit precedence weights — Tier 1 (W_THESIS=1.0): config.thesis_price; Tier 2 (W_TARGETS=0.7 each): config.target_multiples.{pe,ps,pb}_target vs snapshot.fundamentals.{pe,ps,pb}; Tier 3 (W_CONSENSUS=0.5): snapshot.fundamentals.analyst_target_mean vs current_price (REQUIRES 02-07). Each tier scored via `_signed_gap(anchor, current)` linearization saturated at GAP_SATURATION=0.5 (50% premium / 50% discount = ±1). Density-weighted confidence rewards multiple agreeing tiers — `density = min(1.0, total_w / 2.2)` saturates at 1.0 when 2+ tiers active with multiple multiples; ~0.45 when only thesis_price set. `confidence = min(100, int(round(abs(aggregate) * 100 * density)))`. Empty-data UNIFORM RULE (4 short-circuit branches): snapshot.data_unavailable / prices=None / prices.data_unavailable / current_price None or ≤0 → AgentSignal(data_unavailable=True, evidence=["no current price"]). None-configured UNIFORM RULE: when ALL three tiers absent → evidence=["no thesis_price, no target_multiples, no consensus"] (distinct from empty-data because the root cause is configuration, not ingestion). Cross-phase 02-07 dependency is HARD — `snapshot.fundamentals.analyst_target_mean` read directly (no getattr-defensive); locked by dedicated `test_02_07_dependency_present` (asserts the field is in `FundamentalsSnapshot.model_fields`). Verdict tiering reuses strict > 0.6 / 0.2 boundaries from fundamentals/technicals (DRY trigger fired with the 3rd copy; shared `analysts.verdict._total_to_verdict` deferred to v1.x). Provenance header (INFRA-07) names `virattt/ai-hedge-fund/src/agents/valuation.py` and explicitly documents the methods divergence (virattt: DCF + Owner Earnings + EV/EBITDA + Residual Income from financials; we: thesis_price + target_multiples + yfinance consensus per 03-CONTEXT.md scoring philosophy). The multi-method weighted aggregation pattern carries over verbatim — only the methods themselves differ. `tests/analysts/test_valuation.py` (~520 lines, 23 tests) above plan's ≥18 floor — covers 02-07 dep / 4 thesis-only / 4 targets-only (pe / ps / pe+ps blend / pb) / 3 consensus-only (bullish / bearish / opinion_count=None) / 1 all-three blend / 1 none-configured / 4 empty-data / 1 density-confidence / 4 determinism+provenance+meta. **Wave 2 CLOSEOUT (Task 3): tests/analysts/test_invariants.py @pytest.mark.xfail markers REMOVED.** With all four Wave 2 analyst modules shipped (fundamentals + technicals + news_sentiment + valuation), both cross-cutting invariant tests flipped GREEN naturally: `test_always_four_signals` (4 AgentSignals produced; analyst_id set covers all 4 canonical Literal values) and `test_dark_snapshot_emits_four_unavailable` (dark snapshot → 4 signals all with data_unavailable=True / verdict='neutral' / confidence=0 / 1 evidence string each). The strict=True markers held throughout Wave 2 — they did NOT unexpectedly turn GREEN at any earlier plan, confirming no Wave 2 plan accidentally over-shipped the cross-cutting contracts. **Decisions:** (1) Three-tier blend with explicit precedence (1.0 / 0.7 / 0.5) per 03-CONTEXT.md scoring philosophy. (2) Per-multiple sub-signals at the targets tier — each of pe/ps/pb yields its own sub-signal at W_TARGETS=0.7 (encourages user to encode their full thesis). (3) `_signed_gap` saturated at GAP_SATURATION=0.5 — prevents extreme outliers (e.g. typo'd thesis_price 10x current) from dominating the aggregate. (4) Density-weighted confidence math `density = min(1.0, total_w / 2.2)` — saturates at 1.0 when 2+ tiers active with multiple multiples; locked by `test_density_bonus_more_tiers_more_confidence`. (5) Cross-phase 02-07 dependency is HARD — direct attribute read; fail loudly at AttributeError if 02-07 reverts. (6) Empty-data UNIFORM RULE collapses 4 branches to a single 'no current price' evidence string — distinguishing them offered no debugging value. (7) None-configured UNIFORM RULE distinct from empty-data — different root cause (config vs ingestion); evidence string tells user what to do. (8) Verdict tiering reuses strict > 0.6 / 0.2 — DRY trigger fired but shared helper deferred to v1.x. (9) Wave 2 closeout: xfail markers removed in the same plan that ships the last analyst. (10) ROADMAP.md plan-progress row updated MANUALLY per precedent. 23 tests green; coverage on `analysts/valuation.py` **100% line / 100% branch** (gate ≥90/85). Full repo regression **310 passed, 0 xfailed** (up from 285 + 2 xfailed at end of 03-04: +23 valuation tests + 2 invariants flipping from xfail → pass). **Phase 3 / Wave 2 analyst implementation COMPLETE.** **Deviations:** 0 — implementation matches the plan's specifications byte-for-byte; all 23 tests passed on first GREEN run; Wave 2 closeout ran clean (no analyst-module bug surfaced by the integration tests). Plan-level commits: `037324e` (Task 1 RED — failing tests for valuation analyst), `e89b293` (Task 2 GREEN — analysts/valuation.py implementation), `36f3912` (Task 3 — Wave 2 closeout, xfail markers removed). ANLY-04 marked complete in REQUIREMENTS.md (Pending → Complete in traceability table; checkbox `[ ]` → `[x]`). **Phase 3 closes complete: 5/5 plans, 310/310 tests green, 0 xfailed, ANLY-01..04 all `[x]`.**

- **2026-05-03 (Phase 3 / Plan 04 — news/sentiment analyst)**: Wave 2 / third analyst lands. `analysts/news_sentiment.py` (~225 lines) ships pure-function `score(snapshot: Snapshot, config: TickerConfig, *, computed_at: Optional[datetime] = None) -> AgentSignal` classifying per-headline VADER compound polarity weighted by exponential recency decay (3-day half-life: `DECAY_K = math.log(2.0)/3.0 ≈ 0.231`) and source credibility (yahoo-rss/google-news=1.0, finviz=0.7, press-wires=0.5 forward-compat dead code per Pitfall #6). Lazy VADER singleton: `_VADER = None` at module-import time + `_vader()` accessor with lazy `from vaderSentiment.vaderSentiment import SentimentIntensityAnalyzer` inside the accessor — schema-only test runs (test_signals.py + conftest.py) don't pay VADER's import cost. Filters: undated headlines (published_at=None) dropped → silent; short-title headlines (<20 chars) dropped → surfaced in evidence as "noise floor"; stale headlines (>14 days, decay weight < 4%) dropped → silent. All-filtered case (n_used==0 BUT len(news) > 0) returns canonical data_unavailable signal with evidence enumerating ALL three filter reasons for full diagnostic transparency. Empty-data UNIFORM RULE: `snapshot.data_unavailable=True` OR `not snapshot.news` → data_unavailable signal with `evidence=["no news headlines available"]`. Verdict tiering uses VADER-distribution-tuned boundaries: `COMPOUND_STRONG_BULLISH=0.4 / COMPOUND_BULLISH=0.05 / COMPOUND_BEARISH=-0.05 / COMPOUND_STRONG_BEARISH=-0.4` — tighter than fundamentals/technicals 0.6/0.2 because VADER compounds for financial headlines cluster in [-0.5, +0.5]. Confidence formula `min(100, int(round(min(1.0, abs(weighted_compound) * 2.5) * 100)))` — the 2.5x multiplier stretches |compound|=0.4 to confidence=100. Pitfall #7 single-now discipline: `now = computed_at if computed_at is not None else datetime.now(timezone.utc)` read ONCE at top of score(); `_score_one` and `_aggregate` accept `now` as a parameter; no helper calls `datetime.now()` directly (locked by `test_no_module_level_clock_in_helpers` via AST walk). Provenance header (INFRA-07) names `virattt/ai-hedge-fund/src/agents/news_sentiment.py` and explicitly documents the LLM-vs-VADER divergence: virattt classifies sentiment via async `call_llm()` (paid, non-deterministic, INFRA-02-incompatible); we use VADER (free, deterministic, lite-mode-friendly). Documented accuracy gap (~55-68% on financial headlines vs FinBERT's ~85% per published comparison studies) acknowledged in docstring with forward pointer to FinVADER / Loughran-McDonald deferred ideas in 03-CONTEXT.md. virattt has NO recency or source weighting; we add both per 03-CONTEXT.md scoring philosophy. `tests/analysts/test_news_sentiment.py` (~525 lines, 22 tests) above plan's ≥18 floor — covers lazy-init / 3 happy paths / 2 recency / 2 source / 3 filtering / 2 all-filtered / 2 empty-data / 8 determinism+provenance+meta. **Decisions:** (1) VADER, not LLM — full sentiment-classifier divergence from virattt for INFRA-02 lite-mode compatibility. (2) Lazy VADER singleton — module import doesn't pay VADER's cost. (3) 3-day half-life recency decay; 14-day stale-drop floor — per 03-CONTEXT.md threshold defaults. (4) Source weights: yahoo+google=1.0, finviz=0.7; unknown sources default to 0.5 (defensive). (5) press-wires=0.5 forward-compat dead code per Pitfall #6 — locked by dedicated test that asserts dict entry + comment present. (6) Verdict boundaries 0.4/0.05/-0.05/-0.4 — VADER-distribution-tuned (tighter than fundamentals/technicals 0.6/0.2). (7) Confidence stretching 2.5x — maps |compound|=0.4 to confidence=100. (8) Filters in order: undated → short → stale (cheapest first). (9) n_stale silent in evidence; n_undated + n_short surfaced (ingestion-bug signals the user might want to know). (10) `now` read ONCE at top of score(); helpers receive `now` as parameter (Pitfall #7). (11) ROADMAP.md plan-progress row updated MANUALLY per precedent. 22 tests green; coverage on `analysts/news_sentiment.py` **91% line+branch combined** (gate ≥90/85). Full repo regression **285 passed + 2 xfailed** (up from 263 + 2 xfailed at end of 03-03: +22 news_sentiment tests; xfail markers in test_invariants.py held strict — Wave 2 still needs 1 more analyst module to flip them green; 03-05 removes the markers as its final task). **Deviations:** 1 auto-fixed (Rule 1 — test-side bug surfaced during GREEN: `test_no_module_level_clock_in_helpers` substring scan false-positived on docstring text legitimately documenting the score() contract; switched to AST walk that parses the module and verifies no helper function calls `datetime.now()`). Plan-level commits: `7787aee` (Task 1 RED — failing tests for news/sentiment analyst), `c26a65b` (Task 2 GREEN — analysts/news_sentiment.py implementation + 1 test-side fix folded in). ANLY-03 marked complete in REQUIREMENTS.md (Pending → Complete in traceability table; checkbox `[ ]` → `[x]`).

- **2026-05-03 (Phase 3 / Plan 03 — technicals analyst)**: Wave 2 / second analyst lands. `analysts/technicals.py` (414 lines) ships pure-function `score(snapshot: Snapshot, config: TickerConfig, *, computed_at: Optional[datetime] = None) -> AgentSignal` deterministically scoring MA stack alignment (MA20/MA50/MA200), momentum at 1m/3m/6m horizons (21/63/126 bars; ±5/10/15% thresholds), and Wilder ADX(14) trend regime against the snapshot's price history. All indicator math hand-rolled with `pandas.Series.rolling().mean()` (SMA) and `.ewm(alpha=1/14, adjust=False).mean()` (Wilder smoothing — mathematically identical to Wilder's recursive formula per StockCharts walkthrough) — NO pandas-ta / ta-lib / pandas-ta-classic dependency (pandas + numpy come transitively via yfinance, verified 03-RESEARCH.md). Min-bars warm-up guards: <20 bars → data_unavailable=True; <27 → ADX skipped (Wilder needs 2N-1=27 bars to settle); <50 → MA stack skipped (no MA50); 50..199 → MA20/MA50 only with "MA200 unavailable" evidence; <22/64/127 → 1m/3m/6m momentum each skipped. Closes 03-RESEARCH.md Pitfall #1 — virattt/ai-hedge-fund's reference implementation has no min-bars guards and silently produces NaN at iloc[-1] for cold-start tickers. ADX-as-amplifier rule (informational by default, contributes +1/-1 vote ONLY when ADX > 25 AND stacks_total != 0). Empty-data UNIFORM RULE: snapshot.data_unavailable / prices is None / prices.data_unavailable / empty history → AgentSignal(data_unavailable=True, ...) with distinct evidence reason per branch. Provenance header (INFRA-07) names `virattt/ai-hedge-fund/src/agents/technicals.py` and enumerates 6 modifications (SMA stacks vs EMA stacks; min-bars guards added; MA + momentum + ADX only — RSI/Bollinger/MACD/Hurst belong to Phase 4 POSE; pure pandas — no ta-lib / pandas-ta dep; pure function vs graph node; UNIFORM RULE empty-data guard). `tests/analysts/test_technicals.py` (~510 lines, 25 tests) above the plan's ≥18 floor — 3 known-regime regressions (252-bar uptrend / downtrend / sideways using 03-01 conftest synthetic builders), 5 warm-up cases (15 / 25 / 50 / 100 bars + lt20 floor), 4 empty-data UNIFORM RULE branches, 4 indicator-correctness tests (bullish stack / bearish stack / mixed / sort defense), 2 ADX regime tests, 7 provenance/determinism/contract tests. **Decisions:** (1) SMA stacks (MA20/MA50/MA200), not EMA stacks (8/21/55) — per 03-CONTEXT.md, matches stockcharts/finviz convention. (2) Min-bars guards added — virattt has none; closes 03-RESEARCH.md Pitfall #1. (3) MA + momentum + ADX only; RSI/Bollinger/MACD/Hurst defer to Phase 4 POSE. (4) Pure pandas — no pandas-ta / ta-lib / pandas-ta-classic dep; pandas + numpy come transitively via yfinance. (5) Wilder smoothing as `.ewm(alpha=1/14, adjust=False).mean()` (mathematical identity locked in docstring). (6) ADX-as-amplifier rule — informational by default; bounded amplifier (max 1 vote, only fires when stacks_total != 0). (7) Aggregate normalization against MAX POSSIBLE score (5), NOT n_scored — same partial-data damping pattern as 03-02. (8) Defensive normalized clamp at [-1, +1] before _total_to_verdict. (9) Empty-data branches each emit distinct evidence reason string for downstream debugging. (10) `_build_df` defensive `dropna(subset=['close'])` before iloc[-1] — survives future schema relaxation. (11) Sideways regression assertion is loose (verdict ∈ {neutral, bullish, bearish}, confidence ≤ 40, strong tier MUST NOT be hit) — sideways noise + integer rounding can tilt either direction at the boundary. (12) ROADMAP.md plan-progress row updated MANUALLY per precedent. 25 tests green; coverage on `analysts/technicals.py` **92% line+branch combined** (gate ≥90/85). Full repo regression **263 passed + 2 xfailed** (up from 238 + 2 xfailed at end of 03-02: +25 technicals tests; xfail markers in test_invariants.py held strict — Wave 2 still needs 2 more analyst modules to flip them green; 03-05 removes the markers as its final task). **Deviations:** 2 auto-fixed (both Rule 1 — test-side bugs surfaced during GREEN: (a) `test_pandas_imported_no_pandas_ta` substring scan false-positived on the provenance docstring's legitimate enumeration of pandas-ta / ta-lib as deps we deliberately rejected — tightened to inspect actual `import` / `from … import` statements via regex; (b) `test_ma_alignment_mixed` V-shape construction was too aggressive — 150 down + 50 up @ +2% daily pulled MA50 above MA200 producing a fully bullish stack instead of mixed; rebalanced to 200 down @ -0.5% + 30 up @ +0.5% so MA200 stays elevated above MA50 while MA20 > MA50). Plan-level commits: `4986e7a` (Task 1 RED — failing tests for technicals analyst, committed in earlier session), `4098613` (Task 2 GREEN — analysts/technicals.py implementation + 2 test-side bug fixes folded in). ANLY-02 marked complete in REQUIREMENTS.md (Pending → Complete in traceability table; checkbox `[ ]` → `[x]`).

- **2026-05-03 (Phase 3 / Plan 02 — fundamentals analyst)**: Wave 2 / first analyst lands. `analysts/fundamentals.py` (265 lines) ships pure-function `score(snapshot: Snapshot, config: TickerConfig, *, computed_at: Optional[datetime] = None) -> AgentSignal` deterministically scoring P/E, P/S, ROE, debt/equity, profit_margin against per-`TickerConfig.target_multiples` overrides (when `pe_target` / `ps_target` set) or hard-coded fallback bands (PE_BULLISH_BELOW=15.0, PE_BEARISH_ABOVE=30.0, PS=2/8, ROE=0.05/0.15, DE=0.5/1.5, PM=0.05/0.15, PER_CONFIG_BAND=0.20). Emits one AgentSignal per call with 5-state ladder verdict + 0-100 confidence + per-metric evidence list. Provenance header (INFRA-07) names `virattt/ai-hedge-fund/src/agents/fundamentals.py` and enumerates 6 modifications (5-metric vs 13-metric; per-config target_multiples overrides; module-level threshold constants; 5-state vs 3-state Verdict; pure function vs graph node; UNIFORM RULE empty-data guard). `tests/analysts/test_fundamentals.py` (392 lines, 26 tests) above the plan's ≥18 floor — covers per-config (target_multiples) path, fallback band path, per-metric isolation, empty-data UNIFORM RULE (3 branches), 5-state ladder edges (strong_bullish / strong_bearish / neutral / partial-bullish-boundary / partial-bearish-boundary), provenance header presence, computed_at default + override, ticker normalization round-trip, return-type contract, determinism. **Decisions:** (1) Aggregate normalization divides by max possible score (5), NOT by n_scored — partial-data signals are deliberately damped (1 bullish + 4 None → normalized=0.2 → neutral, NOT strong_bullish). Matches the per-metric isolation contract. (2) 5-state ladder boundaries are STRICT > (not >=) — normalized=0.6 (3/5 bullish) maps to 'bullish' not 'strong_bullish'; forces "strong" verdicts to require ≥4/5 directional consensus. (3) Per-metric helper signatures explicitly differentiate metrics that take per-config overrides (`_score_pe`, `_score_ps` accept `Optional[target]`) from those that don't (`_score_roe` / `_score_de` / `_score_pm` have no target parameter — FundamentalTargets schema has no roe_target / de_target / pm_target). (4) Empty-data branches each emit a distinct evidence reason string ("snapshot data_unavailable=True" / "fundamentals snapshot missing" / "fundamentals.data_unavailable=True" / "all 5 fundamentals metrics missing") — same data_unavailable signal shape but the evidence string disambiguates which path fired for downstream debugging. (5) Per-config band is symmetric ±20% around target. (6) `computed_at` follows 03-01 contract — explicit kwarg or `datetime.now(timezone.utc)` default. (7) `_MAX_POSSIBLE_SCORE = 5` is a private constant — future amendments that change metric count MUST update it in the same commit. (8) ROADMAP.md plan-progress row updated MANUALLY per precedent (gmd-tools roadmap update-plan-progress mangles the descriptive Phase Summary row format). 26 tests green; coverage on `analysts/fundamentals.py` **100% line / 100% branch** (gate ≥90/85). Full repo regression **238 passed + 2 xfailed** (up from 212 + 2 xfailed at end of 03-01: +26 fundamentals tests; the 2 xfail markers in test_invariants.py held strict and remained xfail as expected — Wave 2 needs 3 more analyst modules to flip them green; 03-05 removes the markers as its final task). **Deviations:** 1 auto-fixed (Rule 2 — additive coverage tightening: added `test_per_config_ps_bearish_overvalued` + `test_per_config_ps_neutral_within_band` to clear the 96%→100% coverage gap on per-config P/S branches lines 116-119; without them the coverage stayed above the gate but 2 untested branches were real per-config P/S behaviors the plan promised). Plan-level commits: `a887176` (Task 1 RED — failing tests for fundamentals analyst), `ef8af69` (Task 2 GREEN — analysts/fundamentals.py implementation + 2 P/S coverage-clearing tests). ANLY-01 marked complete in REQUIREMENTS.md (Pending → Complete in traceability table; checkbox `[ ]` → `[x]`).

- **2026-05-01 (Phase 2 / Plan 06 — refresh orchestrator)**: Plan integrates all 5 Wave-2 ingestion modules into a single end-to-end refresh routine with deterministic JSON output. `analysts/data/snapshot.py` (71 lines) ships `Snapshot` per-ticker aggregate (prices/fundamentals/filings/news/social + errors list). `ingestion/manifest.py` (105 lines) ships `Manifest`/`TickerOutcome` + symmetric `write_manifest`/`read_manifest` (atomic-write parity with `watchlist/loader.save_watchlist`). `ingestion/refresh.py` (261 lines) ships `run_refresh(*, watchlist_path, snapshots_root, only_ticker, now)`: per-source try/except isolation in `_fetch_one`, snapshot atomic writer in `_write_snapshot`, deterministic-clock contract (`now if now is not None else datetime.now(timezone.utc)` for both run_started_at AND run_completed_at when `now` is supplied — frozen-time runs produce byte-identical files). `cli/refresh.py` (80 lines) ships the thin shim → run_refresh; ticker normalized at the CLI layer (un-normalizable → exit 1, no orchestrator call); summary printed; errors echoed to stderr; exit 0 always (errors are data, not faults). `cli/main.py` SUBCOMMANDS extended via the documented +4-line precedent (1 import + 1 dict entry, zero dispatcher edits). **Decisions:** (1) Snapshot.errors is list[str] (not dict) — source attribution encoded in the string. (2) data_unavailable propagates iff every sub-source returns nothing useful (DATA-07 contract). (3) TickerOutcome.success couples to the same any_useful predicate; per-source weather doesn't flip success when other sources produced. (4) manifest.errors is run-level only (failed-to-load-watchlist, only_ticker miss); per-ticker errors stay in TickerOutcome. (5) Snapshot-write OSError is caught at orchestrator level → converts to outcome.error="snapshot write failed: ..."; the run keeps moving. (6) `read_manifest` shipped alongside `write_manifest` (Rule 2 — symmetric reader locks the schema; Phase 8 mid-day refresh will need it for staleness checks). 22 tests green, coverage gates cleared (snapshot 100%/100%, manifest 100% line, refresh 91%/100%, cli/refresh 100%/100%); full repo 177/177. **Probes:** 2-W3-01 (`test_full_refresh` + `test_only_ticker`), 2-W3-02 (`test_partial_failure` + `test_partial_failure_all_sources_fail`), 2-W3-03 (`test_manifest_schema` + `test_write_manifest_atomic` + `test_read_manifest_round_trip`), 2-W3-04 (`test_determinism`), 2-W3-05 (`test_refresh_no_arg_invokes_full_refresh` + 5 CLI siblings). DATA-06 + DATA-07 marked complete; DATA-04 (news, shipped earlier under 02-04 but not previously ticked) flipped to `[x]` during this Phase-2 closeout. **Deviations:** 2 auto-fixed (1 cosmetic — Task 1 RED + Task 2 RED tests committed together as `13b8b41` since they share fixtures; 1 Rule 2 — added `read_manifest` symmetric reader for schema-locking). Plan-level commits: `13b8b41` / `ee32f98` / `38c0ae5` / `7c9358d` / `30eaea3`.
