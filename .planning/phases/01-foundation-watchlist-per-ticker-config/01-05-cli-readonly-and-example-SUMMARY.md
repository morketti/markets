---
phase: 01-foundation-watchlist-per-ticker-config
plan: 05
subsystem: cli
tags: [argparse, difflib, pydantic, cli, readonly, onboarding, readme]

requires:
  - phase: 01-foundation-watchlist-per-ticker-config
    provides: "load_watchlist + save_watchlist (Plan 03), normalize_ticker module-level helper (Plan 02), SUBCOMMANDS dict extension surface + format_validation_error (Plan 04)"

provides:
  - "`markets list` — deterministic alphabetical watchlist dump (TICKER/LENS/THESIS/NOTES columns; empty state prints copy-the-add-command hint)"
  - "`markets show TICKER` — full structured per-ticker config dump with WATCH-03-mirroring did-you-mean ergonomics"
  - "`cli/_errors.py:suggest_ticker(unknown, known)` — single-source-of-truth difflib wrapper used by both show_command and remove_command"
  - "`watchlist.example.json` — 5-ticker seed file (AAPL value, NVDA growth, BRK-B value, GME contrarian, V mixed) spanning all four long_term_lens values; BRK-B uses hyphen form per CONTEXT.md correction #1"
  - "`README.md` — quick-start (uv sync → cp example → list/show), per-ticker schema reference table, run-tests instruction, project structure, provenance note"
  - "`tests/test_cli_readonly.py` — 9 tests including the WATCH-01 user-facing demo probe (35-ticker readback) and the WATCH-03 ergonomics-mirror probe (show typo → did-you-mean)"

affects: [phase-2-ingestion, phase-3-analysts, phase-5-routine, phase-6-frontend]

tech-stack:
  added: []  # No new deps — stdlib only (argparse + difflib + pathlib)
  patterns:
    - "Shared CLI helpers: difflib calls live in cli/_errors.py, never inline; future subcommands import suggest_ticker rather than re-implement."
    - "Read-only subcommands print plain text columns (no color/table libs) — argparse-style precedent."
    - "Onboarding via dogfooded example data: watchlist.example.json was generated by running real `markets add` invocations, proving end-to-end correctness while producing the seed file."

key-files:
  created:
    - "cli/list_watchlist.py (45 lines) — `markets list` deterministic dump"
    - "cli/show_ticker.py (83 lines) — `markets show TICKER` structured dump"
    - "watchlist.example.json (65 lines) — 5-ticker seed spanning 4 lenses"
    - "README.md (88 lines) — quick-start + schema reference + provenance"
    - "tests/test_cli_readonly.py (199 lines) — 9 readonly tests"
  modified:
    - "cli/_errors.py — added `suggest_ticker` (61 lines total, was 38; +23)"
    - "cli/main.py — appended `list`/`show` to SUBCOMMANDS dict (4-line patch using Plan 04's documented extension surface)"
    - "cli/remove_ticker.py — replaced inline difflib call with shared `suggest_ticker`; Plan 04 tests stay green with no test changes"

key-decisions:
  - "Locked: `cli/_errors.py:suggest_ticker(unknown, known) -> str | None` is the single source of truth for did-you-mean. Both `cli/remove_ticker.py` and `cli/show_ticker.py` import it; no inline `difflib.get_close_matches` calls remain anywhere outside `cli/_errors.py`. (Closes Plan 04 SUMMARY's deferred decision.)"
  - "Locked: `cli/show_ticker.py` imports `normalize_ticker` from `analysts.schemas` — same pattern as `cli/remove_ticker.py` from Plan 04. Single source of truth across the entire CLI surface."
  - "`markets list` empty watchlist prints a copy-the-add-command hint and exits 0 — empty is not an error (mirrors loader Plan 03 contract that a missing file is a valid first-run state)."
  - "`markets show INVALID_FMT` returns exit 2 (mirrors `markets remove` behavior); `markets show UNKNOWN_BUT_VALID_FMT` returns exit 1 with optional did-you-mean (also mirrors remove). Single error-code convention across the CLI."
  - "Notes column in `markets list` truncated at 40 chars with `...` suffix — keeps output one-line-per-ticker readable even for tickers with verbose notes. Threshold chosen so a typical 80-col terminal shows the full row without wrap."
  - "`watchlist.example.json` was generated by running real `markets add` invocations (not hand-crafted). This dogfoods the entire add → schema → loader → save pipeline while producing the seed file — and it forced BRK-B normalization through the actual code path."

patterns-established:
  - "Shared difflib wrapper in cli/_errors.py: future subcommands needing did-you-mean import `suggest_ticker` rather than re-tune cutoff."
  - "Read-only CLI output is plain text (no rich/tabulate dep)."
  - "Example seed files generated via dogfood, not hand-edited — guarantees they exercise the real schema + loader paths."

requirements-completed: [WATCH-01, WATCH-03]

duration: 4min
completed: 2026-05-01
---

# Phase 1 Plan 05: CLI Readonly + Example Summary

**`markets list` and `markets show` ship the read-only CLI surface with WATCH-01's 30+-ticker user-facing demo, WATCH-03's did-you-mean ergonomics extended to show, a 5-ticker `watchlist.example.json` spanning all four lenses (BRK-B hyphenated), and a quick-start README — closing Phase 1 of the foundation.**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-05-01T04:20:44Z
- **Completed:** 2026-05-01T04:24:35Z
- **Tasks:** 2 (TDD: RED → GREEN; no REFACTOR needed)
- **Files modified:** 8 (5 created, 3 modified)

## Accomplishments

- Read-only CLI surface (`markets list`, `markets show`) ships using shared helpers — zero duplication of `normalize_ticker` or difflib logic across the four-subcommand CLI.
- `cli/_errors.py:suggest_ticker` extracted as the single source of truth for did-you-mean; `cli/remove_ticker.py` updated to use it; Plan 04 tests stay green with no test changes (proving the refactor is internal).
- WATCH-01 user-facing demo proven: `test_list_30_plus_tickers` runs `markets list` over the 35-ticker `large_watchlist_path` fixture and asserts every symbol round-trips through the dispatcher → loader → list output path.
- WATCH-03 ergonomics extended to show: `markets show AAPK` (typo) suggests `AAPL`; `markets show ZZZZZ` (no close match below cutoff 0.6) correctly emits no spurious suggestion.
- `watchlist.example.json` generated by dogfooding `markets add` — proves end-to-end correctness while producing the seed file. Spans all four `long_term_lens` values; BRK-B uses hyphen form (CONTEXT.md correction #1 sentinel preserved).
- README.md ships quick-start (uv sync → cp example → list/show → add/remove), per-ticker schema reference table, run-tests instruction, project structure tree, and provenance note (Phase 1 has no reference-repo adaptations).
- Phase 1 coverage gate cleared: 94% global across `analysts`, `watchlist`, `cli` (gate ≥90%); 100% on `cli/list_watchlist.py`, 98% on `cli/show_ticker.py`.

## Task Commits

1. **Task 1 (RED): failing readonly + example-file tests** — `6f555d3` (test)
2. **Task 2 (GREEN): list/show + example file + README + shared suggest_ticker** — `ba679ce` (feat)

_TDD: REFACTOR step skipped per plan (code already minimal)._

## Files Created/Modified

- `cli/list_watchlist.py` (created, 45 lines) — `markets list` alphabetical dump
- `cli/show_ticker.py` (created, 83 lines) — `markets show TICKER` structured dump
- `cli/_errors.py` (extended +23 lines) — added shared `suggest_ticker` helper
- `cli/main.py` (extended +4 lines) — appended list/show to SUBCOMMANDS + imports
- `cli/remove_ticker.py` (refactored) — uses shared `suggest_ticker`; Plan 04 tests still green
- `tests/test_cli_readonly.py` (created, 199 lines, 9 tests)
- `watchlist.example.json` (created, 65 lines) — 5 tickers, 4 lenses, BRK-B hyphenated
- `README.md` (created, 88 lines) — quick-start + schema reference + provenance note

## Decisions Made

See `key-decisions` in frontmatter. Headlines:

1. **`suggest_ticker` is locked at `cli/_errors.py`** — single source of truth for did-you-mean. No inline `difflib.get_close_matches` anywhere outside that module. Closes Plan 04's deferred decision.
2. **`cli/show_ticker.py` imports `normalize_ticker` from `analysts.schemas`** — same pattern as `cli/remove_ticker.py` from Plan 04. No inline regex.
3. **Empty watchlist via `markets list` is exit 0** — mirrors loader contract (missing file = valid first-run state).
4. **Exit codes mirror remove**: 0 ok / 1 not-found-with-optional-did-you-mean / 2 invalid-format. One error-code convention across the CLI surface.
5. **Notes column truncated at 40 chars + `...`** — readable on 80-col terminals.
6. **Example file generated via dogfood, not hand-edited** — exercises the full add → schema → loader → save pipeline.

## Deviations from Plan

None — plan executed exactly as written.

The one minor refinement: I added 3 extra coverage tests (`test_show_invalid_ticker_format`, `test_show_with_technical_and_targets`, `test_list_truncates_long_notes`) on top of the 6 specified. This was needed to clear the ≥90% Phase 1 coverage gate (initial 6-test run came in at 88%; with the 3 additions it's 94%). All three tests cover named branches in the production code, not new behavior. Logged here for transparency, not as a deviation from the spec — the plan's verification section explicitly allows accepting 88-90% with a note OR adding tests; this chose the latter. No design changes.

**Total deviations:** 0 auto-fixed.
**Impact on plan:** Zero scope creep. Coverage decision documented above.

## Issues Encountered

None. RED step failed cleanly with the expected `argparse.ArgumentError: invalid choice: 'list'`. After GREEN edits, all 6 originally-specified tests passed on first run (the 3 added were additive). Manual smoke tests against the freshly-generated `watchlist.example.json` produced the expected outputs end-to-end.

## Plan Verification — Outputs as Captured

**`markets --help`** (lists all four subcommands as required):

```
usage: markets [-h] {add,remove,list,show} ...

watchlist management — add/remove tickers and per-ticker config

positional arguments:
  {add,remove,list,show}
    add                 add subcommand
    remove              remove subcommand
    list                list subcommand
    show                show subcommand
```

**`markets list --watchlist /tmp/wl.json`** (after `cp watchlist.example.json /tmp/wl.json`):

```
TICKER  LENS         THESIS    NOTES
AAPL    value        200.00    -
BRK-B   value        -         -
GME     contrarian   -         volatility test
NVDA    growth       -         AI infra
V       mixed        -         -
```

**`markets show AAPL --watchlist /tmp/wl.json`** (full structured dump):

```
TICKER: AAPL
short_term_focus: True
long_term_lens: value
thesis_price: 200.0
technical_levels: -
target_multiples: -
notes: (none)
created_at: 2026-05-01T04:22:57+00:00
updated_at: 2026-05-01T04:22:57+00:00
```

**`markets show ZZZZZ --watchlist /tmp/wl.json`** (no close match, no suggestion, exit 1):

```
error: ticker 'ZZZZZ' not in watchlist.
(exit 1)
```

**`markets show AAPK --watchlist /tmp/wl.json`** (typo, did-you-mean, exit 1) — confirms WATCH-03 ergonomics extension to show:

```
error: ticker 'AAPK' not in watchlist. did you mean 'AAPL'?
(exit 1)
```

**BRK-B sentinel** (`grep "BRK[\.\-]B" watchlist.example.json`):

```
14:    "BRK-B": {
22:      "ticker": "BRK-B",
```

Hyphen form only — no `BRK.B` anywhere on disk.

## Final Phase 1 Coverage (`uv run pytest --cov=analysts --cov=watchlist --cov=cli --cov-report=term`)

```
Name                    Stmts   Miss Branch BrPart  Cover
---------------------------------------------------------
analysts\__init__.py        0      0      0      0   100%
analysts\schemas.py        72      0     18      1    99%
cli\__init__.py             0      0      0      0   100%
cli\_errors.py             22      1      6      2    89%
cli\add_ticker.py          39      0      6      0   100%
cli\list_watchlist.py      21      0      6      0   100%
cli\main.py                31      7      4      1    77%
cli\remove_ticker.py       34      6     10      2    77%
cli\show_ticker.py         48      0     10      1    98%
watchlist\__init__.py       0      0      0      0   100%
watchlist\loader.py        25      0      2      0   100%
---------------------------------------------------------
TOTAL                     292     14     62      7    94%
============================= 33 passed in 0.34s ==============================
```

The remaining ~6% miss is concentrated in `cli/main.py` (FileNotFoundError + `__main__` block) and `cli/remove_ticker.py` (interactive `input()` prompt + abort branches). These are integration-style branches that would require subprocess fixtures to exercise — out of scope for unit-level coverage. 94% global beats the ≥90% gate by 4 points.

## Phase 1 Closeout — ROADMAP Success Criteria

All four criteria met:

1. ✓ `watchlist.json` schema with 30+ tickers loads and validates via Pydantic v2 (proven by Plan 03 `test_load_50_tickers_under_100ms` and Plan 05 `test_list_30_plus_tickers`).
2. ✓ CLI utilities `cli/add_ticker.py` and `cli/remove_ticker.py` work end-to-end (Plan 04 tests + Plan 05 smoke tests).
3. ✓ Per-ticker config supports all 6 fields: `short_term_focus`, `long_term_lens`, `thesis_price`, `technical_levels`, `target_multiples`, `notes` (Plan 02 schemas, Plan 04 add CLI flags, Plan 05 show display covers every field).
4. ✓ Invalid configs are rejected with actionable Pydantic error messages (Plan 04 `format_validation_error` + Plan 04 smoke test confirmed `markets add AAPL --thesis -1` → `validation failed (1 error): ...` exit 2).

## Next Phase Readiness

**Ready for `/gmd:verify-work`** — all 21 probes specified in Phase 1's verification matrix are now covered by 33 tests (8 schemas + 7 loader + 9 cli core + 9 cli readonly = 33; some tests cover multiple probes).

**Ready for Phase 2 kickoff** — `/gmd:plan-phase 2` (Ingestion — Keyless Data Plane). Phase 2 needs:
- `watchlist.tickers.keys()` to know which tickers to fetch (provided by Plan 03 `load_watchlist`)
- `TickerConfig.short_term_focus` to skip tactical analysis for tickers opted out (provided by Plan 02 schema)
- The `watchlist.example.json` seed for first-run sanity testing without hand-crafting fixtures (provided by Plan 05)

No blockers, no open architectural questions, no deferred decisions remain in Phase 1.

---

## Self-Check: PASSED

- File `cli/list_watchlist.py`: FOUND
- File `cli/show_ticker.py`: FOUND
- File `cli/_errors.py` (extended): FOUND
- File `cli/main.py` (extended): FOUND
- File `cli/remove_ticker.py` (refactored): FOUND
- File `tests/test_cli_readonly.py`: FOUND
- File `watchlist.example.json`: FOUND
- File `README.md`: FOUND
- Commit `6f555d3` (RED): FOUND
- Commit `ba679ce` (GREEN): FOUND

---
*Phase: 01-foundation-watchlist-per-ticker-config*
*Completed: 2026-05-01*
